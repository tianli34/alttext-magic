## `GET /api/bootstrap` 任务完成

### 现有代码验证

[`api.bootstrap.tsx`](app/routes/api.bootstrap.tsx) 路由和 [`bootstrap.service.ts`](server/modules/bootstrap/bootstrap.service.ts) 服务层已存在且完全符合 §6.1 规格，无需修改：

- **路由层**：鉴权 → shopDomain 查 shopId → 调用 `getBootstrapData` → 返回 JSON
- **服务层**：并行获取 shop/notice/latestScan → 构建 scope 三件套 + plan/quota 占位 → 返回 `BootstrapData`
- **fresh shop 默认态**：`needsNoticeAck: true`、默认四类全开 scope、`lastPublishedScopeFlags: null`、`effectiveReadScopeFlags` 全 false、`latestScan: null`

### 新增 API 测试

[`tests/api.bootstrap.test.ts`](tests/api.bootstrap.test.ts) — 7 个测试用例全部通过：

1. **fresh shop 场景**：`needsNoticeAck=true`、默认四类 scope、`latestScan=null`
2. **返回结构稳定性**：所有 §6.1 约定字段存在且类型正确
3. **有扫描记录**：`latestScan` 字段完整、`isRunning` 与 status 一致
4. **shop 不存在**：返回 404
5. **非 GET 方法**：返回 405
6. **RUNNING 扫描**：`isRunning=true`
7. **effectiveReadScopeFlags 交集计算**：`scanScopeFlags ∩ lastPublishedScopeFlags`

### STATUS.md

已更新 [`docs/STATUS.md`](docs/STATUS.md) 补充 API 测试通过信息。
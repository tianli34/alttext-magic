可以。下面我直接给你一套**可执行的 AI 提示词清单**，你按顺序一批一批喂给 AI 就行。

## 先说使用方法
这套 prompt 的使用原则只有 4 条：

1. **每一批都要附上“当前最新完整的 `schema.prisma`”**
   - 不是最初版本
   - 是上一批改完后的最新版本

2. **每一批都尽量附上本批相关的数据模型文档**
   - 如果不好拆，就直接贴 **§5.1–§5.26 全文**
   - 不要让 AI 靠猜

3. **每一批完成后都本地执行**
   - `npx prisma format`
   - `npx prisma validate`

4. **如果 AI 说信息不足，让它列出 blockers，不要让它硬猜**

---

# Prompt 0：全量盘点与建模计划
> 用途：先让 AI 把 31 张表的依赖、关系、枚举、批次边界梳理清楚。  
> 这一步**先不要改 schema**。

```text
请基于我提供的现有 `schema.prisma` 和数据模型文档，先完成“建模盘点与实施计划”，不要直接输出最终 schema patch。

【任务背景】
P2-02 目标：补齐 Phase 2 所需的 Prisma schema，覆盖以下 31 张表及其关系：
- shops
- scan_notice_ack
- shop_operation_lock
- scan_job
- scan_task
- scan_task_attempt
- stg_product
- stg_media_image_product
- stg_media_image_file
- stg_collection
- stg_article
- scan_result_target
- scan_result_usage
- alt_target
- image_usage
- decorative_mark
- alt_candidate
- candidate_group_projection
- alt_draft
- audit_log
- webhook_event
- resource_image_fingerprint
- job_batch
- job_item
- billing_subscription
- overage_pack_purchase
- credit_bucket
- credit_reservation
- credit_reservation_line
- credit_ledger
- billing_ledger

【硬约束】
- 只改 Prisma schema，不要输出 SQL，不要输出 migration
- 尽量与现有 `Shop` 模型兼容，不重建已有基础字段
- `shops` 对应现有 `Shop` 模型时，只能扩展，不要新建重复模型
- 字段级注释最终需要标明对应架构节号（如 `/// §5.x`）
- 严格复用当前 schema 的命名风格：model 命名、enum 命名、字段命名、`@@map` / `@map` / `@db.*` 风格
- 如果文档信息不足，不要猜；请明确列出 blocker 和待确认项

【请输出以下内容】
1. `MODEL_PLAN`
   - 31 张表分别映射到哪个 Prisma model
   - 是否需要 `@@map("table_name")`
   - 哪些是新增模型，哪些是扩展现有模型

2. `DEPENDENCY_GRAPH`
   - 每张表的主要外键依赖
   - 哪些模型必须先建
   - 哪些关系需要双向字段

3. `ENUM_PLAN`
   - 预计需要新增/复用的枚举
   - 哪些枚举跨多个模型共享

4. `BATCH_PLAN`
   - 把 31 张表分成可执行批次
   - 每批目标、涉及模型、依赖前置

5. `RISK_AND_BLOCKERS`
   - 可能冲突的命名
   - 与现有 `Shop`/已有模型兼容性风险
   - 文档不明确处
   - 必须人工确认的问题

【输出要求】
- 先分析，不要给出完整 schema patch
- 不要写伪代码
- 用清晰编号和表格输出
- 如果你认为我后续应该按批次实施，请明确给出最优批次顺序

【当前完整 schema.prisma】
```prisma
[在这里粘贴当前完整 schema.prisma]
```

【数据模型文档】
```md
[在这里粘贴数据模型文档，最好直接贴 §5.1–§5.26 全文]
```
```

---

# Prompt 1：Shop 扩展 + 店铺级辅助表
> 范围：
- `shops`（扩展现有 `Shop`）
- `scan_notice_ack`
- `shop_operation_lock`

```text
请基于现有 `schema.prisma` 和数据模型文档，完成第 1 批 Prisma schema patch。

【本批范围】
- shops（映射到现有 `Shop` 模型，只扩展，不重建）
- scan_notice_ack
- shop_operation_lock

【本批目标】
1. 扩展现有 `Shop` 模型，但不要破坏已有基础字段
2. 给 `Shop` 新增以下字段：
   - `scan_scope_flags Json`
   - `last_published_scope_flags Json`
3. 新增/补齐：
   - `scan_notice_ack`
   - `shop_operation_lock`
4. 补齐必要关系、反向关系、唯一约束、索引
5. 所有新增或修改字段都要加字段级注释，注明对应架构节号（例如 `/// §5.x`）

【硬约束】
- 只改 Prisma schema，不要输出 SQL / migration
- 不要重写无关模型
- 严格复用当前 schema 的命名风格、`@@map` / `@map` / `@db.*` 风格
- 如果当前 schema 使用 PascalCase model + `@@map("snake_case_table")`，请保持一致
- 对已有 `Shop` 模型：
  - 只允许做“最小增量修改”
  - 不删除、不重命名、不重排已有基础字段
- 如果文档没有明确某个字段/约束，不要擅自猜测；请放到 `BLOCKERS`
- 关系必须双向完整；如需在已有模型上增加最小必要的反向 relation 字段，可以改，但不要引入无关业务字段

【输出格式】
请严格按以下顺序输出：

1. `CHANGE_SUMMARY`
   - 本批新增/修改了哪些 model / enum / relation / index

2. `PATCH`
   - 只输出“新增或修改后的完整 model / enum 块”
   - 不要输出整个 schema
   - 不要使用省略号
   - 输出必须可直接粘贴到 `schema.prisma`

3. `RELATION_CHECKLIST`
   - 列出每条 relation 的两端
   - 标明 relation field / FK field / references

4. `BLOCKERS`
   - 若无 blocker，写 `None`
   - 若信息不足，明确指出缺什么，不要自己脑补

【当前完整 schema.prisma】
```prisma
[在这里粘贴当前最新完整 schema.prisma]
```

【本批相关数据模型文档】
```md
[在这里粘贴本批相关数据模型文档；不确定时直接贴 §5.1–§5.26 全文]
```
```

---

# Prompt 2：扫描主链路
> 范围：
- `scan_job`
- `scan_task`
- `scan_task_attempt`
- `job_batch`
- `job_item`

```text
请基于现有 `schema.prisma` 和数据模型文档，完成第 2 批 Prisma schema patch。

【本批范围】
- scan_job
- scan_task
- scan_task_attempt
- job_batch
- job_item

【本批目标】
1. 新增/补齐上述 5 个模型
2. 补齐它们与 `Shop`、以及彼此之间的关系
3. 补齐必要的状态枚举、唯一约束、索引、默认值
4. 若为了双向关系完整，需要对前一批已存在模型做最小增量修改，只允许新增必要 relation 字段或索引
5. 所有新增或修改字段都要加字段级注释，标明对应架构节号（例如 `/// §5.x`）

【硬约束】
- 只改 Prisma schema，不要输出 SQL / migration
- 不要改动无关模型
- 严格复用当前 schema 的命名风格、`@@map` / `@map` / `@db.*` 风格
- 不要为了“看起来整齐”而重构前一批模型
- 所有 relation 必须双向可用
- 如果文档不清楚某个状态字段、nullable、唯一性，不要猜；写入 `BLOCKERS`

【输出格式】
请严格按以下顺序输出：

1. `CHANGE_SUMMARY`

2. `PATCH`
   - 只输出新增或修改后的完整 model / enum 块
   - 不要输出整个 schema
   - 必须可直接粘贴

3. `RELATION_CHECKLIST`
   - 按模型列出 relation 两端和 FK

4. `ENUM_CHECKLIST`
   - 本批新增/复用的枚举
   - 是否与已有枚举重名/冲突

5. `BLOCKERS`

【当前完整 schema.prisma】
```prisma
[在这里粘贴当前最新完整 schema.prisma]
```

【本批相关数据模型文档】
```md
[在这里粘贴本批相关数据模型文档；不确定时直接贴 §5.1–§5.26 全文]
```
```

---

# Prompt 3：采集 / Staging 内容层
> 范围：
- `stg_product`
- `stg_media_image_product`
- `stg_media_image_file`
- `stg_collection`
- `stg_article`
- `resource_image_fingerprint`

```text
请基于现有 `schema.prisma` 和数据模型文档，完成第 3 批 Prisma schema patch。

【本批范围】
- stg_product
- stg_media_image_product
- stg_media_image_file
- stg_collection
- stg_article
- resource_image_fingerprint

【本批目标】
1. 新增/补齐上述 staging / media / fingerprint 相关模型
2. 补齐它们与 `Shop`、扫描链路模型、以及彼此之间的关系
3. 补齐必要索引、唯一约束、去重键、查找键
4. 若需要对前序模型增加最小必要的反向 relation 字段，可以改，但不要改动业务字段


【硬约束】
- 只改 Prisma schema，不要输出 SQL / migration
- 严格复用当前 schema 的命名和映射风格
- 不要因为本批新增模型而重写 scan_job / scan_task 等已有模型
- 如果文档没有明确某个 media/fingerprint 字段的数据类型、唯一性、nullable，不要猜，写入 `BLOCKERS`
- relation 必须双向闭合

【输出格式】
请严格按以下顺序输出：

1. `CHANGE_SUMMARY`

2. `PATCH`
   - 只输出新增或修改后的完整 model / enum 块
   - 不要输出整个 schema
   - 不要省略任何字段

3. `RELATION_CHECKLIST`

4. `INDEX_AND_UNIQUE_CHECKLIST`
   - 列出本批新增的 `@@index` / `@@unique` / 去重策略

5. `BLOCKERS`

【当前完整 schema.prisma】
```prisma
[在这里粘贴当前最新完整 schema.prisma]
```

【本批相关数据模型文档】
```md
[在这里粘贴本批相关数据模型文档；不确定时直接贴 §5.1–§5.26 全文]
```
```

---

# Prompt 4：扫描结果 / 目标识别层
> 范围：
- `scan_result_target`
- `scan_result_usage`
- `alt_target`
- `image_usage`
- `decorative_mark`

```text
请基于现有 `schema.prisma` 和数据模型文档，完成第 4 批 Prisma schema patch。

【本批范围】
- scan_result_target
- scan_result_usage
- alt_target
- image_usage
- decorative_mark

【本批目标】
1. 新增/补齐扫描结果与目标识别相关模型
2. 明确这些模型与：
   - scan_task / scan_job
   - staging 内容层
   - 图片资源相关模型
   - `Shop`
   之间的关系
3. 补齐必要状态字段、唯一约束、索引、反向关系


4. 若需要对前序模型增加最小必要的 relation 字段或索引，可以修改，但不要重写其业务字段
5. 所有新增或修改字段必须带字段级注释，标明对应架构节号（例如 `/// §5.x`）

【硬约束】
- 只改 Prisma schema，不要输出 SQL / migration
- 严格复用现有 schema 风格
- 不要对 staging 模型做无关重构
- 如果文档没有明确 target / usage / decorative mark 的归属关系、唯一性或状态枚举，不要猜，写入 `BLOCKERS`
- relation 必须双向完整，relation name 不能前后不一致

【输出格式】
请严格按以下顺序输出：

1. `CHANGE_SUMMARY`

2. `PATCH`
   - 只输出新增或修改后的完整 model / enum 块
   - 不要输出整个 schema

3. `RELATION_CHECKLIST`
   - 特别标出：
     - target 归属链
     - usage 归属链
     - 与图片/资源/内容实体的连接方式

4. `ENUM_CHECKLIST`

5. `BLOCKERS`

【当前完整 schema.prisma】
```prisma
[在这里粘贴当前最新完整 schema.prisma]
```

【本批相关数据模型文档】
```md
[在这里粘贴本批相关数据模型文档；不确定时直接贴 §5.1–§5.26 全文]
```
```

---

# Prompt 5：候选文案 / 草稿层
> 范围：
- `alt_candidate`
- `candidate_group_projection`
- `alt_draft`

```text
完成第 5 批 Prisma schema patch。

【本批范围】
- alt_candidate
- candidate_group_projection
- alt_draft

【本批目标】
1. 新增/补齐候选文案与草稿相关模型
2. 补齐它们与 `alt_target`、扫描结果层、`Shop` 的关系
3. 明确 group / candidate / draft 的归属关系、唯一性和状态字段
4. 可以对前序模型增加最小必要的 relation 字段或索引



5. 所有新增或修改字段必须加字段级注释，标明对应架构节号（例如 `/// §5.x`）

【硬约束】
- 只改 Prisma schema，不要输出 SQL / migration
- 严格复用现有 schema 风格
- 不要重构前一批的 target / usage 模型
- 如果 group/candidate/draft 的唯一键、状态枚举、发布态/草稿态不明确，不要猜，写入 `BLOCKERS`
- relation 必须双向完整

【输出格式】
请严格按以下顺序输出：

1. `CHANGE_SUMMARY`

2. `PATCH`
   - 只输出新增或修改后的完整 model / enum 块
   - 不要输出整个 schema

3. `RELATION_CHECKLIST`

4. `UNIQUENESS_CHECKLIST`
   - 列出 candidate / group / draft 的唯一约束与索引

5. `BLOCKERS`

【当前完整 schema.prisma】
```prisma
[在这里粘贴当前最新完整 schema.prisma]
```

【本批相关数据模型文档】
```md
[在这里粘贴本批相关数据模型文档；不确定时直接贴 §5.1–§5.26 全文]
```
```

---

# Prompt 6：审计与事件层
> 范围：
- `audit_log`
- `webhook_event`

```text
请基于现有 `schema.prisma` 和数据模型文档，完成第 6 批 Prisma schema patch。

【本批范围】
- audit_log
- webhook_event

【本批目标】
1. 新增/补齐审计与 webhook 事件相关模型
2. 明确它们与 `Shop`、作业链路、草稿/发布链路的关联方式
3. 补齐事件幂等键、外部事件标识、状态字段、索引、时间字段
4. 若需要对前序模型增加最小必要的 relation 字段或索引，可以修改，但不要改动其业务字段
5. 所有新增或修改字段必须带字段级注释，标明对应架构节号（例如 `/// §5.x`）

【硬约束】
- 只改 Prisma schema，不要输出 SQL / migration
- 严格复用现有 schema 风格
- 不要为了方便而引入不必要的外键
- 若文档未明确 webhook/audit 的实体关联策略、幂等性、payload 存储方式，不要猜，写入 `BLOCKERS`
- relation 必须双向完整；如果某模型应为“弱关联/仅存 id”，请遵循文档，不要擅自改成强 FK

【输出格式】
请严格按以下顺序输出：

1. `CHANGE_SUMMARY`

2. `PATCH`
   - 只输出新增或修改后的完整 model / enum 块
   - 不要输出整个 schema

3. `RELATION_CHECKLIST`

4. `IDEMPOTENCY_AND_INDEX_CHECKLIST`
   - 列出 webhook/audit 相关唯一键、查找键、幂等策略对应的 schema 设计

5. `BLOCKERS`

【当前完整 schema.prisma】
```prisma
[在这里粘贴当前最新完整 schema.prisma]
```

【本批相关数据模型文档】
```md
[在这里粘贴本批相关数据模型文档；不确定时直接贴 §5.1–§5.26 全文]
```
```

---

# Prompt 7：计费与额度层
> 范围：
- `billing_subscription`
- `overage_pack_purchase`
- `credit_bucket`
- `credit_reservation`
- `credit_reservation_line`
- `credit_ledger`
- `billing_ledger`

```text
请基于现有 `schema.prisma` 和数据模型文档，完成第 7 批 Prisma schema patch。

【本批范围】
- billing_subscription
- overage_pack_purchase
- credit_bucket
- credit_reservation
- credit_reservation_line
- credit_ledger
- billing_ledger

【本批目标】
1. 新增/补齐计费、额度、预留、台账相关模型
2. 明确它们与 `Shop` 以及彼此之间的关系
3. 补齐：
   - 订阅状态 / 购买状态 / 桶状态 / 预留状态 / 账务状态 枚举
   - 必要唯一键、查找索引、幂等键、外部账单引用
   - reservation / line / bucket / ledger 的归属链
4. 若需要对已有模型增加最小必要的 relation 字段或索引，可以修改，但不要改动无关业务字段


【硬约束】
- 只改 Prisma schema，不要输出 SQL / migration
- 严格复用现有 schema 风格
- 不要擅自定义账务规则；只反映文档中明确的结构
- 如果文档未明确正负方向、余额字段、幂等键、外部引用键、生命周期状态，不要猜，写入 `BLOCKERS`
- relation 必须双向完整
- 避免重复定义 ledger 语义相近但命名冲突的枚举

【输出格式】
请严格按以下顺序输出：

1. `CHANGE_SUMMARY`

2. `PATCH`
   - 只输出新增或修改后的完整 model / enum 块
   - 不要输出整个 schema

3. `RELATION_CHECKLIST`
   - 特别列出：
     - subscription -> purchase
     - bucket -> reservation -> reservation_line
     - ledger 与 bucket / reservation / billing 实体的连接

4. `ENUM_CHECKLIST`

5. `INDEX_UNIQUE_IDEMPOTENCY_CHECKLIST`

6. `BLOCKERS`

【当前完整 schema.prisma】
```prisma
[在这里粘贴当前最新完整 schema.prisma]
```

【本批相关数据模型文档】
```md
[在这里粘贴本批相关数据模型文档；不确定时直接贴 §5.1–§5.26 全文]
```
```

---

# Prompt 8：最终总对齐 / 补漏 / 一致性修复
> 用途：前 7 批都完成后，做最后总审查。  
> 这一步非常关键。

```text
下面这份 `schema.prisma` 已经分批实现了 P2-02。请你进行“最终总对齐与补漏”，目标是让 31 张表/模型在 Prisma 层面保持一致、可验证、可维护。

【全量范围】
- shops
- scan_notice_ack
- shop_operation_lock
- scan_job
- scan_task
- scan_task_attempt
- stg_product
- stg_media_image_product
- stg_media_image_file
- stg_collection
- stg_article
- scan_result_target
- scan_result_usage
- alt_target
- image_usage
- decorative_mark
- alt_candidate
- candidate_group_projection
- alt_draft
- audit_log
- webhook_event
- resource_image_fingerprint
- job_batch
- job_item
- billing_subscription
- overage_pack_purchase
- credit_bucket
- credit_reservation
- credit_reservation_line
- credit_ledger
- billing_ledger

【你的任务】
请不要新增“文档之外的新业务字段”，只做以下 8 类事情：

1. 检查并修复所有双向 relation 是否完整
2. 检查 relation name、FK 字段、references 是否一致
3. 检查是否存在重复或冲突的 enum，统一命名
4. 检查 `@@index` / `@@unique` / 复合唯一键 是否遗漏
5. 检查现有 `Shop` 模型是否被错误重构；确保只是兼容性扩展
6. 检查 `@@map` / `@map` / 原生类型风格 是否与现有 schema 一致
7. 检查所有新增/修改字段是否都有对应架构节号注释（如 `/// §5.x`）
8. 找出明显会导致 `prisma validate` 失败或后续维护困难的问题

【硬约束】
- 只改 Prisma schema，不要输出 SQL / migration
- 不要做大规模重构
- 不要重命名已有模型/字段，除非这是修复明显冲突所必需；若必须改名，请先在 `HIGH_RISK_FIXES` 中明确说明
- 不要因为“也许更合理”而修改文档未要求的业务结构
- 如果有无法确定的问题，请写入 `BLOCKERS`，不要擅自脑补

【输出格式】
请严格按以下顺序输出：

1. `FINAL_AUDIT_SUMMARY`
   - 本次发现了哪些问题
   - 修复了哪些问题
   - 哪些问题仍需人工确认

2. `PATCH`
   - 只输出最终需要修改的 model / enum 块
   - 不要输出整个 schema
   - 必须可直接粘贴

3. `COVERAGE_CHECKLIST`
   - 按 31 张表逐个确认：
     - 是否已建模
     - 是否有主要关系
     - 是否有必要索引/唯一键
     - 是否有字段级节号注释

4. `HIGH_RISK_FIXES`
   - 如果本次存在可能影响兼容性的修复，逐条说明
   - 若无，写 `None`

5. `BLOCKERS`
   - 若无，写 `None`

【当前完整 schema.prisma】
```prisma
[在这里粘贴前 7 批完成后的最新完整 schema.prisma]
```

【数据模型文档】
```md
[在这里粘贴数据模型文档，建议直接贴 §5.1–§5.26 全文]
```
```

---

## 建议你实际执行的顺序
按这个顺序最稳：

1. Prompt 0：全量盘点  
2. Prompt 1：Shop + 店铺辅助表  
3. Prompt 2：扫描主链路  
4. Prompt 3：Staging 内容层  
5. Prompt 4：扫描结果层  
6. Prompt 5：候选/草稿层  
7. Prompt 6：审计/事件层  
8. Prompt 7：计费/额度层  
9. Prompt 8：最终总对齐  

---

## 两个额外建议

### 建议 1：每批都让 AI “只输出 patch”
这样最好 review，避免它重写整个 schema。

### 建议 2：如果某批关系很多，可以再拆成两次
比如：
- Prompt 3 拆成：
  - 3A：`stg_product / stg_collection / stg_article`
  - 3B：`stg_media_image_product / stg_media_image_file / resource_image_fingerprint`

如果你想，我下一步可以继续帮你做两件事里的任意一个：

1. **把这 9 个 prompt 再压缩成“更短版”**，适合直接贴给 Cursor / Claude  
2. **给你一份“每批 review checklist”**，让你审 Prisma patch 时知道重点看什么
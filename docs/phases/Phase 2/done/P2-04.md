已实现安装时的额度初始化，并接到了 Shopify `afterAuth` 流程上。`[app/shopify.server.ts](E:/alttext-magic/app/shopify.server.ts)` 现在改为调用 `[shop-install.service.ts](E:/alttext-magic/app/lib/server/shop/shop-install.service.ts)`；新服务会在一个 Prisma 事务里完成 `shop upsert`，然后幂等创建两个 bucket：`WELCOME(50)` 和当月 `FREE_MONTHLY_INCLUDED(25)`。

幂等策略依赖 `credit_bucket(shop_id, bucket_type, cycle_key)` 唯一约束，并用 `createMany({ skipDuplicates: true })` 落库。因为文档只明确了 Free 月额度的 `cycle_key=FREE:YYYY-MM`，但没定义安装欢迎额度的 `cycle_key`，我这里补了固定键 `WELCOME:INSTALL`，否则 PostgreSQL 对 `NULL` 不会真正防重。状态已同步记录到 `[docs/STATUS.md](E:/alttext-magic/docs/STATUS.md)`。

已验证：`pnpm exec tsc --noEmit` 通过。
### Scope 服务实现完成

**修改文件**

1. **[`scope.service.ts`](server/modules/shop/scope.service.ts)** — 完整重写，实现三个核心函数：

   - [`getScopeSettings(shopId)`](server/modules/shop/scope.service.ts:93) — 从 DB 读取 `scanScopeFlags` + `lastPublishedScopeFlags`，计算 `effectiveReadScopeFlags`，shop 不存在时回退默认四类全开
   - [`updateScanScopeFlags(shopId, flags)`](server/modules/shop/scope.service.ts:126) — zod 校验输入 → 仅更新 `scan_scope_flags` + `scan_scope_updated_at`，**绝不改** `last_published_scope_flags`
   - [`computeEffectiveReadScopeFlags(scanScopeFlags, grantedReadScopes)`](server/modules/shop/scope.service.ts:62) — 纯函数，计算 `scanScopeFlags ∩ grantedReadScopes`（即 `lastPublishedScopeFlags`），`grantedReadScopes` 为 null 时返回全 false

2. **[`scope.service.test.ts`](tests/scope.service.test.ts)** — 新建单测，覆盖：
   - 默认值四类全开
   - `normalizeScanScopeFlags` 合法/非法输入（缺少字段、额外字段、类型错误、null、数字等均报错）
   - `computeEffectiveReadScopeFlags` 交集计算（granted 为 null → 全 false、两者全开、部分重叠、完全不相交等场景）

3. **[`STATUS.md`](docs/STATUS.md)** — 极简更新完成项

**验收对照**
- ✅ 输入非法 flag 会报错（zod `scopeFlagStateSchema.parse` 抛出）
- ✅ 更新后 `last_published_scope_flags` 不变（Prisma `data` 只包含 `scanScopeFlags` + `scanScopeUpdatedAt`）
- ✅ fresh shop 默认四类全开（`DEFAULT_SCAN_SCOPE_FLAGS` + `getScopeSettings` 回退逻辑）
- ✅ 有单测（`npx tsx tests/scope.service.test.ts` 通过）

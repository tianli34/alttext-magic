## P3-02 完成：`POST /api/scan/start` API 实现

### 修改/新增文件清单

| 文件 | 说明 |
|------|------|
| [`server/modules/scan/scan.types.ts`](server/modules/scan/scan.types.ts) | 扫描模块共享类型：`CreateScanJobParams`、`CreateScanJobResult`、`ScanProgressData`、`ScanStartResponse` |
| [`server/modules/scan/scan.constants.ts`](server/modules/scan/scan.constants.ts) | Redis 进度键前缀 `scan:progress`、`ScopeFlag→ScanResourceType` 映射、`scopeFlagsToResourceTypes()` |
| [`server/modules/scan/catalog/scan-job.service.ts`](server/modules/scan/catalog/scan-job.service.ts) | `createScanJobWithTasks()` — 单一 Prisma 事务内原子创建 `scan_job` + 按 scope 创建 `scan_task` |
| [`server/sse/progress-publisher.ts`](server/sse/progress-publisher.ts) | Redis 进度发布器：`initScanProgress()`、`incrementScanProgress()`、`setScanProgressStatus()`、`getScanProgress()` |
| [`app/routes/api.scan.start.tsx`](app/routes/api.scan.start.tsx) | 完整 API 路由：鉴权→zod 校验→获取 SCAN 锁→ackNotice→updateScope→事务创建 Job+Tasks→Redis 进度初始化→BullMQ 入队→返回 `scanJobId/batchId/status` |
| [`tests/api.scan.start.test.ts`](tests/api.scan.start.test.ts) | 10 条路由层测试全部通过 |
| [`docs/STATUS.md`](docs/STATUS.md) | 更新 P3-02 完成记录 |

### 验收标准对照

- ✅ **成功返回 `scanJobId/batchId`** — 响应体包含 `{ scanJobId, batchId, status }`
- ✅ **产生对应 `scan_job` 和 `scan_task`** — `createScanJobWithTasks` 事务内原子创建 1 个 scan_job + N 个 scan_task（按 scope）
- ✅ **扫描锁生效** — `acquireLock(shopId, "SCAN", lockOwner)` 保证 shop 级互斥
- ✅ **重复启动扫描时返回 409** — 锁冲突时返回 `{ error: "Another scan is already running..." }` + 409
- ✅ **TypeScript 编译通过** — `tsc --noEmit` 无错误
- ✅ **测试全部通过** — `npx tsx tests/api.scan.start.test.ts` ✅
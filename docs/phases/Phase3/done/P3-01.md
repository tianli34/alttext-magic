Task P3-01 首次扫描说明页前端已完成，所有 TypeScript 编译错误已修复（`npx tsc --noEmit` 零错误）。

### 交付文件

1. **[`ScanNotice.tsx`](app/components/onboarding/ScanNotice.tsx)** — 扫描说明展示组件
   - 四块说明内容：扫描范围、用途说明、留存期限、AI 使用边界
   - 确认勾选 checkbox（受控组件，`acknowledged` + `onAcknowledgeChange`）
   - 全部使用 Polaris Web Components（`s-box`, `s-stack`, `s-heading`, `s-paragraph`, `s-text`）
   - 修复了所有无效属性值：`background="subdued"` 替代 `bg-surface-secondary`，`padding="small"` 替代 `tight`，移除 `s-heading level`、`s-text size/weight` 等不存在的属性

2. **[`ScopeSelector.tsx`](app/components/onboarding/ScopeSelector.tsx)** — Scope 复选框组件
   - 4 类图片 scope 复选框：PRODUCT_MEDIA / FILES / COLLECTION_IMAGE / ARTICLE_IMAGE
   - 全选 / 取消全选按钮
   - 至少选一个校验提示

3. **[`app.onboarding.tsx`](app/routes/app.onboarding.tsx)** — 说明页路由
   - `loader`：鉴权 → 获取 bootstrap 数据 → `needsNoticeAck=false` 则 302 重定向 `/app`
   - 表单校验：必须勾选确认 + 至少一个 scope
   - 提交：`POST /api/scan/start`，成功后 `navigate("/app")`
   - 错误提示使用 `s-text tone="critical"` / `tone="caution"`

4. **[`api.scan.start.tsx`](app/routes/api.scan.start.tsx)** — 后端扫描启动 API
   - 鉴权 → zod 校验请求体 → 至少一个 scope → `acquireLock` → `ackNotice` → `updateScanScopeFlags` → `prisma.scanJob.create` → BullMQ `scan-start` 队列入队
   - 失败时自动 `releaseLock` 回滚

5. **[`STATUS.md`](docs/STATUS.md)** — 已更新完成记录
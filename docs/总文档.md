
---

# 产品意图书 v1.3｜AltText Magic：Shopify 图片 Alt Text 批量补齐与增长

---

## 1. 产品愿景与定位

**AltText Magic** 是一款面向 **Shopify 中小商家** 的轻量 App，用 AI 在“可控、可审阅”的前提下，为店铺批量补齐缺失的图片 Alt Text，帮助商家改善**图片层面的无障碍基础能力**，降低因图片缺失 Alt Text 带来的**部分风险暴露**，并提升**图片 SEO 流量**。

---

## 2. 目标用户与核心痛点（Why Now）

### 目标用户（Who）

- **拥有大量店铺图片** 的 Shopify 中小商家
- 面向欧美市场，受到 **ADA（美国）/ EAA（欧盟）** 等无障碍要求影响
- 缺少专职 SEO 或无障碍优化团队，时间碎片化，对批量操作风险敏感

### 首月规模预期

| 指标 | 预估值 |
| :-- | :-- |
| 安装店铺数 | ~100 |
| 付费店铺数 | ~5 |
| 单店平均图片数（含产品 / 文件库 / 集合 / 文章等） | ~500–800 张 |

### 核心痛点（Problem）

1. **图片无障碍基础缺失**：图片缺少 Alt Text 会影响屏幕阅读器理解，也会增加投诉、整改和潜在法律咨询成本
2. **SEO 流量损失**：Google 难以理解图片内容，图片搜索几乎无收益
3. **人工成本高**：逐张写 Alt Text 枯燥且耗时，质量不稳定

### 现有替代方案的不足（How they cope）

- 不写，风险与流量双损失
- 手写，2–3 天起步且不稳定
- 用标题充数，重复、单一、不是最佳实践
- 外包，成本高且仍需校对
- 模板工具“看不见图片”，描述空泛

**核心洞察：** 市场缺的不是“能生成文本”，而是一个**又快、又准、又可控**的批量工作流。

---

## 3. 核心用户旅程与首要体验（First Action）

### 首要动作（First Action）

用户首次打开 App 后，第一件事不是立刻开始扫描，而是先进入**扫描说明与设置页**，确认以下信息后，再主动点击 **“开始扫描”**：

- 扫描范围
- 扫描用途
- 数据留存期限
- 第三方 AI 使用边界

### 扫描策略

#### 全量扫描（Catalog Scan）

| 场景 | 策略 |
| :-- | :-- |
| **用户首次打开 App** | 先展示扫描说明与设置页，默认勾选全部支持的图片类型，并明确：扫描阶段不会发送图片给第三方 AI；只有用户后续主动选择“生成 Alt Text”时，才会发送必要的图片 URL 与上下文给 AI 服务 |
| **用户确认开始扫描后** | 触发全店异步全量扫描，覆盖产品媒体、文件库、集合、文章等图片类型，同时展示“正在扫描您的店铺…”进度动画 |
| **扫描过程中** | 展示进度状态或骨架屏，避免白屏等待 |
| **扫描完成后** | 保存最近一次扫描结果，用于快速打开仪表盘并按图片类型分组展示 |
| **用户再次打开 App** | 优先展示最近一次扫描结果，并提供“重新扫描”按钮刷新全量数据 |

#### 增量扫描（Continuous Scan）——付费计划专属

| 场景 | 策略 |
| :-- | :-- |
| **计划限制** | 增量扫描为 **付费计划专属功能**；Free 计划依赖用户手动“重新扫描”刷新数据 |
| **产品 / 集合等资源发生变更** | 对支持自动同步的资源类型执行增量刷新 |
| **自动同步原则** | 仅在与图片相关的变更发生时刷新，尽量过滤明显与图片无关的变更，避免无效重扫 |
| **文章 / 文件库变更** | MVP 阶段暂不自动同步，由用户通过“重新扫描”手动刷新 |
| **升降级联动** | 用户使用付费计划时开启自动增量同步能力；降至 Free 后关闭该能力 |

### 体验目标

- **首次进入**：清楚说明扫描范围、用途、留存期限与第三方 AI 使用边界，让用户在知情前提下开始扫描
- **首次扫描**：展示进度动画，完成后立即渲染仪表盘
- **再次访问**：快速打开最近一次扫描结果，支持一键刷新；付费计划支持的资源类型可自动保持较新状态
- 仪表盘按图片类型分组展示：总图片数 / 已有 Alt Text / 缺失数量 / 已标记为装饰性数量
- 首页顶部展示当前计划、剩余月配额、剩余欢迎额度、剩余超额包额度
- 醒目展示**上次扫描时间**
- 从“诊断”自然进入“生成与回写”，形成闭环

---

## 4. 产品范围：MVP

> 原则：先把“扫描 → 生成 → 审阅 → 批量写回”跑通，且体验底线不被破坏。

### P0 功能清单

**安装 & 鉴权（Shopify App 基础））**

- OAuth 安装、Session 持久化
- **嵌入式 App（Embedded App）**：运行于 Shopify Admin 内，遵循 Shopify Polaris 设计系统
- `app/uninstalled` Webhook
- GDPR Webhook（数据访问 / 删除等）
- 付费计划支持产品 / 集合资源的自动增量刷新能力

**全店扫描（诊断）**

生成、写回过程中禁用扫描，反之亦然。

首次扫描前，必须向用户清楚说明：

- 扫描覆盖的资源范围
- 扫描结果的用途
- 扫描结果与草稿、审计记录的留存期限
- 扫描阶段不会发送图片给第三方 AI
- 仅在用户主动执行“生成 Alt Text”时，才会发送必要图片 URL 与上下文给 AI 服务

扫描范围覆盖以下**四类**图片资源，按优先级排列：

| 优先级 | 图片类型 | 说明 |
| :-- | :-- | :-- |
| A | **产品媒体（Product Media）** | 商家最关心、最直接影响转化与图片搜索的图片集合 |
| B | **文件库图片（Files）** | 覆盖全店通用素材、营销图、主题上传图等 |
| C | **集合横幅 / 封面图（Collection Image）** | 作为独立资源类型处理 |
| D | **文章封面图（Article Image）** | 博客文章封面图 |

*注：富文本内嵌图片因涉及 HTML 解析与改写风险，MVP 阶段不包含其扫描与处理，留至 V2 实现。*

- 支持**异步全量扫描**，适配大批量店铺
- 增量扫描为**付费计划专属**：MVP 阶段覆盖产品、集合；文章、文件库仍由用户手动刷新
- 统计并列出所有 Alt Text 为空的条目，按图片类型和所属资源分组
- 输出可操作列表：缩略图、所属资源名称、图片位置、当前 Alt 状态、图片类型标签
- 商家可在扫描设置中**勾选需要处理的图片类型**，默认全选 A–D
- 保存最近一次扫描结果，支持再次访问快速展示；支持一键“重新扫描”
- 扫描不消耗图片配额，**所有计划均不限扫描次数**

**AI 生成 Alt Text（可控生成）**

- 仅在用户主动发起生成时，系统才会将**Shopify 图片 URL**与必要上下文发送给具备图像理解能力的 AI 服务
- **图片不落盘、不长期缓存、不存储原图**
- 输出英文 Alt Text（≤ 125 字符），强调：简洁、具体、无关键词堆砌
- 配额扣减时机：**成功生成即扣额度**，无论后续是否写回
- 生成前明确展示：本次预计消耗张数、当前剩余月配额、剩余欢迎额度、剩余超额包额度

**预览 / 编辑 / 批量写回（可审阅执行）**

- 列表视图：缩略图 + 图片类型标签 + AI 草稿 + 可编辑输入框
- **装饰性图片标记**：每张图片旁提供“标记为装饰性（跳过）”操作。被标记的图片将从本次写回批次中排除且不生成 Alt Text；标记状态持久化存储，后续扫描中该图片不再计入“缺失”统计，仪表盘单独展示“已标记为装饰性”的数量；用户可随时取消标记，使其重新进入待处理列表
- 支持全选 / 单选写回，可按图片类型筛选

写回要求如下：

| 图片类型 | 写回要求 | 备注 |
| :-- | :-- | :-- |
| 产品媒体 / 文件库图片 | 更新对应图片 Alt Text | 若同一共享文件被多个位置复用，Alt Text 更新可能全局生效，写回前需明确提示用户 |
| 集合横幅图 | 更新集合图片 Alt Text | 作为独立资源类型处理 |
| 文章封面图 | 更新文章图片 Alt Text | — |

- 写回前必须展示二次确认
- 写回后保存变更记录（新 Alt / 图片 ID / 图片类型 / 时间）用于审计追溯

**进度反馈（可预期）**

- 扫描 / 生成 / 写回三阶段进度
- 失败项单独展示，并支持重试
- 不允许“静默失败”

**安全护栏（默认安全）**

- **仅处理 Alt 为空的图片，不处理已有 Alt Text 的图片**
- **装饰性图片保护**：避免为本应保持空 Alt 的装饰性图片错误添加描述性文本
- 二次确认弹窗 + 变更记录可在 App 内查阅
- **不提供任何导出功能**，变更记录仅 App 内查看

**计费与配额**

- 接入 Shopify 订阅计费，支持**月付**与**年付**
- 计划分为 **Free / Starter / Growth / Pro / Max** 五档
- 配额按图片张数计，不区分图片类型，**1 张图片 = 1 次 AI 生成调用**
- **月配额按月发放并按月重置**
- **Free 计划无超额包**；额度用尽后，需等待下月重置或升级付费计划
- **Starter / Growth / Pro / Max** 支持按所属计划购买超额包；超额包必须由用户主动购买，不做自动超扣
- 扫描不消耗图片配额，**所有计划不限扫描次数**
- **增量扫描为付费计划专属功能**，Free 计划依赖手动“重新扫描”
- 欢迎额度、超额包规则详见第 9 节“定价与计费策略”

**数据留存策略**

- 最近一次扫描结果缓存：保留当前最新一份，用于再次打开 App 时快速展示；新扫描完成后覆盖旧缓存；卸载 App 时删除
- 生成草稿（AI 产出但未写回的 Alt Text）：保留 **30 天**后自动清除
- 写回审计记录（已写回的变更日志）：保留 **90 天**后自动清除
- 装饰性图片标记：持久化存储，不自动清除，用户可随时取消标记
- 用户可在留存期内随时在 App 内查阅历史记录

---

## 5. 体验底线（不可妥协的产品原则）

1. **不写错**：生成内容必须与图片一致，避免误导
2. **不乱改**：MVP 阶段**完全不处理已有 Alt Text 的图片**
3. **不“假完成”**：写回失败必须明确提示、可定位、可重试
4. **不产垃圾**：避免“image of / photo of”、关键词堆砌、重复啰嗦
5. **不越权不泄露**：权限最小化、数据处理透明、支持 GDPR 流程
6. **不失控批量**：必须有预览、抽检能力；变更记录在 App 内可查阅，不提供导出
7. **不越界风险**：富文本内嵌图片处理留至 V2，绝不触碰商家原有富文本结构
8. **不做隐形扣费**：生成前必须展示预计消耗张数与当前可用额度；超额包只能在用户明确确认后购买
9. **不破坏图片语义**：装饰性图片不应被强制添加描述性 Alt Text，用户可标记为装饰性以保留空 Alt
10. **不夸大承诺**：产品只改善图片 Alt Text，不宣称替代整站无障碍审查、法律审查或合规认证

---

## 6. 成功指标（MVP 验证标准）

### 业务 / 增长指标

- 安装 → 首次开始扫描转化率（Activation）
- 扫描 → 生成 → 写回完成率（Core Loop Completion）
- 单店铺平均补齐图片数、平均耗时（Time-to-Value）
- 各图片类型的处理覆盖率（产品媒体 / 文件库 / 集合 / 文章）
- 付费转化：Free → 付费率（首月目标 ~5%）、月留存
- 各付费计划分布、年付占比
- 超额包购买率、单店月均消耗张数
- 装饰性图片标记使用率

### 质量与风险指标（关键）

- 写回成功率（目标：>99%）
- 生成文本被用户编辑比例
- 客诉：误描述、数据合规问题、计费争议（目标：0 重大事故）
- 客户支持响应时效达标率

---

## 7. 非目标（MVP 暂不做）

- 全站 SEO 策略（站点结构、元标签等）
- 多语言 Alt Text
- 对已有 Alt Text 的任何处理（包括优化、重写、覆盖）
- 深度图片理解的行业模板库
- 批量回滚 / Undo
- 任何形式的数据导出功能（变更记录、扫描结果等）
- **富文本内嵌图片的任何处理**
- 文章 / 文件库的自动增量刷新
- Free 计划自动超额续购
- Free 计划增量扫描
- 付费计划按张实时自动扣费
- AI 自动判断装饰性图片
- **任何法律、无障碍或合规认证结论的输出**

---

## 8. 风险与对策（上线前必须想清楚）

| 风险 | 对策 |
| :-- | :-- |
| **AI 偶发误判** | 默认“仅填空缺”、强制预览、抽检建议；因不支持回滚，需在写回前强化审阅体验 |
| **Shopify API 限流** | 异步批量处理 + 队列 + 重试 |
| **写回部分失败** | 明确失败清单、逐项重试、不可静默失败 |
| **合规与信任** | 权限最小化、数据处理透明、首次扫描前明确说明；同时清楚声明本产品不替代法律、无障碍或合规专业审查 |
| **共享文件 Alt 更新影响范围较大** | 写回前向用户明确提示共享文件的 Alt 更新可能全局生效 |
| **多资源类型写回复杂度** | 不同图片类型分别验证写回逻辑与错误处理 |
| **AI 服务可用性或质量波动** | 预留替代服务能力，持续监控生成质量与稳定性 |
| **额度与超额包理解偏差** | UI 实时展示“月配额 / 欢迎额度 / 超额包”三类余额与本次预计消耗 |
| **升级 / 降级引发额度争议** | 在账单页与 FAQ 中同步说明升级、降级、欢迎额度与超额包规则 |
| **自动增量刷新事件丢失 / 延迟** | 引导用户必要时手动重新扫描，并对数据时效性进行提醒 |
| **自动增量刷新噪音过大** | 仅在图片相关变更时刷新，减少无效重扫 |
| **Free 用户数据时效性** | 在仪表盘醒目展示“上次扫描时间”，数据较旧时提示手动重新扫描或升级付费计划 |
| **装饰性图片误加 Alt Text** | 提供“标记为装饰性（跳过）”功能，被标记图片不参与生成与写回，不计入缺失统计 |

---

## 9. 定价与计费策略

### 定价模型：Freemium + 阶梯订阅 + 超额包

| 计划 | 月价 | 年付月价（-30%） | 月配额 | 超额包 | 说明 |
| :-- | :-- | :-- | :-- | :-- | :-- |
| **Free** | $0 | — | 25 张 | 无 | 欢迎额度 50 张 |
| **Starter** | $4.99 | $3.49 | 150 张 | $2.99 / 100张 | 欢迎额度 +200 张；增量扫描 |
| **Growth** | $9.99 | $6.99 | 350 张 | $4.99 / 200张 | 欢迎额度 +500 张；增量扫描 |
| **Pro** | $14.99 | $10.49 | 800 张 | $7.99 / 400张 | 欢迎额度 +1000 张；增量扫描 |
| **Max** | $24.99 | $17.49 | 2000 张 | $9.99 / 800张 | 欢迎额度 +3000 张；增量扫描 |

> **欢迎额度说明：** 所有店铺安装时获得 50 张欢迎额度；首次开通付费计划时再获得对应计划的欢迎额度，汇入同一欢迎额度池。  
> **增量扫描说明：** 自动保持产品 / 集合数据较新，为付费计划专属功能。

### 计费规则

| 决策项 | 规则 |
| :-- | :-- |
| 配额单位 | 图片张数，**1 张图片 = 1 次 AI 生成调用** |
| 扣减时机 | **成功生成**即扣额度，无论后续是否写回 |
| 扫描计费 | 扫描（含全量扫描与增量扫描）**不消耗**配额 |
| 月配额发放 | 所有计划均按**月配额**发放；月付按月重置，年付按年一次性发放全年总量并重置 |
| 年付说明 | 表中“年付月价”为折算展示价；年付订单按年收取 |
| Free 计划 | 含 25 张月配额；**无超额包**；**无增量扫描**，依赖手动“重新扫描” |
| 付费计划 | Starter / Growth / Pro / Max 含各自月配额；支持按所属计划购买超额包；含增量扫描 |
| 超额包购买 | 超额包**不自动触发**，必须由用户主动购买并确认后生效 |
| 超额包适用范围 | 用户只能购买当前订阅计划对应的超额包规格与价格 |
| 欢迎额度 | 统一的一次性奖励额度池，分为**安装赠送**与**首次付费赠送**两阶段 |
| 欢迎额度发放次数 | 安装赠送与付费赠送**各仅发放一次**；后续升降级不重复叠加发放 |
| 额度消费顺序 | **优先消耗当月月配额，再消耗欢迎额度，最后消耗超额包额度** |
| 欢迎额度有效期 | 欢迎额度为一次性额度，**不随月重置**，可跨月结转直至用完或店铺卸载 App |
| 超额包有效期 | 已购买的超额包额度**不自动续购**，可跨月结转直至用完或店铺卸载 App |
| 升级规则 | 升级后按新计划发放后续月配额并开启增量扫描；历史欢迎额度与已购超额包保留 |
| 降级规则 | 降级后按新计划规则发放后续月配额；降至 Free 后不可再购买超额包且增量扫描关闭，但历史未用完的欢迎额度 / 超额包额度仍可继续消耗 |
| 退款与争议处理 | 所有额度消耗与购买记录须在账单页可查；超额包购买前需二次确认 |

---

## 10. 技术约束与实现原则

> 本节只保留平台硬约束与能力要求，不固定具体部署、协议、模型、API 版本和实现路线。

| 项目 | 要求 / 原则 | 备注 |
| :-- | :-- | :-- |
| App 类型 | **嵌入式 App（Embedded App）** | 运行于 Shopify Admin 内 |
| UI 约束 | **Shopify Polaris + App Bridge** | 属于平台硬性要求 |
| 全量扫描能力 | 需支持官方允许的异步批量扫描能力 | 适配大批量店铺 |
| 增量刷新能力 | 付费计划对产品 / 集合支持自动增量刷新；Free 计划依赖手动刷新 | 文章 / 文件库在 MVP 阶段手动刷新 |
| 自动刷新原则 | 应尽量过滤与图片无关的变更，减少无效重扫 | 提升效率与稳定性 |
| 图片范围 | 产品媒体 / 文件库 / 集合 / 文章 | 富文本内嵌图片留至 V2 |
| 写回能力 | 需按不同图片资源类型调用 Shopify 支持的对应更新接口 | 共享文件更新可能全局生效，需提示 |
| AI 生成能力 | 使用具备图像理解能力的 AI 服务生成英文 Alt Text | 供应商与模型由研发根据质量、成本、可用性决定 |
| 图片处理边界 | 扫描阶段不向第三方 AI 发送图片；生成阶段仅发送必要图片 URL 与上下文；图片不落盘、不长期保存原图 | 数据处理需透明 |
| 进度与可靠性 | 必须提供扫描 / 生成 / 写回进度、失败提示与重试能力 | 不可静默失败 |
| 数据留存 | 扫描结果缓存保留最近一份；草稿 30 天；审计记录 90 天；装饰性标记持久化 | App 内可查阅 |

---

## 11. 客户支持计划

> MVP 阶段的最小可用支持方案，满足 Shopify App Store 上架要求与基本用户体验。

| 项目 | 方案 |
| :-- | :-- |
| 主要支持通道 | **邮件支持**（support@alttext-magic.com 或等效邮箱），在 App 内“帮助 / 反馈”入口与 App Store 列表页均可见 |
| 响应 SLA | 工作日 **24 小时内**首次响应；非工作日 **48 小时内**首次响应 |
| App 内帮助入口 | 在 App 导航栏或页脚提供“帮助与反馈”链接 |
| FAQ / 帮助文档 | 上线前准备最小 FAQ 页面，覆盖：安装与权限说明、首次扫描说明与数据处理边界、扫描与生成流程、配额与计费规则、增量扫描功能说明、装饰性图片标记说明、产品边界说明、常见问题 |
| App Store 要求 | 在 Shopify App Store 列表页填写支持邮箱与帮助文档链接 |
| 后续扩展（非 MVP） | 根据用户量增长评估是否引入 App 内实时聊天或工单系统 |

---

**结论：** AltText Magic 的关键不是“能生成”，而是提供一个对商家友好的 **诊断—生成—审阅—写回** 安全闭环。产品聚焦于改善**图片层面的无障碍基础能力**和图片搜索基础，不夸大为整站合规解决方案，也不替代法律、无障碍或合规专业审查。首次使用必须建立在用户知情和主动开始扫描的前提下，清楚说明扫描范围、用途、留存期限与第三方 AI 边界。MVP 通过全量扫描、付费计划专属的自动增量刷新、装饰性图片跳过机制、写回前审阅与审计记录，帮助商家在可控前提下高效补齐缺失 Alt Text，并以清晰的配额与计费规则避免隐形扣费与争议。


---














---

# AltText Magic 系统架构（MVP｜修订版 v1.4）

> 适用对象：研发 / 测试 / 运维  
> 目标：实现 **扫描（Bulk 全量 + 付费计划增量）→ 生成（AI 队列）→ 审阅（可编辑）→ 写回（按图片类型路由）** 的安全闭环，并满足：
> - 支持 **4 类图片资源**：**产品媒体 / 文件库 / 集合封面 / 文章封面**
> - 仪表盘按**图片类型分组**展示：**Total / Has Alt / Missing / Decorative**
> - **首次打开不自动扫描**，必须先展示说明与设置，由用户确认后主动开始扫描
> - **生成、写回过程中禁用扫描**，扫描过程中禁用生成/写回（Shop 级互斥）
> - Webhook 增量扫描**感知互斥锁、计划权限与 scope 权限**，仅付费计划且在 scope 内的资源类型可自动增量刷新
> - 支持**装饰性图片标记**，影响统计、生成排除、写回排除、持久化与取消标记
> - Bulk 解析按 Shopify Bulk 扁平化输出特性设计（`__parentId` 关联），避免丢关联/错关联
> - Bulk 结果 URL 过期或下载失败时，**按 task attempt 级自动重跑 bulk**
> - 全量扫描结果采用**待发布结果 + 原子发布**机制，避免污染“最近一次扫描结果”
> - `PARTIAL_SUCCESS` 下仅对**成功资源类型**做 sweep，失败类型保留上一版已发布结果
> - 增量扫描通过 **debounce / coalesce + 图片指纹** 尽量过滤明显与图片无关的变更
> - `image_usage` 支持**陈旧关联清理**，避免分组统计与影响范围提示失真
> - Candidate 状态机支持**生成失败重试**与**写回失败重试**
> - 审阅列表采用**唯一 candidate + group 展示投影**模型，支持**所属资源名称 / primary usage / 图片位置 / 共享文件影响范围**
> - `scope_flags` 支持**独立设置 API**；变更后立即影响**前台展示、生成/写回入口与自动增量 gate**
> - 当用户重新勾回已发布过的图片类型时，**上次已发布历史数据立即恢复可见**；如需刷新线上最新状态，仍需重新扫描
> - 计费严格对齐产品：**Free / Starter / Growth / Pro / Max 五档 + 月付/年付 + 欢迎额度 + 手动购买超额包**，**不做自动超扣**
> - **Free 计划 25 张月配额**有明确发放周期、去重约束与回归 Free 时的恢复规则
> - 年付配额严格按产品定义：**按年一次性发放全年总量并按年重置**
> - 生成前必须做**Shopify 线上真值复核**，已被外部补齐的图片**不调用 AI、不扣额度**
> - 共享文件生成时采用**上下文分级策略**：单一使用场景可用资源上下文，多使用场景默认使用中性上下文
> - `scope_flags` 同时作为**扫描边界、展示边界与自动增量边界**

---

## 0. 交付物概览（面向研发 / 测试 / 运维）

本方案给出 AltText Magic MVP 的可落地架构，覆盖：

- 核心闭环：**扫描 → 生成 → 审阅 → 写回**
- 图片类型覆盖：**Product Media / Files / Collection Image / Article Image**
- 首次体验：先展示**扫描说明与设置页**，记录 `notice_version` 与 `ack_at`，再允许启动扫描
- 仪表盘分组：按**图片类型**聚合 `total / hasAlt / missing / decorative`
- 扫描去重基线：仍以 **“一条候选 = 一个写回动作”** 为口径  
  唯一键：`(shop_id, alt_plane, write_target_id, locale)`
- 审阅展示采用**group 投影层**：同一 `FILE_ALT` candidate 可在 `Product Media` 与 `Files` 视图下拥有不同展示上下文
- `image_usage` 持久化 `position_index`，支持产品媒体列表展示“图片位置 / 顺序”
- Bulk 解析：**Staging 两阶段解析 + attempt 隔离**
- Bulk 恢复：**task attempt 级重跑 bulk**，旧失败 attempt 不参与 derive
- 全量扫描：**先写待发布结果，不直接改已发布结果**
- 发布机制：**scan_job 完成后原子发布**，扫描进行中前台始终读取上一次已发布结果
- `PARTIAL_SUCCESS`：**仅成功资源类型替换已发布切片**，失败资源类型保持旧数据
- 增量扫描：**仅付费计划**对产品 / 集合启用；并通过**事件合并 + 图片指纹**减少无效刷新
- 装饰性图片：**独立持久化标记 + candidate 状态联动**
- 共享文件上下文：**按 present usage 数量选择 RESOURCE_SPECIFIC / FILE_NEUTRAL / SHARED_NEUTRAL**
- 计费与配额：**订阅 + 手动购买超额包 + 欢迎额度 + Free 月配额 + 月付 included / 年付 annual included**
- Free 额度：**按 UTC 自然月发放 `FREE_MONTHLY_INCLUDED` 25 张，账本可审计**
- 年付额度：**一次性发放 12 倍月额度到年度 bucket**
- 额度安全：生成前**预检并原子预留**，无可用额度绝不调用 AI
- 生成安全：**AI 调用前逐条实时复核 Shopify 当前 Alt**
- scope 闭环：`scope_flags` 同时控制**全量扫描任务创建、仪表盘展示、候选列表、生成/写回入口、自动增量刷新**
- scope 可独立修改：通过设置页 / `POST /api/settings/scope` 立即生效，不强制触发扫描
- 数据留存：扫描结果缓存最近一份、草稿 30 天、审计 90 天、装饰性标记持久化
- Railway 部署拓扑、可观测性、测试要点

---

## 1. 需求抽取与关键约束

### 1.1 MVP 范围（必须做）

1. Shopify OAuth 安装、Session 持久化，使用 Offline token 支持长时 Bulk。
2. Webhooks：
   - `app/uninstalled`
   - GDPR：`CUSTOMERS_DATA_REQUEST` / `CUSTOMERS_REDACT` / `SHOP_REDACT`
   - `BULK_OPERATIONS_FINISH`
   - 增量扫描：`products/create`、`products/update`、`collections/create`、`collections/update`
3. 首次进入 App：
   - 不自动扫描
   - 先展示扫描说明与设置页
   - 由用户确认扫描范围、用途、留存期限、第三方 AI 边界后，主动点击“开始扫描”
   - 记录 `notice_version`、`ack_at`、当次 `scope_flags`
4. `scope_flags` 必须形成完整闭环：
   - 当前配置 scope 可通过**设置页独立修改**，不依赖启动扫描
   - 既是**全量扫描边界**
   - 也是**仪表盘 / 候选列表 / 生成 / 写回**的展示与可操作边界
   - 也是**自动增量刷新边界**
   - 用户取消某类 scope 后，该类历史已发布数据可以保留在库中，但前台必须隐藏，且 webhook 不再自动刷新
   - 用户重新勾回某类 scope 后：
     - 若最近一次已发布结果已覆盖该类，则旧数据立即恢复可见
     - 若最近一次已发布结果未覆盖该类，则仍不可见，直到新的全量扫描成功发布后才进入 `effective_read_scope_flags`
5. 全量扫描（Catalog Scan）使用 GraphQL Bulk Operations 异步导出图片清单，覆盖四类资源：
   - 产品媒体：`Product.media` 中 `MediaImage.alt`
   - 文件库图片：`files(query:"media_type:IMAGE")` 中 `MediaImage.alt`
   - 集合封面：`Collection.image.altText`
   - 文章封面：`Article.image.altText`
   - 不做富文本 HTML 内嵌 `<img>`（V2）
6. 扫描去重：
   - 同一 `MediaImage` 可能被产品媒体扫描和 Files 扫描重复扫到
   - 必须按唯一键收敛为 1 条 target / candidate
   - 引用关系通过 `image_usage` 展开
   - 展示层允许同一 candidate 在不同 group 下形成不同展示投影，但写回对象仍然只有 1 条
7. 保存最近一次**已发布**扫描结果，支持再次进入秒开；支持一键重新扫描。
8. 全量扫描必须采用**待发布结果**机制：
   - 扫描过程中不得污染当前已发布结果
   - scan_job 完成后才允许发布
   - 前台在扫描进行中始终读取上一版已发布结果，并叠加“正在重新扫描”状态
9. 增量扫描：
   - 仅付费计划启用
   - MVP 仅覆盖产品、集合
   - Files、文章仍依赖手动重新扫描
   - 必须尽量过滤与图片无关的变更
   - 必须受当前 `scope_flags` 约束
10. AI 生成：
   - 仅在用户主动发起生成时发送必要图片 URL 与上下文给 AI
   - 图片不落盘、不长期缓存、不存原图
   - 输出英文 Alt Text，**≤ 125 字符**
   - **每个 candidate 在真正调用 AI 前，必须再次从 Shopify 实时读取当前 Alt 真值**
   - 若当前 Alt 已非空，则直接跳过，不调用 AI，不扣额度
11. 仅处理缺失 Alt：
   - `null` / 空串 / 全空白
   - 生成前要做实时真值校验
   - 写回前必须二次读校验，避免覆盖外部刚填的 Alt
12. 审阅与编辑：
   - 列表预览 + 可编辑输入框
   - 全选 / 单选写回
   - 按图片类型筛选
   - 必须展示：
     - 所属资源名称
     - 当前图片类型分组
     - 图片位置 / 顺序（如适用）
     - 共享文件影响范围摘要
13. 装饰性图片标记：
   - 可标记为装饰性并跳过
   - 后续扫描不再计入缺失统计
   - 仪表盘单独展示 decorative 数
   - 持久化保存
   - 可取消标记，重新回到待处理流
14. 写回按图片类型路由：
   - 产品媒体 / Files：`fileUpdate`
   - 集合：`collectionUpdate`
   - 文章：`articleUpdate`
15. 进度反馈：
   - 扫描 / 生成 / 写回均提供进度
   - 失败项可重试
   - 不允许静默失败
16. Candidate 状态机必须支持：
   - 生成失败后可再次生成
   - 写回失败后可再次写回
17. 共享文件的生成上下文必须有明确定义：
   - `usageCountPresent = 1` 时，可使用单一资源上下文
   - `usageCountPresent > 1` 时，必须默认使用**中性上下文**
   - 不得把某个共享文件默认写成某个产品专属描述
18. 计费与配额必须对齐产品意图书：
   - **Free / Starter / Growth / Pro / Max**
   - 支持**月付 / 年付**
   - 安装欢迎额度 50
   - 首次付费欢迎额度按计划发放一次
   - Starter / Growth / Pro / Max 支持**手动购买超额包**
   - **不做自动超扣**
   - 扫描不消耗配额
   - 成功生成即扣额度
   - **年付按年一次性发放全年总量并按年重置**
   - **Free 计划 25 张月配额按月发放并按月重置，需可审计**
19. 数据留存：
   - 最近一次扫描结果缓存保留当前最新一份
   - 草稿 30 天
   - 审计 90 天
   - 装饰性标记持久化
20. 互斥控制：
   - 生成 / 写回过程中禁用扫描
   - 扫描过程中禁用生成 / 写回
21. Webhook 增量与互斥锁交互：
   - shop 有 RUNNING 锁时延迟消费
   - 不触碰全量扫描的待发布结果
   - 增量只原子更新当前已发布结果

### 1.2 关键非功能约束

- 写回成功率目标 >99%
- Shopify API 限流下可稳定运行：Bulk + 队列 + 重试 + 断点续跑
- `fileUpdate` 全局生效，同一媒体资产被多处引用时必须能提示影响范围
- 图片不落盘，仅保存 URL、ID、草稿文本、必要上下文与审计
- 生成前实时真值复核必须成立：
  - 已被外部补齐 Alt 的图片不得再调用 AI
  - 不得发生“已非空仍扣费”
- 共享文件在多使用场景下必须使用中性上下文，避免误写
- 展示模型必须区分：
  - **唯一写回对象**
  - **group 级展示投影**
- 产品媒体必须有稳定的 `position_index` 持久化口径，用于 UI 展示“图片位置”
- 额度一致性：
  - `credit_bucket / credit_reservation / credit_ledger / billing_ledger` 可交叉验证
  - 无可用额度时绝不调用 AI
- Free 配额必须能由账本自证：
  - 周期定义明确
  - 发放不可重复
  - 回归 Free 时行为可追溯
- 年付额度必须能由账本自证：年度发放、年度消费、年度到期
- Bulk 解析必须显式处理扁平化输出和 `__parentId`
- Bulk 结果 URL 过期必须可恢复，不得整 job 直接失败
- Bulk 结果解析必须流式下载、流式解析，避免大文件内存溢出
- 再次访问必须稳定读取**最近一次已发布结果**
- `PARTIAL_SUCCESS` 不能误删失败资源类型的历史有效数据
- `scope_flags` 改动后，前台与自动增量必须立即按新 scope 生效
- 增量刷新必须尽量避免无效重扫
- `image_usage` 必须支持 sweep，避免历史脏数据污染分组统计
- 装饰性标记必须在扫描、统计、生成、写回全链路生效
- 周期性额度桶必须具备**防重复发放约束**

---

## 2. 关键假设（信息不足时的合理默认）

1. 技术栈推荐：
   - Node.js + TypeScript
   - Web：Remix 或 Next.js
   - ORM：Prisma
   - DB：PostgreSQL
   - Queue：Redis + BullMQ
2. App 形态：Shopify Admin Embedded App（App Bridge + Polaris）。
3. AI 调用：统一 AI Gateway，支持主模型与降级模型切换。
4. Bulk 完成通知：优先 `BULK_OPERATIONS_FINISH` webhook，轮询作为降级方案。
5. `fileUpdate` 写回目标使用 `MediaImage` 全局 ID。
6. Shopify 计费能力使用：
   - 订阅能力承载月付 / 年付计划
   - 一次性购买能力承载超额包
   - 具体 mutation 名称随 Shopify 当前稳定版校对
7. 年付配额严格按产品实现：
   - 年付计划激活或续费时，一次性发放 `12 × 月配额` 到 `ANNUAL_INCLUDED` bucket
   - 年度 included bucket 在年周期结束时到期并重置
   - 前端额度摘要按 `billing_interval` 展示“月度 included 余额”或“年度 included 余额”
8. Free 计划配额采用 **UTC 自然月** 作为周期锚点：
   - 每个 UTC 月最多发放 1 个 `FREE_MONTHLY_INCLUDED` bucket
   - `cycle_key` 形如 `FREE:2026-03`
   - 同一店铺同一自然月不得重复发放
9. 周期切换时，可能同时存在多个 active included bucket（例如月中升级后的旧 Free bucket 与新付费 bucket）：
   - 这不影响产品规则
   - included 家族内部按**最早到期优先**消费
   - 对前端统一汇总为 `includedRemaining`
10. 产品媒体位置口径：
   - 若 Shopify 当前稳定版可直接返回媒体 position 字段，则优先直接持久化该字段
   - 若稳定版未提供可用 position 字段，则解析器按**同一 product 下 media 的返回顺序**生成 `position_index`
   - `position_index` 仅用于展示与排序，不作为业务唯一键

---

## 3. 高层架构（可部署形态）

### 3.1 组件（逻辑视图）

**Web / API 服务（App Server）**

- Shopify OAuth 与 Session
- 首次说明页、Dashboard、Review、History、Billing、Settings
- REST API
- SSE 事件流
- Webhook Receiver
- 操作锁校验
- 计划权限校验
- scope 权限校验
- 扫描说明确认记录
- 当前 scope 独立设置
- 已发布扫描结果读取
- 候选展示投影读取
- 共享文件上下文模式展示

**Worker 服务（Background Worker）**

- 全量扫描任务：
  - 触发 bulk
  - 流式下载 / 流式解析 NDJSON
  - staging 落库
  - derive 到 `scan_result_target / scan_result_usage`
  - 发布时再合并到 `alt_target / image_usage / alt_candidate / candidate_group_projection`
- 发布任务：
  - 在 scan_job 结束后执行原子发布
  - 仅成功资源类型替换已发布切片
  - 发布后更新 `last_published_scope_flags`
  - 重建受影响 group 展示投影
- 增量扫描任务：
  - 消费 `webhook_event`
  - debounce / coalesce
  - 校验 shop 当前计划是否允许自动增量
  - 校验资源类型是否在当前 `scope_flags` 内
  - 读取单资源
  - 计算图片指纹
  - 指纹未变则跳过
  - 指纹变化时原子 patch 已发布结果并做 scoped sweep
  - 重建受影响 group 展示投影
- 生成任务：
  - 额度预留
  - Shopify 当前 Alt 真值复核
  - 共享文件上下文构建
  - AI 调用
  - 草稿落库
  - 成功后正式扣减额度
- 写回任务：
  - 二次读校验
  - mutation 写回
  - 审计落库
- 计费与权益任务：
  - 订阅同步
  - Free 月配额发放
  - 欢迎额度发放
  - 月度 included 发放
  - 年度 included 发放
  - 超额包购买确认
- 清理任务：
  - 草稿 / 审计 / staging / scan_result / 失败 attempt / webhook_event 清理
  - 释放过期 reservation
  - 锁超时回收
  - GDPR 删除

**PostgreSQL**

- 店铺、会话、扫描说明确认
- 扫描作业 / 任务 / attempt
- staging
- 待发布扫描结果
- 已发布 Alt 目标清单
- 已发布引用关系
- 候选队列
- 候选 group 展示投影
- 装饰性标记
- 草稿与审计
- 资源图片指纹
- 订阅、超额包、额度桶、预留、账本
- 操作锁
- webhook_event

**Redis**

- BullMQ 队列
- 进度缓存 / PubSub
- 增量刷新 debounce / coalesce key

**外部依赖**

- Shopify Admin GraphQL API
- AI Provider
- Shopify Billing

### 3.2 Railway 部署拓扑（建议）

- `web`：Remix / Next
- `worker`：BullMQ Worker
- `postgres`
- `redis`
- `cron` 可由 worker 内分布式锁实现

---

## 4. 核心流程设计（端到端）

### 4.1 安装与鉴权

1. 商家安装 App，完成 OAuth。
2. 存储：
   - `shop_domain`
   - `offline access token`（加密）
   - 安装时间
3. 初始化店铺默认数据：
   - `plan = FREE`
   - `scan_scope_flags = { PRODUCT_MEDIA, FILES, COLLECTION_IMAGE, ARTICLE_IMAGE }`
   - `last_published_scope_flags = {}`
   - 创建安装欢迎额度 bucket：50 张
   - 创建当月 Free 月配额 bucket：25 张  
     - `bucket_type = FREE_MONTHLY_INCLUDED`
     - `source_type = FREE_MONTHLY_GRANT`
     - `cycle_key = FREE:YYYY-MM`
4. 注册 Webhooks：
   - `APP_UNINSTALLED`
   - GDPR 三类
   - `BULK_OPERATIONS_FINISH`
   - `products/create`
   - `products/update`
   - `collections/create`
   - `collections/update`

> Webhook 可以全量注册，但是否真正执行增量刷新，由 worker 在消费时按当前计划、当前 scope、debounce 与图片指纹做 gate。

---

### 4.2 首次打开、扫描说明确认、scope 闭环与 Shop 级互斥

#### 4.2.1 首次打开与说明确认

首次打开 App 时：

1. `GET /api/bootstrap`
2. 若店铺不存在当前版本的扫描说明确认记录，则前端进入“扫描说明与设置页”
3. 页面必须明确展示：
   - 扫描范围
   - 扫描用途
   - 扫描结果 / 草稿 / 审计记录留存期限
   - 扫描阶段不会发送图片给第三方 AI
   - 仅在用户主动点击“生成 Alt Text”时，才会发送必要图片 URL 与上下文
4. 默认勾选四类图片类型，允许用户修改
5. 用户点击“开始扫描”时：
   - 记录 `notice_version`
   - 记录 `ack_at`
   - 记录 `scope_flags_snapshot`
   - 更新 shop 当前配置 `scan_scope_flags`
   - 创建 scan_job

> 若未来说明文案有重大调整，可提升 `notice_version`，要求用户在下次发起扫描前重新确认。  
> 重新确认不影响既有仪表盘浏览，只影响“开始扫描”动作。

#### 4.2.2 scope_flags 的完整闭环

为避免“扫描范围、展示范围、自动刷新范围”不一致，定义三类 scope：

**A. 当前配置 scope：`shops.scan_scope_flags`**

- 表示用户当前希望处理的资源类型
- 可由：
  - 首次说明页的“开始扫描”提交更新
  - 设置页独立保存更新
- 控制：
  - 新建全量扫描任务时哪些 `scan_task` 会被创建
  - Webhook 自动增量时哪些资源类型允许刷新
  - 前台哪些资源类型可以进入生成 / 写回
  - 前台哪些分组可见

**B. 已发布结果 scope：`shops.last_published_scope_flags`**

- 表示当前“最近一次已发布结果”实际覆盖过哪些资源类型
- 仅在 publish 成功后更新为 `scan_job.scope_flags`

**C. 前台有效读取 scope：`effective_read_scope_flags`**

- 计算方式：`intersection(shops.scan_scope_flags, shops.last_published_scope_flags)`
- 控制：
  - 仪表盘显示哪些分组
  - 候选列表显示哪些资源类型
  - 生成 / 写回 API 是否接受该 candidate

由此保证：

1. 用户取消某类 scope 后，该类前台数据立即隐藏。
2. 该类 webhook 自动增量立即停止，标记为 `SKIPPED_SCOPE`。
3. 用户重新勾回某类 scope 后：
   - 若 `last_published_scope_flags` 已包含该类，则上次已发布历史数据立即恢复可见
   - 若 `last_published_scope_flags` 尚未包含该类，则仍不会显示，直到后续成功发布覆盖该类

> out-of-scope 的历史已发布数据可以继续保留在库中，便于排障与后续重新纳入 scope 时复用底层记录；但 MVP 前台只展示 `effective_read_scope_flags` 内的数据。  
> 重新勾回后恢复显示的是**上次已发布历史数据**，不是新的线上真值刷新结果；如需刷新，仍需重新扫描。

#### 4.2.3 独立 scope 设置 API

新增：

`POST /api/settings/scope`

行为：

1. 校验 session 与 shop 权限
2. 仅更新 `shops.scan_scope_flags`
3. 不创建 `scan_job`
4. 不修改 `shops.last_published_scope_flags`
5. 返回：
   - `scanScopeFlags`
   - `lastPublishedScopeFlags`
   - `effectiveReadScopeFlags`

立即生效范围：

- 前台分组展示
- 候选列表过滤
- 生成 / 写回入口 gate
- webhook 自动增量 gate

补充约束：

- 正在运行的 `scan_job` 使用其创建时的 `scope_flags snapshot`，不因设置页后改而变更任务内容
- 正在运行的 `generation / writeback batch` 不被强制中断；scope 新值仅影响后续新请求与前台读取

#### 4.2.4 互斥规则

同一 shop 任一时刻最多允许 1 个重操作运行：

- `SCAN`
- `GENERATE`
- `WRITEBACK`

规则如下：

- 有 RUNNING 的 `GENERATE / WRITEBACK` 时：
  - `POST /api/scan/start` 返回 `409`
- 有 RUNNING 的 `SCAN` 时：
  - `POST /api/generation/start`
  - `POST /api/writeback/start`
  - 返回 `409`
- Webhook 增量扫描也受锁约束：
  - 有锁则延迟消费
  - 不改动已发布结果
  - 不触碰全量扫描待发布结果

#### 4.2.5 锁实现建议

使用表 `shop_operation_lock`，`unique(shop_id)`。

字段包括：

- `lock_type`：`SCAN / GENERATE / WRITEBACK`
- `batch_id`
- `acquired_at`
- `heartbeat_at`
- `expires_at`
- `status`

获取锁方式：

- 事务内 `SELECT ... FOR UPDATE`
- 或基于唯一键插入

释放锁方式：

- batch 完成后释放
- cleanup job 回收超时锁，例如 30 分钟无 heartbeat

---

### 4.3 扫描（Catalog Scan + Continuous Scan）

#### 4.3.1 扫描归一化口径

扫描产出分三层：

1. **Alt 目标清单（Inventory）**：全部图片目标，用于 `total / hasAlt / missing / decorative`
2. **Alt 候选队列（Queue）**：唯一写回对象，统一 candidate 生命周期
3. **候选展示投影（Group Projection）**：用于按 group 展示所属资源、位置与影响范围

统一字段：

- `shop_id`
- `alt_plane`
  - `FILE_ALT`
  - `COLLECTION_IMAGE_ALT`
  - `ARTICLE_IMAGE_ALT`
- `write_target_id`
- `locale = default`
- `current_alt_text`
- `current_alt_empty`
- `preview_url`
- `display metadata`
- `usage references`

候选唯一键：

`(shop_id, alt_plane, write_target_id, locale)`

> 同一 `MediaImage` 被多次扫描到时，只保留 1 条 target / candidate；引用关系累积在 `image_usage`。  
> 展示层不改变写回唯一性，而是通过 `candidate_group_projection` 让同一 candidate 在不同 group 下拥有各自展示语义。  
> 对 `FILE_ALT` 而言，`alt_target.display_title / display_handle` 仅作为 target 级 fallback 与排障字段，不作为前台 group 列表的权威标题来源。

#### 4.3.2 已发布结果与待发布结果

为满足“再次访问优先展示最近一次扫描结果”，全量扫描采用**待发布结果 + 原子发布**。

**已发布结果**

- `alt_target / image_usage / alt_candidate / candidate_group_projection` 只保存**当前已发布结果**
- Dashboard、候选列表、审计前台读取都只依赖已发布结果
- 扫描进行中，这些表保持不变
- 前台读取时还要额外经过 `effective_read_scope_flags` 过滤

**待发布结果**

- 每次全量扫描在 `scan_result_target / scan_result_usage` 中构建待发布结果
- derive 成功后仍不直接改 `alt_target / image_usage / alt_candidate / candidate_group_projection`
- scan_job 完成后，由 publish worker 统一合并

**前台读取原则**

- 扫描 RUNNING 时：
  - 前台继续读取上一次已发布结果
  - 额外显示“正在重新扫描”
- scan_job 完成且 publish 成功后：
  - 原子切换到新结果
  - 更新 `shops.last_published_scope_flags = scan_job.scope_flags`
- scan_job 失败且没有成功资源类型：
  - 不发布
  - 继续保留旧结果
  - 但前台仍按当前 `shops.scan_scope_flags` 做 scope 过滤

#### 4.3.3 Catalog Scan 触发规则

**不会自动触发首次扫描。**

触发时机仅有：

- 用户首次确认说明后主动点击“开始扫描”
- 用户点击“重新扫描”
- 未来低频运营任务，如产品明确新增时再引入

扫描范围以用户当前 `scan_scope_flags` 为准，默认全选四类。

#### 4.3.4 Catalog Scan 任务拆分

MVP 拆分为 4 个 bulk task：

- Task A：产品媒体
- Task B：Files 图片
- Task C：集合封面
- Task D：文章封面

每个 `scan_task` 代表一个逻辑资源类型；每次重跑 bulk 创建新的 `scan_task_attempt`。

只为当前 `scan_scope_flags` 中选中的资源类型创建 task。

#### 4.3.5 Bulk 查询设计与前置验证

必须在真实测试店铺跑通 4 类 query，并固化脱敏 NDJSON 样本。

建议查询示例：

产品媒体：
```graphql
{
  products {
    edges {
      node {
        __typename
        id
        title
        handle
        media(first: 250) {
          edges {
            node {
              __typename
              ... on MediaImage {
                id
                alt
                image { url width height }
              }
            }
          }
        }
      }
    }
  }
}
```

Files 图片：
```graphql
{
  files(first: 250, query: "media_type:IMAGE") {
    edges {
      node {
        __typename
        ... on MediaImage {
          id
          alt
          image { url width height }
        }
      }
    }
  }
}
```

集合封面：
```graphql
{
  collections(first: 250) {
    edges {
      node {
        __typename
        id
        title
        handle
        image { url altText }
      }
    }
  }
}
```

文章封面：
```graphql
{
  articles(first: 250) {
    edges {
      node {
        __typename
        id
        title
        handle
        image { url altText }
      }
    }
  }
}
```

补充要求：

- 对产品媒体，若当前稳定版已提供可直接使用的 media position 字段，则将其加入 query 并持久化
- 若稳定版未提供该字段，则解析器按同一 product 下 media 的**出现顺序**生成 `position_index`
- 必须用真实 bulk 样本验证：
  - `__parentId` 关联
  - 共享 `MediaImage` 去重
  - `position_index` 推导稳定性

#### 4.3.6 Bulk 解析：Staging 两阶段 + Attempt 隔离

为避免“旧 staging 污染新结果”，解析采用：

- `scan_task`：逻辑任务
- `scan_task_attempt`：每次 bulk 提交 / 下载 / 解析尝试
- staging 表按 `scan_task_attempt_id` 隔离
- **只有最新成功 attempt 的 staging 才能参与 derive**
- derive 只写入**待发布结果表**，不直接写已发布表

##### Attempt 流程

1. 创建 `scan_task`
2. 创建 `scan_task_attempt #1`
3. 提交 `bulkOperationRunQuery`
4. `BULK_OPERATIONS_FINISH` 到达后，定位到具体 attempt
5. worker 进入下载与解析
6. 若 URL 过期 / 下载失败 / 网络超时：
   - 当前 attempt 标记失败
   - `parse_attempts += 1`
   - 若未超过上限，则创建新的 `scan_task_attempt`
   - 重新提交 bulk
   - 旧 attempt staging 保留但**永不参与 derive**
7. 若超过 `max_parse_attempts`：
   - `scan_task = FAILED`
   - `scan_job` 进入 `PARTIAL_SUCCESS` 或 `FAILED`

##### 流式下载与解析要求

必须采用 stream pipeline：

`fetch -> response.body -> ndjson parser -> batch upsert staging`

禁止整文件一次性读入内存。

##### Staging 表建议

- `stg_product`
- `stg_media_image_product`
- `stg_media_image_file`
- `stg_collection`
- `stg_article`

全部带 `scan_task_attempt_id`。

其中：

- `stg_media_image_product` 必须带 `position_index`
- `position_index` 优先使用 Shopify 明确字段；否则按同一 product 下的解析顺序赋值

##### Derive 到待发布结果的规则

当某个 attempt 成功后，执行 derive：

1. 写入 `scan_result_target`
2. 写入 `scan_result_usage`
3. 不直接更新 `alt_target / image_usage / alt_candidate / candidate_group_projection`

资源类型映射：

- `PRODUCT_MEDIA`
  - 写 `scan_result_target(FILE_ALT)`
  - 写 `scan_result_usage(usage_type = PRODUCT)`
  - 继承 `position_index`
- `FILES`
  - 写 `scan_result_target(FILE_ALT)`
  - 写 `scan_result_usage(usage_type = FILE)`
- `COLLECTION_IMAGE`
  - 写 `scan_result_target(COLLECTION_IMAGE_ALT)`
- `ARTICLE_IMAGE`
  - 写 `scan_result_target(ARTICLE_IMAGE_ALT)`

> 同一 `FILE_ALT` target 可能同时来自 `PRODUCT_MEDIA` 与 `FILES` 两个成功 task。  
> 对同一 `write_target_id` 的字段冲突应做一致性校验；若 `alt / url` 不一致，记录告警并按“非空优先、最后写入胜出”合并，但该情况应被视为异常监控。

#### 4.3.7 Full Scan Finalize 与原子发布

当本轮所有纳入 scope 的 task 都结束后，执行 finalize。

##### scan_job 状态判定

- 全部成功：`SUCCESS`
- 至少一个成功、至少一个失败：`PARTIAL_SUCCESS`
- 全部失败：`FAILED`

##### 发布原则

- **只有至少一个资源类型成功时才允许 publish**
- publish 在单个数据库事务中完成
- publish 只替换**成功资源类型对应的已发布切片**
- 失败资源类型保留上一版已发布结果，不做缺席推断
- 成功 publish 后更新：
  - `shops.last_published_scan_job_id`
  - `shops.last_published_at`
  - `shops.last_published_scope_flags = scan_job.scope_flags`

##### 切片发布规则

**A. PRODUCT_MEDIA 成功时**

- upsert 本次 `PRODUCT_MEDIA` 结果中的 `FILE_ALT` target
- upsert 对应 `image_usage(usage_type = PRODUCT)`，并更新 `position_index`
- 对已发布表中属于 `usage_type = PRODUCT`、但本次结果未出现的 usage 做 sweep：
  - `present_status = NOT_FOUND`
- 重新计算本次受影响的 `FILE_ALT` targets 的 `present_status`

**B. FILES 成功时**

- upsert 本次 `FILES` 结果中的 `FILE_ALT` target
- upsert 对应 `image_usage(usage_type = FILE)`
- 对已发布表中属于 `usage_type = FILE`、但本次结果未出现的 usage 做 sweep：
  - `present_status = NOT_FOUND`
- 重新计算本次受影响的 `FILE_ALT` targets 的 `present_status`

**C. COLLECTION_IMAGE 成功时**

- upsert 本次集合图片 target
- 仅对 `COLLECTION_IMAGE_ALT` 切片执行 sweep：
  - 本轮 scope 内、该 task 成功、但本次结果未再次出现的 target：
    - `alt_target.present_status = NOT_FOUND`
    - `alt_candidate.status = NOT_FOUND`

**D. ARTICLE_IMAGE 成功时**

- upsert 本次文章图片 target
- 仅对 `ARTICLE_IMAGE_ALT` 切片执行 sweep：
  - 本轮 scope 内、该 task 成功、但本次结果未再次出现的 target：
    - `alt_target.present_status = NOT_FOUND`
    - `alt_candidate.status = NOT_FOUND`

##### FILE_ALT target 的存在判定

`FILE_ALT` 不能按单个 task 的“缺席”直接判定 `NOT_FOUND`。  
其 `present_status` 采用以下规则重算：

- 只要存在任一 `PRESENT` 的 `image_usage`：
  - `usage_type = PRODUCT` 或 `usage_type = FILE`
  - 则 `alt_target.present_status = PRESENT`
- 两类 usage 都不存在 `PRESENT` 时：
  - `alt_target.present_status = NOT_FOUND`

这保证：

- `PRODUCT_MEDIA` 成功、`FILES` 失败时，不会把 Files 侧旧数据误判删除
- `FILES` 成功、`PRODUCT_MEDIA` 失败时，也不会误删产品侧历史有效关系

##### Candidate 发布收敛规则

publish 最后一步，对本次受影响的 targets 重算 `alt_candidate`：

- target 不存在：`NOT_FOUND`
- target 存在且线上 Alt 非空：`RESOLVED`
- target 存在且线上 Alt 为空，且有激活的 decorative mark：`DECORATIVE_SKIPPED`
- target 存在且线上 Alt 为空，且无 decorative mark：
  - 若存在未过期 draft：
    - 若当前 candidate 为 `WRITEBACK_FAILED_RETRYABLE`，保持该状态
    - 否则为 `GENERATED`
  - 若不存在未过期 draft：
    - 若当前 candidate 为 `GENERATION_FAILED_RETRYABLE`，保持该状态
    - 否则为 `MISSING`

##### Group 展示投影重建规则

publish 同一事务内，对本次受影响 targets 重建 `candidate_group_projection`：

**A. `FILE_ALT` target**

- 若存在至少 1 条 `PRESENT` 的 `PRODUCT` usage：
  - upsert 1 条 `group_type = PRODUCT_MEDIA` 投影
  - `primary_usage` 取排序最靠前的 `PRODUCT` usage：
    - `position_index ASC NULLS LAST`
    - `usage_id ASC`
  - `primary_position_index = primary_usage.position_index`
  - `additional_usage_count = present_product_usage_count - 1`
  - `usage_count_present = present_product_usage_count + present_file_usage_count`
  - `impact_scope_summary` 至少包含：
    - `productUsageCountPresent`
    - `fileUsageCountPresent`
- 若不存在 `PRESENT PRODUCT usage`：
  - 删除 `group_type = PRODUCT_MEDIA` 的投影

- 若存在至少 1 条 `PRESENT` 的 `FILE` usage：
  - upsert 1 条 `group_type = FILES` 投影
  - `primary_usage = FILE`
  - `primary_position_index = null`
  - `additional_usage_count = present_file_usage_count - 1`
  - `usage_count_present = present_product_usage_count + present_file_usage_count`
  - `impact_scope_summary` 同上
- 若不存在 `PRESENT FILE usage`：
  - 删除 `group_type = FILES` 的投影

**B. `COLLECTION_IMAGE_ALT` target**

- 若 target `present_status = PRESENT`：
  - upsert 1 条 `group_type = COLLECTION`
  - `primary_usage_type = SELF`
  - `primary_usage_id = write_target_id`
  - `primary_title / primary_handle` 取集合自身
  - `primary_position_index = null`
  - `additional_usage_count = 0`
  - `usage_count_present = 1`
- 若 target `NOT_FOUND`：
  - 删除该投影

**C. `ARTICLE_IMAGE_ALT` target**

- 若 target `present_status = PRESENT`：
  - upsert 1 条 `group_type = ARTICLE`
  - `primary_usage_type = SELF`
  - `primary_usage_id = write_target_id`
  - `primary_title / primary_handle` 取文章自身
  - `primary_position_index = null`
  - `additional_usage_count = 0`
  - `usage_count_present = 1`
- 若 target `NOT_FOUND`：
  - 删除该投影

##### 发布完成

- 更新 `shops.last_published_scan_job_id`
- 更新 `shops.last_published_at`
- 更新 `shops.last_published_scope_flags = scan_job.scope_flags`
- 标记 `scan_job.publish_status = PUBLISHED`

> sweep 只针对**本次 scope 覆盖且对应 task 成功**的资源类型执行，不影响未扫描或扫描失败的类型。  
> out-of-scope 资源类型不会被误扫成 `NOT_FOUND`，而是单纯被排除出 `effective_read_scope_flags`。

#### 4.3.8 `image_usage` 维护与陈旧关联清理

`image_usage` 不仅 upsert，还必须 sweep。

字段包括：

- `last_seen_at`
- `last_seen_scan_job_id`
- `present_status`
- `position_index`

处理规则：

**全量扫描发布**

- `PRODUCT_MEDIA` 成功：
  - upsert 本轮产品 usage 为 `PRESENT`
  - 更新 `position_index`
  - 对 shop 下所有 `usage_type = PRODUCT` 且本轮未出现的 usage 标记 `NOT_FOUND`
- `FILES` 成功：
  - upsert 本轮 file usage 为 `PRESENT`
  - 对 shop 下所有 `usage_type = FILE` 且本轮未出现的 usage 标记 `NOT_FOUND`
- 失败资源类型不 sweep

**产品增量扫描**

- 读取该 product 当前所有 `MediaImage`
- 对返回的 usage upsert 为 `PRESENT`
- 按当前返回顺序或明确 position 字段更新 `position_index`
- 对该 `product_id` 对应、但本次未返回的 usage 标记为 `NOT_FOUND`

**Files 无增量**

- `usage_type = FILE` 仅在全量扫描中刷新与 sweep

仪表盘分组与共享文件影响范围提示，只统计 `present_status = PRESENT` 的 usage。

#### 4.3.9 Continuous Scan（增量扫描，Webhook 驱动）

MVP 仅支持：

- 产品
- 集合

##### Webhook 接收与暂存

Webhook Receiver 只做三件事：

1. HMAC 校验
2. 以 `X-Shopify-Webhook-Id` 幂等写入 `webhook_event`
3. 快速返回 200，并投递队列

##### Debounce / Coalesce

为了尽量过滤明显与图片无关的更新，worker 在真正读取资源前先做合并：

- 合并键：`(shop_id, topic, resource_id)`
- 建议窗口：30~90 秒
- 窗口内重复事件：
  - 保留 1 个待处理刷新任务
  - 其余事件标记 `COALESCED`

这一步只减少重复读取，不直接判定是否有图片变化。

##### Worker 消费 gate

worker 处理前必须同时通过四个 gate：

1. **互斥锁 gate**
   - shop 有 RUNNING 锁则延迟重试
2. **计划权限 gate**
   - 仅 `Starter / Growth / Pro / Max` 可执行自动增量刷新
   - `Free` 计划直接标记 `SKIPPED_PLAN`
   - 不更新已发布结果
3. **scope gate**
   - 若该资源类型不在 `shops.scan_scope_flags` 内：
     - 标记 `SKIPPED_SCOPE`
     - 不更新已发布结果
4. **图片指纹 gate**
   - 读取资源当前图片相关字段
   - 计算图片指纹
   - 与上次已处理指纹相同则标记 `SKIPPED_NO_IMAGE_CHANGE`

##### 图片指纹规则

**Product 指纹**

对单个 product 读取当前 media 后，按稳定顺序拼接：

- `media_image_id`
- `alt`
- 主图 URL 或 image URL

然后做 `sha256`。

**Collection 指纹**

对单个 collection 读取：

- `image.url`
- `image.altText`

再做 `sha256`。无图时使用稳定常量 `NO_IMAGE`。

> 指纹只覆盖图片相关字段，不看标题、价格、库存、SEO 文案等无关字段。

##### 增量逻辑

**products/create / update**

1. 读取该 product 当前 media
2. 计算 product 图片指纹
3. 若指纹未变：
   - `webhook_event = SKIPPED_NO_IMAGE_CHANGE`
   - 不更新 `alt_target / image_usage / alt_candidate / candidate_group_projection`
4. 若指纹变化：
   - 原子 upsert 相关 `FILE_ALT` targets
   - 原子 upsert 相关 `image_usage(PRODUCT)`，并更新 `position_index`
   - 对该 product 范围做 usage sweep
   - 重算受影响 `FILE_ALT` target 的 `present_status`
   - 重算受影响 candidate
   - 重建受影响 `candidate_group_projection`
   - 更新 `resource_image_fingerprint`

**collections/create / update**

1. 读取该 collection 当前图片
2. 计算 collection 图片指纹
3. 若指纹未变：
   - `webhook_event = SKIPPED_NO_IMAGE_CHANGE`
4. 若指纹变化：
   - 原子 upsert / sweep `COLLECTION_IMAGE_ALT`
   - 重算 candidate
   - 重建 `group_type = COLLECTION` 投影
   - 更新 `resource_image_fingerprint`

**Files / Articles**

- MVP 不做自动增量刷新

#### 4.3.10 生成上下文构建策略

为避免共享文件被误写成某个产品专属描述，生成上下文采用明确分级策略。

##### A. Collection / Article

上下文直接来自当前资源本身：

- `resource_type`
- `title`
- `handle`
- 当前图片 URL
- 约束提示：简洁、具体、英文、≤125 字符

##### B. FILE_ALT 的上下文模式

对 `FILE_ALT`，基于 `image_usage where present_status = PRESENT` 计算：

- `usageCountPresent`
- `usageTypesPresent`

上下文模式定义如下：

1. **`RESOURCE_SPECIFIC`**
   - 条件：恰好存在 1 个 `PRODUCT` usage，且无其他 present usage
   - 可用上下文：
     - 产品标题
     - 产品 handle
     - 当前图片 URL
   - 用途：单一产品专属图

2. **`FILE_NEUTRAL`**
   - 条件：仅存在 1 个 `FILE` usage，且无 `PRODUCT` usage
   - 可用上下文：
     - 当前图片 URL
     - 文件库通用说明
   - 不使用任何产品专属文案

3. **`SHARED_NEUTRAL`**
   - 条件：
     - `usageCountPresent > 1`
     - 或同时存在 `PRODUCT` 与 `FILE` usage
   - 可用上下文：
     - 当前图片 URL
     - 共享使用说明，例如“shared store image reused in multiple contexts”
     - `usageCountPresent`
   - 明确禁止把某个产品标题、活动标题或单一 usage 标题作为主上下文

##### C. Prompt 约束

当 `context_mode = SHARED_NEUTRAL` 时，Prompt 必须显式增加约束：

- 生成**通用、跨场景仍成立**的描述
- 不提及某个具体产品名、系列名、促销名，除非图片本身可直接读出
- 不根据某个单一 usage 臆测用途

##### D. 审阅界面展示

候选列表与草稿审阅页应展示：

- `usageCountPresent`
- `contextMode`
- 若为 `SHARED_NEUTRAL`，显示“Shared asset / Neutral context”标识

##### E. 草稿审计

生成时将 `context_mode` 与 `context_snapshot` 写入草稿记录，便于排障与计费争议追溯。

#### 4.3.11 审阅列表展示投影

为解决“唯一 candidate 写回对象”与“按 group 审阅展示”之间的语义差异，引入 `candidate_group_projection`。

##### 设计原则

- `alt_candidate` 仍是唯一写回对象
- `candidate_group_projection` 只承担前台展示，不参与写回唯一性判定
- 同一 candidate 可在多个 group 下各有一条展示投影
- 前台列表永远以 `candidate_group_projection + alt_candidate + alt_target + alt_draft` 联查为准，不直接使用 `alt_target.display_title` 作为 `FILE_ALT` 的主标题

##### 投影生成规则

- `FILE_ALT` + 有 `PRESENT PRODUCT usage`：
  - 生成 `PRODUCT_MEDIA` 投影
- `FILE_ALT` + 有 `PRESENT FILE usage`：
  - 生成 `FILES` 投影
- `COLLECTION_IMAGE_ALT`：
  - 生成 `COLLECTION` 投影
- `ARTICLE_IMAGE_ALT`：
  - 生成 `ARTICLE` 投影

##### 投影内容

每条投影至少包含：

- `group_type`
- `primary_usage`
  - 标题
  - handle
  - usage_type
  - usage_id
- `primary_position_index`
- `additional_usage_count`
- `usage_count_present`
- `impact_scope_summary`

其中：

- `primary_usage` 用于列表主标题
- `additional_usage_count` 用于显示“还有 N 个关联场景”
- `impact_scope_summary` 用于写回前提示共享文件全局影响范围
- 需要查看详细 usage 列表时，再通过详情 API 读取 `image_usage`

##### 选择同一 candidate 时的约束

- 无论从哪个 group 视图选中，同一个写回对象始终对应同一个 `alt_candidate_id`
- 草稿、写回状态、计费都只认 `alt_candidate_id`
- 不允许因为展示投影不同而生成多份草稿或多次写回

---

### 4.4 仪表盘分组（按图片类型）

前台仪表盘只读取**已发布结果**，且只显示 `effective_read_scope_flags` 内的资源类型。

MVP 推荐统一基于 `candidate_group_projection` 统计，以保证：

- Dashboard 分组计数
- 候选列表
- 共享文件影响范围提示

三者口径一致。

分组维度：

- `group_type = PRODUCT_MEDIA`
- `group_type = FILES`
- `group_type = COLLECTION`
- `group_type = ARTICLE`

每组统计：

- `total`：该 group 下 projection 数
- `hasAlt`：对应 target `current_alt_empty = false`
- `missing`：对应 target `current_alt_empty = true` 且无激活 decorative mark
- `decorative`：对应 target `current_alt_empty = true` 且有激活 decorative mark

前台还应展示：

- `lastPublishedAt`
- 当前是否存在 RUNNING 的 `scan_job`
- 当前 `scan_scope_flags`
- 当前 `effective_read_scope_flags`
- 若有进行中的重新扫描，展示“正在重新扫描，当前显示的是上一次已完成结果”

> 同一共享文件若同时出现在 Product Media 与 Files 视角，可同时计入两组；但 candidate 仍只有 1 条，生成与写回只发生一次。  
> out-of-scope 分组不展示，也不允许从前台进入操作流。

---

### 4.5 装饰性图片标记

装饰性图片是 P0 功能，使用**独立表 + candidate 状态联动**实现。

#### 4.5.1 标记动作

用户点击“标记为装饰性”时：

1. 创建或激活 `decorative_mark`
2. 若 target 当前 Alt 为空：
   - candidate 置为 `DECORATIVE_SKIPPED`
3. 该图片从生成与写回列表排除
4. 仪表盘从 `missing` 转入 `decorative`

#### 4.5.2 取消标记

用户点击“取消装饰性标记”时：

1. `decorative_mark.is_active = false`
2. 若 target 当前 Alt 仍为空：
   - 有有效 draft：candidate 回到 `GENERATED`
   - 无 draft：candidate 回到 `MISSING`

#### 4.5.3 与扫描联动

每次全量发布 / 增量刷新完成后，candidate 收敛时都要读取 `decorative_mark`：

- 有 mark 且当前 Alt 为空：`DECORATIVE_SKIPPED`
- 无 mark 且当前 Alt 为空：`MISSING / GENERATION_FAILED_RETRYABLE / GENERATED / WRITEBACK_FAILED_RETRYABLE`
- 当前 Alt 已非空：`RESOLVED`

#### 4.5.4 与生成 / 写回联动

- `DECORATIVE_SKIPPED` 不允许进入生成
- `DECORATIVE_SKIPPED` 不允许进入写回
- 若图片已生成草稿后再被标记装饰性：
  - draft 仍保留到 30 天
  - 写回列表中隐藏
  - 取消标记后可恢复

---

### 4.6 AI 生成（队列化、可降级）

#### 4.6.1 触发

用户在缺失列表页选择 candidate，点击“生成草稿”。

#### 4.6.2 前置校验

- 当前 shop 无 `SCAN / WRITEBACK` 锁
- candidate 属于当前 shop
- candidate 资源类型在 `effective_read_scope_flags` 内
- candidate 状态为：
  - `MISSING`
  - 或 `GENERATION_FAILED_RETRYABLE`
- 对应 target 当前仍为空 Alt
- 非 decorative

#### 4.6.3 额度预检与预留

生成前执行 batch 级预检：

- `expected_cost_upper_bound = 选中图片数`
- 可用额度计算：
  - 当前 active included buckets 剩余总和
    - Free：`FREE_MONTHLY_INCLUDED`
    - 月付：`MONTHLY_INCLUDED`
    - 年付：`ANNUAL_INCLUDED`
  - 欢迎额度剩余
  - 超额包额度剩余
  - 减去未释放 reservation
- 若不足：
  - 返回 `409 INSUFFICIENT_CREDIT`
  - 不调用 AI
- 若足够：
  - 创建 `credit_reservation`
  - 按消费顺序锁定 bucket：
    1. included family
    2. 欢迎额度
    3. 超额包额度

> 前端展示的是“本次最多可能消耗张数”。  
> 由于生成前会再次实时复核 Shopify 当前 Alt，最终实际扣费张数可能**小于**预估上限。

#### 4.6.4 生成流程

1. 创建 `generation_batch`
2. 为每个 candidate 投递 `GenerateAltJob`
3. worker 单条处理时，按以下顺序执行：

**Step A：Shopify 线上真值复核**

- 在真正调用 AI 前，再次从 Shopify 读取当前 Alt：
  - `FILE_ALT`：读 `MediaImage.alt`
  - `COLLECTION_IMAGE_ALT`：读 `Collection.image.altText`
  - `ARTICLE_IMAGE_ALT`：读 `Article.image.altText`

若当前 Alt 已非空：

- 更新 `alt_target.current_alt_text`
- 更新 `alt_target.current_alt_empty = false`
- candidate -> `SKIPPED_ALREADY_FILLED`（下次扫描/发布会自然收敛为 `RESOLVED`）
- 释放该条对应 reservation
- 记录 `job_item = SKIPPED_ALREADY_FILLED`
- **不调用 AI**
- **不扣额度**

**Step B：构建生成上下文**

- 按 4.3.10 的规则计算 `context_mode`
- 生成 `context_snapshot`
- 对共享文件使用 `SHARED_NEUTRAL` Prompt 约束

**Step C：AI 调用**

- 读取 `preview_url + context_snapshot`
- 调主模型
- 失败时按策略降级模型
- 生成结果做轻度清洗：
  - 英文
  - trim
  - ≤125 字符

**Step D：成功收敛**

成功：

- 写 `alt_draft`
- 保存：
  - `model_used`
  - `context_mode`
  - `context_snapshot`
- 从 reservation 转正式消费 1
- 写 `credit_ledger`
- candidate -> `GENERATED`

失败：

- candidate -> `GENERATION_FAILED_RETRYABLE`
- 记录错误码与错误信息
- 释放该条对应 reservation

#### 4.6.5 计费口径

- **成功生成即扣额度**
- 生成前若实时复核发现 Shopify 当前 Alt 已非空：
  - 不调用 AI
  - 不扣额度
- 后续是否写回不影响额度消耗
- 扫描不消耗额度

---

### 4.7 审阅 / 编辑

列表展示数据源为：

`candidate_group_projection + alt_candidate + alt_target + alt_draft`

列表展示：

- 缩略图
- 图片类型标签
- group 类型
- `primaryUsage.title / handle`
- `primaryUsage.positionIndex`（如适用）
- `additionalUsageCount`
- `impactScopeSummary`
- 上下文
- `usageCountPresent`
- `contextMode`
- AI 草稿
- 可编辑输入框
- 装饰性标记开关
- 当前状态标签：
  - `MISSING`
  - `GENERATION_FAILED_RETRYABLE`
  - `GENERATED`
  - `WRITEBACK_FAILED_RETRYABLE`

编辑保存到 `alt_draft.edited_text`。

共享文件详情展示建议：

- 列表页默认展示 `primaryUsage + additionalUsageCount`
- 点击详情或悬浮展开后，再调用 usage 详情接口读取全部 `PRESENT image_usage`
- usage 列表按：
  - `usage_type`
  - `position_index ASC NULLS LAST`
  - `usage_id ASC`
  排序

写回前二次确认弹窗要求：

- 展示选中数量
- 展示本次写回图片类型分布
- 对 `FILE_ALT` 且 `usageCountPresent > 1` 的项，提示全局生效影响范围
- 对 `contextMode = SHARED_NEUTRAL` 的项，提示其为共享文件草稿

---

### 4.8 写回（按 Alt Plane 路由）与审计

#### 4.8.1 触发

`POST /api/writeback/start { altCandidateIds }`

#### 4.8.2 前置校验

- 当前 shop 无 `SCAN / GENERATE` 锁
- candidate 资源类型必须在 `effective_read_scope_flags` 内
- candidate 状态必须为：
  - `GENERATED`
  - 或 `WRITEBACK_FAILED_RETRYABLE`
- 存在 draft
- 非 decorative

#### 4.8.3 写回流程

**Step A：二次读校验**

读取线上当前值：

- `FILE_ALT`：读取 `MediaImage.alt`
- `COLLECTION_IMAGE_ALT`：读取 `Collection.image.altText`
- `ARTICLE_IMAGE_ALT`：读取 `Article.image.altText`

若已非空：

- candidate -> `SKIPPED_ALREADY_FILLED` 或 `RESOLVED`
- 不执行 mutation

**Step B：按平面路由写回**

- `FILE_ALT`：`fileUpdate`
- `COLLECTION_IMAGE_ALT`：`collectionUpdate`
- `ARTICLE_IMAGE_ALT`：`articleUpdate`

**Step C：成功 / 失败收敛**

成功：

- `alt_draft.final_text` 固化
- 写 `audit_log`
- candidate -> `WRITTEN`
- target `current_alt_text`、`current_alt_empty` 立即更新为乐观一致
- decorative mark 若存在则保留，但该项不再计入 decorative

失败：

- candidate -> `WRITEBACK_FAILED_RETRYABLE`
- 记录错误码与错误信息
- 支持重试

---

### 4.9 计费与配额（严格对齐产品）

#### 4.9.1 计划配置

| 计划 | 月付 | 年付折算月价 | 月配额 | 年付一次性发放总量 | 超额包 | 自动增量扫描 |
|---|---:|---:|---:|---:|---|---|
| FREE | $0 | — | 25 | — | 无 | 否 |
| STARTER | $4.99 | $3.49 | 150 | 1800 | $2.99 / 100 | 是 |
| GROWTH | $9.99 | $6.99 | 350 | 4200 | $4.99 / 200 | 是 |
| PRO | $14.99 | $10.49 | 800 | 9600 | $7.99 / 400 | 是 |
| MAX | $24.99 | $17.49 | 2000 | 24000 | $9.99 / 800 | 是 |

欢迎额度：

- 安装赠送：50
- 首次开通付费计划时，再按计划赠送一次：
  - Starter：200
  - Growth：500
  - Pro：1000
  - Max：3000

#### 4.9.2 商业规则

- 扫描不计费
- 成功生成即扣 1
- 超额包必须用户主动购买
- 不自动超扣
- Free 不可购买超额包

**Free 月配额规则**

- Free 计划使用 `FREE_MONTHLY_INCLUDED` bucket
- 周期锚点：**UTC 自然月**
- 每个店铺每个自然月最多发放 1 个 Free 月配额 bucket：
  - `granted_amount = 25`
  - `cycle_key = FREE:YYYY-MM`
- 安装当月立即发放当前自然月 Free bucket
- 月切时若店铺当前为 Free，自动发放下一自然月 Free bucket
- 月中从付费降级回 Free 时：
  - 若当月尚未存在 Free bucket，则立即补发当月 bucket，供当月剩余时间使用
  - 若当月已经发放过 Free bucket，则**不得重复发放**
- Free bucket 到月末失效，不跨月结转

**付费 included 规则**

- 月付计划：
  - 每个计费月创建 `MONTHLY_INCLUDED` bucket
  - 月末或账单周期末到期并按月重置
- 年付计划：
  - 激活或续费时一次性创建 `ANNUAL_INCLUDED` bucket
  - 发放额度 = `12 × 对应月配额`
  - 年末到期并按年重置

**计划切换规则**

- 升级到付费：
  - 自动增量扫描立即可用
  - 按新订阅生效周期发放新的 included bucket
  - 首次付费欢迎额度仅发放一次
  - 已存在但未过期的 Free bucket 不强制回收，按自然到期结束
- 降级到 Free：
  - 自动增量扫描关闭
  - 历史未用完欢迎额度 / 超额包额度仍可继续消耗
  - 若当前自然月尚无 Free bucket，则补发当前月 Free bucket
- 回归 Free 的行为必须可由账本审计，不依赖隐式逻辑

#### 4.9.3 账本设计

采用四层模型：

1. **billing_subscription**
   - 当前订阅计划与周期
2. **overage_pack_purchase**
   - 超额包购买记录
3. **credit_bucket**
   - 额度桶
   - 类型：
     - `FREE_MONTHLY_INCLUDED`
     - `MONTHLY_INCLUDED`
     - `ANNUAL_INCLUDED`
     - `WELCOME`
     - `OVERAGE_PACK`
4. **credit_reservation / credit_reservation_line / credit_ledger**
   - 预留、消费、释放流水

#### 4.9.4 额度消费顺序

固定顺序：

1. 当前有效 included family
   - `FREE_MONTHLY_INCLUDED / MONTHLY_INCLUDED / ANNUAL_INCLUDED`
   - 如存在多个 active included bucket，内部按：
     - `expires_at ASC`
     - `effective_at ASC`
     - `created_at ASC`
2. 欢迎额度
3. 超额包额度

#### 4.9.5 生成前预留

- batch 启动前一次性预留本次所需张数上限
- reservation 不是最终扣费
- 每条成功生成时，再将 1 个预留转换为正式消费
- 生成前实时复核若发现线上 Alt 已非空：
  - 释放对应预留
  - 不产生 consume
- 失败条目释放对应预留
- batch 完成后释放未使用预留

#### 4.9.6 Shopify 账单事件与发放任务

**安装时**

- 创建安装欢迎额度 bucket：50
- 创建当前自然月 Free bucket：25

**Free 月切发放**

- 每日定时 job 检查：
  - 若 `current_plan = FREE`
  - 且当前自然月不存在 `FREE_MONTHLY_INCLUDED`
  - 则创建该月 bucket
- 通过 `cycle_key` 与唯一约束防重

**计划购买 / 变更**

- 用户在账单页选择 `plan + interval`
- 创建 Shopify 订阅确认链接
- 回调或 webhook 成功后更新 `billing_subscription`
- 若是首次付费成功：
  - 发放对应欢迎额度 bucket
- included 发放规则：
  - `MONTHLY`：创建 `MONTHLY_INCLUDED`
  - `ANNUAL`：创建 `ANNUAL_INCLUDED`，额度为 12 倍月配额

**降级回 Free**

- 当付费订阅结束并进入 Free：
  - 关闭自动增量扫描
  - 若当前自然月尚无 `FREE_MONTHLY_INCLUDED`，立即补发 25

**超额包购买**

- 仅付费计划允许
- 用户必须主动进入账单页购买
- 创建一次性购买确认
- 成功后创建 `OVERAGE_PACK` bucket
- 与购买记录写入 `billing_ledger`

**前端展示规则**

- 首页与生成预检接口返回：
  - `includedRemaining`
  - `includedPeriodType = MONTHLY | ANNUAL`
  - `welcomeRemaining`
  - `overageRemaining`
- 对 Free 计划，`includedPeriodType` 仍返回 `MONTHLY`
- 账单页可额外展示 `includedBuckets[]` 以便解释计划切换后的额度来源

> 架构中不再使用自动 Usage Charge，也不存在“超量后后台自动结算”的流程。

---

### 4.10 数据留存、卸载、GDPR

- 最近一次已发布扫描结果：保留当前最新一份
- `alt_draft`：30 天
- `audit_log`：90 天
- `decorative_mark`：持久化，不自动清除
- `scan_task_attempt` 与 staging：
  - 成功 attempt 可保留 7 天
  - 失败 attempt 可保留 7 天便于排障
- `scan_result_*`：
  - publish 后保留 7 天便于排障
- `webhook_event`：已处理 7 天后清理
- `candidate_group_projection`：随已发布结果长期保留，下一次 publish / 增量 patch 时原子重建受影响切片
- `resource_image_fingerprint`：长期保留，卸载时删除
- `APP_UNINSTALLED`：删除 shop 全量数据
- GDPR：
  - customer 数据不存储，但接口必须正确响应
  - `SHOP_REDACT` 按要求清理店铺数据

---

## 5. 数据模型（PostgreSQL，建议表）

> 仅列关键字段；落地时补全索引、外键、迁移脚本。

### 5.1 shops

- `id`
- `shop_domain`
- `access_token_encrypted`
- `installed_at`
- `uninstalled_at`
- `current_plan`（FREE / STARTER / GROWTH / PRO / MAX）
- `billing_interval`（NONE / MONTHLY / ANNUAL）
- `incremental_scan_enabled bool`
- `scan_scope_flags jsonb`
- `scan_scope_updated_at`
- `last_published_scope_flags jsonb`
- `first_paid_bonus_granted_at`
- `last_published_scan_job_id`
- `last_published_at`
- `created_at`
- `updated_at`

---

### 5.2 scan_notice_ack

- `id`
- `shop_id`
- `notice_version`
- `scope_flags_snapshot jsonb`
- `acknowledged_at`

约束：

- `unique(shop_id, notice_version)`

---

### 5.3 shop_operation_lock

- `shop_id`
- `lock_type`
- `batch_id`
- `acquired_at`
- `heartbeat_at`
- `expires_at`
- `released_at`
- `status`

---

### 5.4 scan_job

- `id`
- `shop_id`
- `status`（RUNNING / SUCCESS / PARTIAL_SUCCESS / FAILED）
- `publish_status`（PENDING / PUBLISHED / NOT_PUBLISHED）
- `scope_flags jsonb`
- `notice_version`
- `successful_resource_types jsonb`
- `failed_resource_types jsonb`
- `started_at`
- `finished_at`
- `published_at`
- `error`

---

### 5.5 scan_task

- `id`
- `shop_id`
- `scan_job_id`
- `resource_type`（PRODUCT_MEDIA / FILES / COLLECTION_IMAGE / ARTICLE_IMAGE）
- `status`
- `current_attempt_no`
- `successful_attempt_id`
- `max_parse_attempts`
- `started_at`
- `finished_at`
- `error`

---

### 5.6 scan_task_attempt

- `id`
- `shop_id`
- `scan_task_id`
- `attempt_no`
- `status`（PENDING / RUNNING / READY_TO_PARSE / PARSING / SUCCESS / FAILED）
- `bulk_operation_id`
- `bulk_result_url`
- `result_url_fetched_at`
- `parsed_rows`
- `last_parse_error`
- `started_at`
- `finished_at`

约束：

- `unique(scan_task_id, attempt_no)`

---

### 5.7 Bulk Staging

#### stg_product

- `id`
- `shop_id`
- `scan_task_attempt_id`
- `product_id`
- `title`
- `handle`

约束：`unique(shop_id, scan_task_attempt_id, product_id)`

#### stg_media_image_product

- `id`
- `shop_id`
- `scan_task_attempt_id`
- `media_image_id`
- `parent_product_id`
- `alt`
- `url`
- `width`
- `height`
- `position_index`

约束：`unique(shop_id, scan_task_attempt_id, media_image_id, parent_product_id)`

#### stg_media_image_file

- `id`
- `shop_id`
- `scan_task_attempt_id`
- `media_image_id`
- `alt`
- `url`
- `width`
- `height`

约束：`unique(shop_id, scan_task_attempt_id, media_image_id)`

#### stg_collection

- `id`
- `shop_id`
- `scan_task_attempt_id`
- `collection_id`
- `title`
- `handle`
- `image_alt_text`
- `image_url`

#### stg_article

- `id`
- `shop_id`
- `scan_task_attempt_id`
- `article_id`
- `title`
- `handle`
- `image_alt_text`
- `image_url`

---

### 5.8 scan_result_target

- `id`
- `shop_id`
- `scan_job_id`
- `resource_type`（PRODUCT_MEDIA / FILES / COLLECTION_IMAGE / ARTICLE_IMAGE）
- `alt_plane`
- `write_target_id`
- `locale`
- `display_title`
- `display_handle`
- `preview_url`
- `current_alt_text`
- `current_alt_empty`
- `created_at`
- `updated_at`

约束：

- `unique(shop_id, scan_job_id, resource_type, alt_plane, write_target_id, locale)`

---

### 5.9 scan_result_usage

- `id`
- `shop_id`
- `scan_job_id`
- `resource_type`
- `alt_plane`
- `write_target_id`
- `locale`
- `usage_type`（PRODUCT / FILE）
- `usage_id`
- `title`
- `handle`
- `position_index`
- `created_at`
- `updated_at`

约束：

- `unique(shop_id, scan_job_id, resource_type, alt_plane, write_target_id, usage_type, usage_id)`

---

### 5.10 alt_target

- `id`
- `shop_id`
- `alt_plane`
- `write_target_id`
- `locale`
- `display_title`
- `display_handle`
- `preview_url`
- `current_alt_text`
- `current_alt_empty`
- `last_published_scan_job_id`
- `last_seen_at`
- `present_status`（PRESENT / NOT_FOUND）
- `created_at`
- `updated_at`

约束：

- `unique(shop_id, alt_plane, write_target_id, locale)`

> 说明：`display_title / display_handle` 对 `FILE_ALT` 仅作 target 级 fallback 字段；前台 group 列表主标题以 `candidate_group_projection.primary_title / primary_handle` 为准。

---

### 5.11 image_usage

- `id`
- `shop_id`
- `alt_target_id`
- `usage_type`（PRODUCT / FILE）
- `usage_id`
- `title`
- `handle`
- `position_index`
- `last_published_scan_job_id`
- `last_seen_at`
- `present_status`（PRESENT / NOT_FOUND）
- `created_at`
- `updated_at`

约束：

- `unique(shop_id, alt_target_id, usage_type, usage_id)`

---

### 5.12 decorative_mark

- `id`
- `shop_id`
- `alt_target_id`
- `is_active`
- `marked_at`
- `unmarked_at`
- `created_at`
- `updated_at`

约束：

- `unique(shop_id, alt_target_id)`

---

### 5.13 alt_candidate

- `id`
- `shop_id`
- `alt_target_id`
- `status`（MISSING / GENERATION_FAILED_RETRYABLE / GENERATED / WRITEBACK_FAILED_RETRYABLE / WRITTEN / RESOLVED / NOT_FOUND / DECORATIVE_SKIPPED / SKIPPED_ALREADY_FILLED）
- `missing_reason`（NULL / EMPTY / WHITESPACE）
- `risk_flags jsonb`
- `first_seen_at`
- `last_seen_at`
- `last_seen_scan_job_id`
- `written_at`
- `error_code`
- `error_message`
- `created_at`
- `updated_at`

约束：

- `unique(alt_target_id)`

---

### 5.14 candidate_group_projection

- `id`
- `shop_id`
- `group_type`（PRODUCT_MEDIA / FILES / COLLECTION / ARTICLE）
- `alt_candidate_id`
- `alt_target_id`
- `primary_usage_type`（PRODUCT / FILE / SELF）
- `primary_usage_id`
- `primary_title`
- `primary_handle`
- `primary_position_index`
- `additional_usage_count`
- `usage_count_present`
- `impact_scope_summary jsonb`
- `last_published_scan_job_id`
- `created_at`
- `updated_at`

约束：

- `unique(shop_id, group_type, alt_candidate_id)`

---

### 5.15 alt_draft

- `id`
- `shop_id`
- `alt_candidate_id`
- `model_used`
- `context_mode`（RESOURCE_SPECIFIC / FILE_NEUTRAL / SHARED_NEUTRAL）
- `context_snapshot jsonb`
- `generated_text`
- `edited_text`
- `final_text`
- `created_at`
- `updated_at`
- `expires_at`

约束：

- `unique(alt_candidate_id)`

---

### 5.16 audit_log

- `id`
- `shop_id`
- `alt_plane`
- `write_target_id`
- `alt_candidate_id`
- `old_alt_text`
- `new_alt_text`
- `written_at`
- `model_used`

---

### 5.17 webhook_event

- `id`
- `shop_id`
- `webhook_id`
- `topic`
- `resource_id`
- `payload jsonb`
- `status`（PENDING / COALESCED / PROCESSING / PROCESSED / SKIPPED_PLAN / SKIPPED_SCOPE / SKIPPED_NO_IMAGE_CHANGE / FAILED）
- `attempts`
- `last_attempt_at`
- `processed_at`
- `created_at`

约束：

- `unique(webhook_id)`

---

### 5.18 resource_image_fingerprint

- `id`
- `shop_id`
- `resource_type`（PRODUCT / COLLECTION）
- `resource_id`
- `fingerprint_hash`
- `last_processed_webhook_id`
- `last_processed_at`
- `created_at`
- `updated_at`

约束：

- `unique(shop_id, resource_type, resource_id)`

---

### 5.19 job_batch / job_item

`job_batch`

- `id`
- `shop_id`
- `type`（SCAN / GENERATE / WRITEBACK）
- `reservation_id`
- `total`
- `success`
- `failed`
- `skipped`
- `status`
- `started_at`
- `finished_at`

`job_item`

- `id`
- `batch_id`
- `alt_candidate_id`
- `status`
- `error`

---

### 5.20 billing_subscription

- `id`
- `shop_id`
- `plan_code`
- `billing_interval`
- `provider_subscription_id`
- `status`（ACTIVE / CANCELED / PENDING / FAILED）
- `current_period_start`
- `current_period_end`
- `created_at`
- `updated_at`

---

### 5.21 overage_pack_purchase

- `id`
- `shop_id`
- `plan_code`
- `pack_size`
- `price_amount`
- `currency`
- `provider_purchase_id`
- `status`（PENDING / ACTIVE / FAILED / REFUNDED）
- `purchased_at`
- `created_at`
- `updated_at`

---

### 5.22 credit_bucket

- `id`
- `shop_id`
- `bucket_type`（FREE_MONTHLY_INCLUDED / MONTHLY_INCLUDED / ANNUAL_INCLUDED / WELCOME / OVERAGE_PACK）
- `source_type`（FREE_MONTHLY_GRANT / MONTHLY_GRANT / ANNUAL_GRANT / INSTALL_BONUS / FIRST_PAID_BONUS / PACK_PURCHASE）
- `plan_code`
- `granted_amount`
- `consumed_amount`
- `reserved_amount`
- `remaining_amount`
- `effective_at`
- `expires_at`
- `cycle_key`
- `purchase_id`
- `status`（ACTIVE / EXHAUSTED / EXPIRED）
- `created_at`
- `updated_at`

约束：

- 周期性额度桶增加防重约束：  
  `unique(shop_id, bucket_type, cycle_key)`  
  适用于 `FREE_MONTHLY_INCLUDED / MONTHLY_INCLUDED / ANNUAL_INCLUDED` 且 `cycle_key is not null`

---

### 5.23 credit_reservation

- `id`
- `shop_id`
- `batch_id`
- `reserved_amount`
- `consumed_amount`
- `released_amount`
- `status`（ACTIVE / PARTIAL / CONSUMED / RELEASED / EXPIRED）
- `expires_at`
- `created_at`
- `updated_at`

---

### 5.24 credit_reservation_line

- `id`
- `reservation_id`
- `credit_bucket_id`
- `reserved_amount`
- `consumed_amount`
- `released_amount`
- `priority_order`
- `created_at`
- `updated_at`

---

### 5.25 credit_ledger

- `id`
- `shop_id`
- `credit_bucket_id`
- `reservation_id`
- `alt_candidate_id`
- `event_type`（GRANT / RESERVE / CONSUME / RELEASE / EXPIRE / ADJUST）
- `amount`
- `idempotency_key`
- `created_at`

约束：

- `unique(idempotency_key)`

---

### 5.26 billing_ledger

- `id`
- `shop_id`
- `event_type`（SUBSCRIPTION_ACTIVATED / SUBSCRIPTION_CHANGED / PACK_PURCHASED / REFUND）
- `provider_reference_id`
- `amount`
- `currency`
- `payload jsonb`
- `created_at`

---

## 6. 接口设计（Web / API + Webhooks）

### 6.1 前端 API（REST 示例）

`GET /api/bootstrap`

- 返回：
  - 当前计划与额度摘要
  - `includedRemaining`
  - `includedPeriodType`
  - 是否需要说明确认
  - 当前 `notice_version`
  - `lastPublishedAt`
  - 是否存在最近扫描结果
  - 是否有 RUNNING 的 scan job
  - `scanScopeFlags`
  - `lastPublishedScopeFlags`
  - `effectiveReadScopeFlags`

`POST /api/settings/scope`

- body:
  - `scopeFlags`
- 行为：
  - 仅更新 `shops.scan_scope_flags`
  - 不触发扫描
  - 不修改 `last_published_scope_flags`
- 返回：
  - `scanScopeFlags`
  - `lastPublishedScopeFlags`
  - `effectiveReadScopeFlags`

`POST /api/scan/start`

- body:
  - `scopeFlags`
  - `noticeVersion`
- 行为：
  - 若当前版本未确认，则先写 `scan_notice_ack`
  - 若 body 带 `scopeFlags`，则先更新 `shops.scan_scope_flags`
  - 获取 scan 锁
  - 创建 `scan_job`
- 说明：
  - `scopeFlags` 不是唯一修改入口；独立设置请使用 `/api/settings/scope`

`GET /api/scan/status?scanJobId=...`

- 返回 task 进度
- 返回当前 attempt 状态与错误
- 返回 publish 状态
- 返回前台当前仍应读取的 `lastPublishedAt`

`GET /api/dashboard`

- 返回当前 `effective_read_scope_flags` 内的分组统计：
  - `total / hasAlt / missing / decorative`
- 返回 `lastPublishedAt`
- 返回当前是否有重新扫描进行中
- 返回计划与额度摘要
- 返回 scope 元信息

`GET /api/candidates?group=PRODUCT_MEDIA&status=MISSING`

- 支持 group：
  - PRODUCT_MEDIA
  - FILES
  - COLLECTION
  - ARTICLE
- 支持 status：
  - `MISSING`
  - `GENERATION_FAILED_RETRYABLE`
  - `GENERATED`
  - `WRITEBACK_FAILED_RETRYABLE`
- 仅返回 `effective_read_scope_flags` 内数据
- 查询基于 `candidate_group_projection`
- 每项返回：
  - `usageCountPresent`
  - `contextMode`
  - `primaryUsage`
    - `type`
    - `id`
    - `title`
    - `handle`
    - `positionIndex`
  - `additionalUsageCount`
  - `impactScopeSummary`

`GET /api/candidates/:altCandidateId/usages?group=PRODUCT_MEDIA`

- 返回该 candidate 当前 `PRESENT` 的 usage 列表
- 支持按当前 group 过滤相关 usage
- 每项返回：
  - `usageType`
  - `usageId`
  - `title`
  - `handle`
  - `positionIndex`
- 用于前台展开“影响范围”详情

`POST /api/decorative/mark`

- `{ altTargetId }`

`POST /api/decorative/unmark`

- `{ altTargetId }`

`POST /api/generation/preflight`

- `{ altCandidateIds: [] }`
- 返回：
  - 预计消耗上限
  - `includedRemaining`
  - `includedPeriodType`
  - 欢迎额度剩余
  - 超额包额度剩余
  - 是否足够

`POST /api/generation/start`

- `{ altCandidateIds: [] }`
- 前置：
  - 通过 preflight
  - candidate 状态在 `MISSING / GENERATION_FAILED_RETRYABLE`
  - candidate 在 `effective_read_scope_flags` 内
  - 创建 reservation
- 返回 `generation_batch_id`

`POST /api/draft/update`

- `{ altCandidateId, editedText }`

`POST /api/writeback/start`

- `{ altCandidateIds: [] }`
- 前置：
  - candidate 状态在 `GENERATED / WRITEBACK_FAILED_RETRYABLE`
  - candidate 在 `effective_read_scope_flags` 内
- 返回 `writeback_batch_id`

`GET /api/history`

- 返回审计记录

`GET /api/billing/summary`

- 返回当前计划
- `includedRemaining`
- `includedPeriodType`
- 欢迎额度余额
- 超额包余额
- 最近购买记录
- 可选返回 `includedBuckets[]` 供账单页解释

`POST /api/billing/change-plan`

- `{ planCode, billingInterval }`

`POST /api/billing/purchase-pack`

- `{ planCode }`
- 仅付费计划可用

`GET /api/sse?batchId=...`

- 返回 `{ current, total, status }`

### 6.2 Webhooks

- `POST /webhooks/app_uninstalled`
- `POST /webhooks/gdpr/*`
- `POST /webhooks/bulk_operations_finish`
- `POST /webhooks/products_update`
- `POST /webhooks/collections_update`
- 订阅 / 一次性购买成功回调或 webhook
  - 更新 `billing_subscription`
  - 更新 `overage_pack_purchase`
  - 发放相应 credit bucket
  - 写 `billing_ledger`

通用要求：

- HMAC 校验
- 记录 webhook id 防重放
- 快速 200
- 异步处理

---

## 7. 队列与并发策略（BullMQ）

队列类型：

- `scan_start`
- `parse_bulk_to_staging`
- `derive_scan_attempt_to_result`
- `publish_scan_result`
- `continuous_scan_debounce`
- `continuous_scan_product`
- `continuous_scan_collection`
- `generate_alt`
- `writeback`
- `billing_sync`
- `quota_grant`
- `reservation_reaper`
- `cleanup`
- `gdpr_delete`

并发建议：

- `generate_alt`：5~10
- `writeback`：3~5
- `parse_bulk_to_staging`：1
- `derive_scan_attempt_to_result`：1~2
- `publish_scan_result`：1
- `continuous_scan_debounce`：5
- `continuous_scan_*`：2~3
- `billing_sync`：1
- `quota_grant`：1

Webhook 延迟重试：

- 初始：30 秒
- 指数退避
- 最大：5 分钟
- 最大重试：20

---

## 8. 可靠性与幂等设计

1. `alt_target` 唯一键保证 inventory 幂等。  
2. `alt_candidate unique(alt_target_id)` 保证候选幂等。  
3. `candidate_group_projection` 与 `alt_candidate` 分离，保证展示语义不污染写回唯一性。  
4. `decorative_mark` 独立持久化，避免 candidate 状态变更导致标记丢失。  
5. Bulk 解析使用 `scan_task_attempt` 隔离，旧失败 attempt 不参与 derive。  
6. staging 以 `scan_task_attempt_id` 为唯一域，避免“幽灵数据”。  
7. Bulk URL 过期只重跑当前 task attempt，不回滚整 job。  
8. 全量扫描先写 `scan_result_*`，scan 完成前不触碰已发布结果。  
9. publish 采用单事务原子合并，避免用户看到新旧混杂结果。  
10. `PARTIAL_SUCCESS` 下仅成功资源类型允许 sweep，失败资源类型保持旧数据。  
11. `FILE_ALT` 的存在性由 `image_usage` 的 `PRESENT` 集合决定，避免单 task 缺席误删。  
12. `scope_flags` 通过 `scan_scope_flags + last_published_scope_flags + effective_read_scope_flags` 形成闭环。  
13. `/api/settings/scope` 立即更新当前 scope，但不改写 `last_published_scope_flags`，保证“当前设置”与“已发布覆盖范围”解耦。  
14. 增量扫描先做 `(shop_id, topic, resource_id)` 级 debounce / coalesce。  
15. 增量扫描再做 scope gate 与图片指纹比对，指纹未变则 `SKIPPED_NO_IMAGE_CHANGE`。  
16. 产品媒体 `position_index` 按明确字段或返回顺序持久化，保证审阅页位置展示稳定。  
17. 生成前必须再次实时读取 Shopify 当前 Alt，已非空则跳过、释放预留、不扣费。  
18. 共享文件生成使用显式 `context_mode`，多 usage 默认 `SHARED_NEUTRAL`。  
19. 写回前二次读校验，避免覆盖外部新增 Alt。  
20. shop 级锁避免 scan / generate / writeback 竞态。  
21. webhook_event 先落库再消费，且消费时受锁、计划权限、scope、图片指纹四重 gate。  
22. Candidate 失败状态拆分：
    - `GENERATION_FAILED_RETRYABLE`
    - `WRITEBACK_FAILED_RETRYABLE`
    保证失败项可重试。  
23. 额度预留与消费分离：
    - 无可用额度不调用 AI
    - reservation 与 consume 均具备幂等键。  
24. Free 月配额使用 `FREE_MONTHLY_INCLUDED + cycle_key`，天然防重复发放。  
25. 年付使用 `ANNUAL_INCLUDED` bucket，一次性发放全年总量，账本可审计。  
26. 超额包购买不自动触发，必须由用户显式确认。  
27. 订阅与购买记录写 `billing_ledger`，可审计。  

---

## 9. 安全、权限与合规

最小权限范围需按 Shopify 当前稳定版校对，原则上包括：

- 读：
  - products / media
  - files
  - collections
  - articles / content
- 写：
  - files
  - collections
  - articles / content

其他要求：

- token 加密存储
- webhook HMAC 校验
- API / SSE 基于 session 校验
- 不存储图片二进制
- 首次扫描前明确数据边界
- 草稿 30 天、审计 90 天、装饰性标记持久化、卸载即删

---

## 10. 可观测性与运维（Railway）

结构化日志建议字段：

- `shop_domain`
- `batch_id`
- `job_item_id`
- `alt_plane`
- `write_target_id`
- `group_type`
- `model_used`
- `context_mode`
- `duration_ms`
- `error_code`
- `error_message`

重点指标：

**扫描**

- 每类 task 行数
- attempt 次数
- URL 下载失败次数
- derive 产出 `scan_result_target / scan_result_usage` 数
- publish 耗时
- 本次 publish 替换的资源切片数
- 本次 publish 保留旧切片的失败资源类型数
- parentId 无法关联次数
- `position_index` 缺失或异常次数
- worker 内存峰值

**增量**

- `webhook_event` 延迟消费次数
- `COALESCED` 数量
- `SKIPPED_PLAN` 数量
- `SKIPPED_SCOPE` 数量
- `SKIPPED_NO_IMAGE_CHANGE` 数量
- 产品增量 usage sweep 数量
- 增量重建 `candidate_group_projection` 数量

**展示投影**

- `candidate_group_projection` 当前总量
- 同一 candidate 同时出现在多个 group 的数量
- `primary_usage` 缺失数量
- `additional_usage_count > 0` 的共享候选数量

**装饰性**

- 标记数量
- 取消标记数量
- decorative 占比

**生成**

- 生成前真值复核发现“已非空”数量
- `SKIPPED_ALREADY_FILLED` 数量
- `SHARED_NEUTRAL` 生成数量
- `RESOURCE_SPECIFIC` 生成数量

**计费**

- `FREE_MONTHLY_INCLUDED` 发放数
- `MONTHLY_INCLUDED` 发放数
- `ANNUAL_INCLUDED` 发放数
- 欢迎额度发放数
- 超额包购买成功率
- reservation 创建 / 释放 / 过期数量
- 生成前因额度不足被阻断次数
- 周期 bucket 重复发放冲突次数

**互斥**

- 各类 409 次数
- 锁超时回收次数

**scope**

- `/api/settings/scope` 调用次数
- scope 开关后立即生效的 `SKIPPED_SCOPE` 数量
- scope 重新勾回后恢复显示的 group 数量

**失败重试**

- `GENERATION_FAILED_RETRYABLE` 当前存量
- `WRITEBACK_FAILED_RETRYABLE` 当前存量
- 失败后重试成功率

---

## 11. 测试方案

### 11.1 Bulk 解析专项

- 4 类 bulk 样本脱敏固化
- `__parentId` 关联正确
- 同一 `MediaImage` 在 Product Media 与 Files 中仅生成 1 个 target
- derive 可重复执行且不重复增长
- derive 仅写 `scan_result_*`，不提前污染已发布结果
- 产品媒体 `position_index` 正确推导并落到 `stg_media_image_product / scan_result_usage / image_usage`

### 11.2 Bulk URL 过期与 Attempt 隔离

- 模拟 HTTP 403 / 410：
  - 旧 attempt 失败
  - 自动创建新 attempt
  - 新 `bulk_operation_id` 生效
- 模拟部分写入后失败：
  - 旧 attempt staging 不参与 derive
  - 不产生幽灵数据
- 达到 `max_parse_attempts`：
  - task `FAILED`
  - scan_job `PARTIAL_SUCCESS` 或 `FAILED`

### 11.3 已发布结果与原子发布

- 扫描进行中，Dashboard 始终读取旧的已发布结果
- 某个 task derive 成功后，旧结果不发生变化
- scan_job 完成后一次性切换为新结果
- scan_job 全部失败时，不发布、不污染旧结果

### 11.4 Partial Success 与 sweep 保护

- `FILES` task 失败、其余成功：
  - Product / Collection / Article 切片正常更新
  - Files 旧结果保留
  - Files 不被误标 `NOT_FOUND`
- `PRODUCT_MEDIA` 成功、`FILES` 失败：
  - 仅 sweep `usage_type = PRODUCT`
  - 不 sweep `usage_type = FILE`
- `COLLECTION_IMAGE` 失败：
  - 不对集合切片做缺席推断

### 11.5 首次扫描说明确认

- 首次进入必须先展示说明页
- 未确认不得启动扫描
- 确认后写入 `scan_notice_ack`
- 提升 `notice_version` 后再次扫描需重新确认

### 11.6 scope_flags 闭环测试

- 用户通过 `POST /api/settings/scope` 取消勾选 Collection 后：
  - Dashboard 不再显示 Collection 分组
  - `GET /api/candidates?group=COLLECTION` 无结果
  - `collections/update` webhook 标记 `SKIPPED_SCOPE`
- 用户重新勾选 Collection 且 `last_published_scope_flags` 已包含 Collection：
  - Collection 旧已发布数据立即重新可见
  - 不需要重新扫描即可恢复浏览
- 用户重新勾选某类 scope，但 `last_published_scope_flags` 不包含该类：
  - 该类仍不进入 `effective_read_scope_flags`
  - 直到重新扫描成功发布后才可见
- running scan 的 `scope snapshot` 不受设置页后续修改影响

### 11.7 互斥锁测试

- generate 运行中触发 scan：409
- scan 运行中触发 writeback：409
- 锁超时自动回收后可重试

### 11.8 装饰性图片测试

- 标记后：
  - missing 减 1
  - decorative 加 1
  - 不可生成
  - 不可写回
- 取消标记后：
  - 若有有效 draft，回到 GENERATED
  - 否则回到 MISSING
- 再次扫描后标记仍保留

### 11.9 增量扫描计划 gate、scope gate、debounce 与图片指纹

- Free 店铺收到 `products/update`：
  - `webhook_event = SKIPPED_PLAN`
  - 不更新 target / usage / candidate / projection
- 付费店铺但 Product Media 已不在 scope：
  - `webhook_event = SKIPPED_SCOPE`
  - 不更新 target / usage / candidate / projection
- 付费店铺短时间内收到同一 product 多个 update：
  - 仅处理 1 次
  - 其余标记 `COALESCED`
- product 标题变化但媒体不变：
  - 指纹相同
  - `SKIPPED_NO_IMAGE_CHANGE`
- product 增减媒体或修改图片 alt：
  - 指纹变化
  - 正常增量刷新
- 付费降级到 Free 后：
  - 后续 webhook 全部跳过

### 11.10 `image_usage` sweep 测试

- product 移除某媒体后，旧 PRODUCT usage 被标记 `NOT_FOUND`
- Files 全量扫描成功后，不再存在的 FILE usage 被标记 `NOT_FOUND`
- `FILE_ALT` target 仅在无任何 `PRESENT usage` 时才变为 `NOT_FOUND`
- 仪表盘 Product Media / Files 分组统计不受陈旧 usage 污染
- 产品增量后 `position_index` 正确更新

### 11.11 展示投影与图片位置测试

- 同一 `FILE_ALT` candidate 同时存在 `PRODUCT` 与 `FILE` usage 时：
  - 生成 2 条 projection：
    - `PRODUCT_MEDIA`
    - `FILES`
- `PRODUCT_MEDIA` projection：
  - `primaryUsage.title` 为主产品标题
  - `primaryPositionIndex` 正确
  - `additionalUsageCount` 正确
- `FILES` projection：
  - 不错误复用某个产品标题作为主标题
  - `impactScopeSummary` 正确反映共享范围
- `GET /api/candidates/:id/usages` 能返回完整 present usage 列表并按位置排序
- 同一 candidate 在不同 group 被选中时，最终写回对象仍是同一个 `alt_candidate_id`

### 11.12 共享文件上下文策略测试

- 仅 1 个 PRODUCT usage 的文件：
  - `contextMode = RESOURCE_SPECIFIC`
  - 可使用该产品标题
- 仅 1 个 FILE usage 的文件：
  - `contextMode = FILE_NEUTRAL`
  - 不得引入产品标题
- 多个 PRODUCT usage 或 PRODUCT + FILE 混合：
  - `contextMode = SHARED_NEUTRAL`
  - Prompt 中不得注入某个单一产品标题
- 审阅页正确显示 `usageCountPresent` 与 `contextMode`

### 11.13 Candidate 可重试状态测试

- 生成失败后：
  - candidate = `GENERATION_FAILED_RETRYABLE`
  - 可再次进入生成
- 写回失败后：
  - candidate = `WRITEBACK_FAILED_RETRYABLE`
  - 可再次进入写回
- 有 draft 且写回失败，再次扫描后仍可重试
- 已 resolved / decorative / not_found 的 candidate 不可误重试

### 11.14 生成前 Shopify 真值复核测试

- 扫描后、生成前，商家在 Shopify 后台手动补 Alt：
  - 生成任务实时读到非空 Alt
  - candidate -> `SKIPPED_ALREADY_FILLED`
  - reservation 释放
  - 不调用 AI
  - 不写 `credit_ledger consume`
- 同批次部分被手动补齐、部分仍为空：
  - 最终实际扣费张数小于预估上限
- 真值复核失败重试时：
  - 不得重复 consume

### 11.15 计费与额度测试

- 安装赠送 50 只发一次
- 安装当月创建 `FREE_MONTHLY_INCLUDED = 25`
- 同一自然月不得重复发 Free bucket
- 月切时 Free 店铺自动发下一自然月 bucket
- 年付计划一次性创建 `ANNUAL_INCLUDED = 12 × 月配额`
- 年度 bucket 到期后重置
- Starter / Growth / Pro / Max 可购买各自 pack，Free 不可购买
- reservation 按 included → 欢迎额度 → 超额包顺序占用
- included family 内按最早到期优先占用
- 生成失败释放 reservation
- 生成前真值复核发现已非空时释放 reservation
- 成功生成才真正 consume
- 不足额度时阻断生成，不调用 AI
- `billing/summary` 对 Free / 月付返回 `MONTHLY`，对年付返回 `ANNUAL`

### 11.16 图片类型覆盖测试

- Collection 缺失 -> 生成 -> `collectionUpdate` 成功
- Article 缺失 -> 生成 -> `articleUpdate` 成功
- Dashboard 四组统计正确
- Product Media / Files 审阅页可正确显示资源标题、位置与影响范围

---

## 12. 主要技术风险与备选方案

1. **Bulk 输出结构变化导致解析失败**  
   对策：固化样本 + 回归测试。

2. **Bulk 结果 URL 过期**  
   对策：task attempt 级自动重跑，不污染成功结果。

3. **全量扫描中途覆盖已发布结果**  
   对策：先写 `scan_result_*`，scan 结束后再 publish。

4. **Partial success 误删失败资源类型数据**  
   对策：仅成功资源类型允许 sweep，失败类型保留旧发布切片。

5. **陈旧 usage 导致分组统计失真**  
   对策：全量扫描与产品增量都做 scoped sweep；`FILE_ALT` 由 usage 聚合判定存在性。

6. **增量刷新噪音过大**  
   对策：先做 debounce / coalesce，再做 scope gate 与图片指纹 gate。

7. **scope 改动后展示与自动刷新不一致**  
   对策：使用 `scan_scope_flags + last_published_scope_flags + effective_read_scope_flags` 闭环，并提供独立 `/api/settings/scope`。

8. **重新勾回 scope 后前台行为不清晰**  
   对策：文档与产品文案明确为“恢复显示上次已发布历史数据；如需刷新请重新扫描”。

9. **装饰性标记与 draft / candidate 状态冲突**  
   对策：mark 独立建模，candidate 只做派生状态。

10. **唯一写回对象无法满足 group 展示需求**  
   对策：增加 `candidate_group_projection`，将展示语义与写回语义分离。

11. **产品媒体位置缺失，审阅页无法展示图片顺序**  
   对策：优先读 Shopify position 字段；否则按 product media 返回顺序生成 `position_index` 并持久化。

12. **Free 店铺月配额漏发或重复发放**  
   对策：`FREE_MONTHLY_INCLUDED + cycle_key + unique(shop_id, bucket_type, cycle_key)`。

13. **生成时店主已手动补 Alt 仍被扣费**  
   对策：AI 调用前逐条实时读取 Shopify 真值；已非空则释放预留并跳过。

14. **共享文件被写成某个产品专属描述**  
   对策：按 usage 数量切换 `context_mode`，多 usage 一律走 `SHARED_NEUTRAL`。

15. **额度竞争导致超用**  
   对策：batch 级 reservation，按 bucket 预留后才允许调用 AI。

16. **年付额度与账本口径不一致**  
   对策：独立 `ANNUAL_INCLUDED` bucket，年度发放、年度过期、年度重置。

17. **失败项前台可见但不可重试**  
   对策：拆分 `GENERATION_FAILED_RETRYABLE / WRITEBACK_FAILED_RETRYABLE` 状态。

18. **同一共享文件出现在多个分组引发用户困惑**  
   对策：UI 明确“按使用场景分组”，生成与写回仍只发生一次，并展示 `primaryUsage + additionalUsageCount`。

---

# 附录：fileUpdate 使用说明（已测试验证，术语与 ID 对齐）

Shopify 底层资产库已统一，`Product media` 是底层 `File/Media` 的引用；新版本已废弃 `productUpdateMedia`。对 **产品媒体与文件库图片** 的 Alt 更新，**优先使用 `fileUpdate` 更新全局资产**，并可同步到产品前端引用处。

> 注意：`fileUpdate` 要求目标文件处于 READY 状态（如可读到状态字段，建议写回前校验；否则按错误码重试/失败）。

## 开发防错指南：ID 格式

**核心陷阱：ID 格式错误会导致 404，绝不要使用旧版 ProductImage ID。**

### 1）查询规范（读）
不要查旧版产品 `images`。必须查产品 `media`，获取 **`MediaImage`** 的 ID。

- ✅ 正确：`gid://shopify/MediaImage/12345`
- ❌ 错误：`gid://shopify/ProductImage/12345`

### 2）写入规范（写）
使用 API Version: `2026-01`。将 **`MediaImage` ID** 作为 `fileUpdate` 的 `id` 传入：

```graphql
mutation {
  fileUpdate(files: [{
    id: "gid://shopify/MediaImage/12345",
    alt: "AI generated alt text"
  }]) {
    files { alt }
    userErrors { message }
  }
}
```

**执行结论**：只要 ID 是 `MediaImage`，调用一次 `fileUpdate` 即可完成全局生效，无需再做产品级额外更新。















# AltText Magic — 分阶段开发计划 v1.0

---

## 1. 项目概览

**AltText Magic** 是一款面向 Shopify 中小商家的嵌入式 App（Embedded App），通过 AI 在"可控、可审阅"的前提下，批量为店铺四类图片资源（产品媒体 / 文件库 / 集合封面 / 文章封面）补齐缺失的 Alt Text。MVP 需完整跑通 **"扫描 → 生成 → 审阅 → 写回"** 安全闭环，并支持装饰性图片标记、共享文件影响范围提示、Freemium 五档计费与超额包、付费计划专属增量扫描等能力。

**技术栈**：Node.js + TypeScript / React Router（Web）/ Prisma + PostgreSQL / Redis + BullMQ / Shopify App Bridge + Polaris / AI Gateway（主模型 + 降级模型）。部署目标为 Railway（web + worker + postgres + redis）。MVP 核心交付范围覆盖架构文档 §1.1 所列全部 21 项必须做项，以及 §1.2 非功能约束。

---

## 2. 阶段总览

| 阶段 | 名称 | 核心产出 | 预估工时 |
|:---:|:---|:---|:---:|
| 1 | 基础设施与 Shopify App 骨架 | 可部署的空壳 Embedded App | 8 人天 |
| 2 | 数据模型与核心服务层 | 完整 Schema 迁移 + Scope/Mutex/Notice 服务 | 6 人天 |
| 3 | 全量扫描管线 | Bulk 提交 → 流式解析 → Staging → Derive → 原子发布 | 14 人天 |
| 4 | 仪表盘、候选列表与装饰性标记 | Dashboard 分组统计 + 候选展示投影 + 装饰性标记 | 8 人天 |
| 5 | 计费与配额系统 | 五档订阅 + 欢迎额度 + Free 月配额 + 超额包 + 额度预留 | 10 人天 |
| 6 | AI 生成管线 | 额度预检 → 线上真值复核 → AI 调用 → 草稿 → 扣费 | 10 人天 |
| 7 | 审阅编辑与写回 | 可编辑审阅列表 + 按 alt_plane 路由写回 + 审计 | 8 人天 |
| 8 | 增量扫描与 Webhook 驱动 | Debounce + 四重 Gate + 图片指纹 + 原子 Patch | 7 人天 |
| 9 | 设置、历史记录与运维收尾 | Settings 页 / History 页 / 清理任务 / GDPR / 可观测性 | 6 人天 |
| 10 | 集成测试与上线准备 | 端到端回归 + App Store 提审材料 + FAQ | 7 人天 |
| | **合计** | | **~84 人天** |

> 工时基于 1 名全栈工程师全职开发估算；如有 2 人可并行，可压缩至 ~11 周（Phase 3-4 与 Phase 5 可部分并行）。

---

## 3. 各阶段详细计划

---

### Phase 1：基础设施与 Shopify App 骨架

**阶段目标**
搭建可部署的 Shopify Embedded App 空壳，完成 OAuth 安装流程、Session 持久化、基础 Webhook 注册与接收，建立 Railway 部署管线。此阶段结束后，App 可在 Shopify Admin 内以 iframe 形式加载并展示空白 Polaris 页面。

**功能范围**

| # | 功能项 | 对应架构 |
|---|--------|----------|
| 1.1 | 项目脚手架：React Router + TypeScript + Prisma + BullMQ | §3.1 App Server / §3.2 Railway |
| 1.2 | Railway 部署拓扑：`web` / `worker` / `postgres` / `redis` 四个 Service | §3.2 |
| 1.3 | Shopify OAuth 安装流程 + Offline Access Token 加密存储 | §4.1 |
| 1.4 | Session 持久化（Prisma Session Storage） | §4.1 |
| 1.5 | `shops` 表初始化（安装时写入 `shop_domain`, `installed_at`, `current_plan=FREE`, 默认 `scan_scope_flags`） | §5.1 |
| 1.6 | Webhook 注册：`APP_UNINSTALLED` / GDPR 三类 / `BULK_OPERATIONS_FINISH` / `products/*` / `collections/*` | §4.1 步骤 4 |
| 1.7 | Webhook Receiver：HMAC 校验 + `webhook_event` 幂等落库 + 快速 200 + BullMQ 投递 | §4.3.9 |
| 1.8 | `APP_UNINSTALLED` handler（标记 `uninstalled_at`，后续 Phase 9 补全删除） | §4.10 |
| 1.9 | GDPR Webhook handler（占位实现，返回 200） | §4.10 |
| 1.10 | App Bridge + Polaris 空壳页面（含导航结构占位：Dashboard / Review / History / Billing / Settings / Help） | §3.1 Web |
| 1.11 | 环境变量管理 + 基础结构化日志 | §10 |

**依赖关系**
- 外部：Shopify Partner Dashboard App 创建、Railway 账号、域名
- 内部：无前置阶段依赖

**技术方案**
- 使用 Shopify CLI 或 `@shopify/shopify-app-react-router` 模板初始化项目
- Prisma schema 此阶段只建 `shops` / `sessions` / `webhook_event` 三张表
- Token 加密使用 AES-256-GCM，密钥由环境变量注入
- BullMQ 连接 Redis，此阶段只创建 queue 实例，不处理业务 job
- Railway：`web` service 运行 React Router；`worker` service 运行 BullMQ processor（空循环）；Postgres 与 Redis 使用 Railway 内置插件

**验收标准**
1. ✅ 在 Shopify 开发店铺成功安装 App，Admin 内可打开 Embedded iframe
2. ✅ `shops` 表写入正确，`access_token_encrypted` 可解密还原
3. ✅ 卸载 App 后，`APP_UNINSTALLED` Webhook 被接收并写入 `webhook_event`
4. ✅ GDPR Webhook 返回 200，不报错
5. ✅ Railway `web` / `worker` / `postgres` / `redis` 四个 Service 全部 healthy
6. ✅ 结构化日志可在 Railway Log 中查看

**预估工时**：8 人天

---

### Phase 2：数据模型与核心服务层

**阶段目标**
完成 MVP 完整数据模型的 Prisma Schema 定义与迁移，并交付三个基础服务模块：**扫描说明确认（Notice）**、**Scope 管理**、**Shop 级互斥锁**。此阶段结束后，所有表结构就绪，`GET /api/bootstrap` 可返回正确的初始状态。

**功能范围**

| # | 功能项 | 对应架构 |
|---|--------|----------|
| 2.1 | 完整 Prisma Schema 定义（§5 全部 26 张表）并执行迁移 | §5.1–5.26 |
| 2.2 | 唯一约束与索引：`alt_target(shop_id, alt_plane, write_target_id, locale)`、`alt_candidate(alt_target_id)`、`candidate_group_projection(shop_id, group_type, alt_candidate_id)`、`credit_bucket(shop_id, bucket_type, cycle_key)` 等 | §5 各表约束 |
| 2.3 | `scan_notice_ack` 服务：创建确认记录、版本检查 | §4.2.1 |
| 2.4 | `shops.scan_scope_flags` 管理服务：读取 / 更新 / 计算 `effective_read_scope_flags` | §4.2.2 |
| 2.5 | `POST /api/settings/scope` API | §4.2.3 |
| 2.6 | `shop_operation_lock` 服务：获取锁 / 释放锁 / heartbeat / 超时检测 | §4.2.4–4.2.5 |
| 2.7 | `GET /api/bootstrap` API（返回计划、额度占位、notice 状态、scope 状态、最近扫描状态） | §6.1 |
| 2.8 | 安装时初始化逻辑完善：创建安装欢迎额度 bucket（50 张）+ 当月 Free 月配额 bucket（25 张） | §4.1 步骤 3 |

**依赖关系**
- 前置：Phase 1（App 骨架、`shops` 表基础字段、Prisma 连接）

**技术方案**
- 一次性迁移全部表结构，避免后续频繁 migration；字段级注释标注对应架构文档节号
- `scan_scope_flags` 与 `last_published_scope_flags` 使用 Prisma `Json` 类型，值为 `string[]`（如 `["PRODUCT_MEDIA","FILES","COLLECTION_IMAGE","ARTICLE_IMAGE"]`）
- `effective_read_scope_flags` 为计算属性，不持久化，在服务层 / API 层实时求交集
- `shop_operation_lock` 使用 `unique(shop_id)` + 事务内 `SELECT ... FOR UPDATE` 实现悲观锁；`expires_at` 默认 30 分钟，worker heartbeat 每 5 分钟刷新
- `credit_bucket` 初始化在 OAuth 安装回调中同步执行，事务内完成 shops 创建 + bucket 创建

**验收标准**
1. ✅ `npx prisma migrate deploy` 无错误，26 张表全部创建成功
2. ✅ 安装新店铺后，`credit_bucket` 中存在 `WELCOME(50)` + `FREE_MONTHLY_INCLUDED(25)` 两条记录
3. ✅ `GET /api/bootstrap` 对全新店铺返回 `needsNoticeAck: true`，scope 为默认四类
4. ✅ `POST /api/settings/scope` 只更新 `scan_scope_flags`，不修改 `last_published_scope_flags`
5. ✅ 互斥锁测试：acquire → 再次 acquire 返回 conflict → release → 可再次 acquire
6. ✅ 锁超时 30 分钟后，cleanup 可回收

**预估工时**：6 人天

---

### Phase 3：全量扫描管线

**阶段目标**
实现完整的 Catalog Scan 管线：从用户首次确认说明并点击"开始扫描"，到 Bulk 提交、流式下载与解析、Staging 落库、Derive 到待发布结果、原子发布到已发布表。此阶段结束后，扫描完成可产生正确的 `alt_target` / `image_usage` / `alt_candidate` / `candidate_group_projection` 数据。

**功能范围**

| # | 功能项 | 对应架构 |
|---|--------|----------|
| 3.1 | 首次扫描说明页前端（Polaris 组件：范围说明、用途、留存期限、AI 边界、scope 勾选） | §4.2.1 |
| 3.2 | `POST /api/scan/start`：写 `scan_notice_ack` + 更新 `scan_scope_flags` + 获取 SCAN 锁 + 创建 `scan_job` + 按 scope 创建 `scan_task` | §6.1 / §4.3.3–4.3.4 |
| 3.3 | 4 类 Bulk GraphQL 查询定义与真实样本验证 | §4.3.5 |
| 3.4 | `scan_start` BullMQ Job：逐个 task 提交 `bulkOperationRunQuery`，创建 `scan_task_attempt` | §4.3.4 / §4.3.6 |
| 3.5 | `BULK_OPERATIONS_FINISH` Webhook handler：定位 `scan_task_attempt`，更新 `bulk_result_url`，投递 `parse_bulk_to_staging` Job | §4.3.6 |
| 3.6 | `parse_bulk_to_staging` Job：流式下载 NDJSON + 流式解析 + `__parentId` 关联 + batch upsert staging | §4.3.6 |
| 3.7 | Staging 表数据写入：`stg_product` / `stg_media_image_product`（含 `position_index`）/ `stg_media_image_file` / `stg_collection` / `stg_article` | §5.7 |
| 3.8 | Bulk URL 过期恢复：下载失败时标记 attempt FAILED → 创建新 attempt → 重新提交 bulk（`max_parse_attempts` 限制） | §4.3.6 |
| 3.9 | `derive_scan_attempt_to_result` Job：从成功 attempt 的 staging 写入 `scan_result_target` / `scan_result_usage`（含去重：同一 `MediaImage` 只生成 1 条 target） | §4.3.6 Derive 规则 |
| 3.10 | `publish_scan_result` Job：原子发布到 `alt_target` / `image_usage` / `alt_candidate` / `candidate_group_projection` | §4.3.7 |
| 3.11 | `PARTIAL_SUCCESS` 处理：仅成功资源类型替换切片，失败类型保留旧数据 | §4.3.7 切片发布规则 |
| 3.12 | `FILE_ALT` target 存在性判定：基于 `image_usage` PRESENT 集合重算 | §4.3.7 |
| 3.13 | `image_usage` 全量扫描 sweep：成功 task 对应的 `usage_type` 做缺席标记 `NOT_FOUND` | §4.3.8 |
| 3.14 | Candidate 发布收敛规则：根据 target 状态 + decorative_mark + draft 状态派生 candidate status | §4.3.7 Candidate 发布收敛 |
| 3.15 | Group 展示投影重建：按 §4.3.7 规则为 FILE_ALT / COLLECTION / ARTICLE 生成投影 | §4.3.7 Group 展示投影重建 |
| 3.16 | `shops.last_published_scope_flags` / `last_published_at` / `last_published_scan_job_id` 更新 | §4.3.7 发布完成 |
| 3.17 | `GET /api/scan/status` API：返回 task 进度、attempt 状态、publish 状态 | §6.1 |
| 3.18 | SSE 进度推送（`GET /api/sse?batchId=...`）：扫描阶段进度 | §6.1 |
| 3.19 | 前端扫描进度页：骨架屏 / 进度动画 / "正在扫描您的店铺…" | §3 产品意图书 |

**依赖关系**
- 前置：Phase 2（全部表结构、Notice 服务、Scope 服务、Mutex 服务）
- 外部：Shopify Bulk Operations API 可用、真实测试店铺有足够样本数据

**技术方案**
- **Bulk 提交**：每个 `scan_task` 独立提交一个 `bulkOperationRunQuery`；Shopify 同一 App 同一 Shop 同时只能有 1 个 running bulk，因此 4 个 task 需串行提交（前一个完成后再提交下一个），或利用 BullMQ 的 delayed job 排队
- **流式解析**：采用 `fetch → response.body (ReadableStream) → ndjson-parse transform → batch upsert` 管道，禁止整文件读入内存；batch upsert 每 500 行 flush 一次
- **`__parentId` 关联**：产品媒体的 NDJSON 中，`MediaImage` 行通过 `__parentId` 关联其父 `Product` 行。解析器需维护一个 `parentId → Product` 的内存 Map（按 product 维度，不会超出内存）
- **`position_index`**：优先读取 Shopify 返回的 position 字段；若不可用，解析器为同一 product 下的 `MediaImage` 按出现顺序赋值 `0, 1, 2, ...`
- **去重**：derive 阶段对 `scan_result_target` 使用 `ON CONFLICT (shop_id, scan_job_id, resource_type, alt_plane, write_target_id, locale) DO UPDATE` 实现 upsert；同一 `MediaImage` 来自 PRODUCT_MEDIA 和 FILES 两个 task 时合并为一条 `FILE_ALT` target
- **原子发布**：publish job 在单个 Prisma `$transaction` 中完成所有 upsert + sweep + candidate 收敛 + projection 重建 + shops 字段更新；对于大数据量店铺，可按资源类型切片事务（每个切片独立事务），但需保证同一切片内原子
- **SSE**：worker 将扫描进度写入 Redis（`scan:progress:{batchId}`），web 端通过 SSE 轮询 Redis 推送给前端

**验收标准**
1. ✅ 首次打开 App → 展示说明页 → 确认后 → 扫描开始 → 进度可见 → 完成后 `alt_target` / `image_usage` / `alt_candidate` / `candidate_group_projection` 数据正确
2. ✅ 4 类 Bulk 样本脱敏固化，`__parentId` 关联正确，产品媒体 `position_index` 连续
3. ✅ 同一 `MediaImage` 在产品媒体与 Files 中仅生成 1 条 `alt_target(FILE_ALT)`，`image_usage` 有 2 条（PRODUCT + FILE）
4. ✅ 模拟 Bulk URL 403：旧 attempt FAILED → 新 attempt 创建 → 重新 Bulk → 最终成功
5. ✅ 达到 `max_parse_attempts`：task FAILED → scan_job 进入 PARTIAL_SUCCESS 或 FAILED
6. ✅ PARTIAL_SUCCESS：仅成功切片 sweep，失败切片保留旧数据
7. ✅ 扫描进行中，前端始终读取旧已发布结果（或空状态），不出现新旧混杂
8. ✅ 扫描完成后，`shops.last_published_scope_flags` / `last_published_at` 正确更新
9. ✅ SCAN 锁期间，`POST /api/generation/start` 返回 409
10. ✅ 重新扫描按钮可正常触发新一轮全量扫描

**预估工时**：14 人天

---

### Phase 4：仪表盘、候选列表与装饰性标记

**阶段目标**
在 Phase 3 产出的已发布数据之上，构建用户可交互的仪表盘分组统计、候选列表（含展示投影、共享文件提示）以及装饰性图片标记功能。此阶段结束后，用户可查看扫描结果、浏览缺失图片列表、标记/取消装饰性图片。

**功能范围**

| # | 功能项 | 对应架构 |
|---|--------|----------|
| 4.1 | `GET /api/dashboard` API：按 `effective_read_scope_flags` 过滤，基于 `candidate_group_projection` 统计 `total / hasAlt / missing / decorative` | §4.4 |
| 4.2 | Dashboard 前端页面（Polaris）：四组卡片 + 当前计划与配额摘要（占位） + `lastPublishedAt` + "正在重新扫描"状态提示 + "重新扫描"按钮 | §4.4 |
| 4.3 | `GET /api/candidates` API：支持 `group` / `status` 过滤 + 分页；基于 `candidate_group_projection + alt_candidate + alt_target + alt_draft` 联查 | §6.1 |
| 4.4 | `GET /api/candidates/:altCandidateId/usages` API：返回 PRESENT 的 usage 列表 | §6.1 |
| 4.5 | 候选列表前端页面（Polaris）：缩略图 / 图片类型标签 / 主标题（`primaryUsage.title`）/ 位置 / `additionalUsageCount` / `usageCountPresent` / `contextMode` / 状态标签 / 展开影响范围详情 | §4.7 |
| 4.6 | `POST /api/decorative/mark` 与 `POST /api/decorative/unmark` API | §4.5 |
| 4.7 | 装饰性标记前端交互：每张图片旁的标记/取消切换 | §4.5 |
| 4.8 | 标记联动：mark 时 candidate → `DECORATIVE_SKIPPED`，missing -1 / decorative +1；unmark 时反向恢复 | §4.5.1–4.5.2 |
| 4.9 | out-of-scope group 不展示、不允许操作 | §4.2.2 C |

**依赖关系**
- 前置：Phase 3（已发布扫描结果、`candidate_group_projection`、`alt_candidate`）
- 前置：Phase 2（`effective_read_scope_flags` 计算、`decorative_mark` 表）

**技术方案**
- **Dashboard 统计**：单条 SQL 聚合查询，以 `candidate_group_projection.group_type` 分组，关联 `alt_target.current_alt_empty` 和 `decorative_mark.is_active` 计算四个指标；WHERE 条件限定 `group_type IN (effective_read_scope_flags 对应的 group 类型)`
- **候选列表**：基于 `candidate_group_projection` 做主查询，LEFT JOIN `alt_candidate` / `alt_target` / `alt_draft`；分页使用 cursor-based pagination（`id > :lastId LIMIT 20`）
- **Usage 详情**：独立 API 只在前端展开"影响范围"时调用，避免列表页 N+1
- **装饰性标记**：`decorative_mark` upsert 在事务内完成，同事务更新 `alt_candidate.status`；标记/取消后 Dashboard 需重新 fetch

**验收标准**
1. ✅ Dashboard 四组统计数字与 `candidate_group_projection` 数据一致
2. ✅ 同一共享文件同时出现在 Product Media 与 Files 两组统计中
3. ✅ 候选列表按 group 过滤后，`primaryUsage.title` / `positionIndex` / `additionalUsageCount` 正确展示
4. ✅ 用户取消 scope 中的 Collection 后，Dashboard 不再展示 Collection 分组，`GET /api/candidates?group=COLLECTION` 返回空
5. ✅ 标记装饰性 → missing 减 1、decorative 加 1；取消标记 → 反向恢复
6. ✅ 已标记装饰性的候选不可进入后续生成/写回操作入口

**预估工时**：8 人天

---

### Phase 5：计费与配额系统

**阶段目标**
接入 Shopify 订阅计费与一次性购买能力，实现 Free / Starter / Growth / Pro / Max 五档计划切换、月付/年付、欢迎额度、Free 月配额自动发放、超额包手动购买、额度消费顺序与预留机制。此阶段结束后，计费闭环完整可用，为 Phase 6 的 AI 生成提供额度保障。

**功能范围**

| # | 功能项 | 对应架构 |
|---|--------|----------|
| 5.1 | 计划配置常量定义（五档 + 月付/年付价格 + 月配额 + 年度总量 + 超额包规格 + 欢迎额度） | §4.9.1 |
| 5.2 | `POST /api/billing/change-plan`：创建 Shopify `appSubscriptionCreate` 确认链接 | §6.1 / §4.9 |
| 5.3 | 订阅成功回调 / Webhook 处理：更新 `billing_subscription` + 发放 included bucket + 首次付费欢迎额度 + 写 `billing_ledger` | §4.9.6 |
| 5.4 | 月付 `MONTHLY_INCLUDED` bucket 发放 | §4.9.2 |
| 5.5 | 年付 `ANNUAL_INCLUDED` bucket 一次性发放（`12 × 月配额`） | §4.9.2 |
| 5.6 | Free 月配额自动发放：`quota_grant` 定时 Job（每日检查 UTC 自然月 + `cycle_key` 防重） | §4.9.2 / §4.9.6 |
| 5.7 | `POST /api/billing/purchase-pack`：创建 Shopify `appPurchaseOneTimeCreate` → 成功后创建 `OVERAGE_PACK` bucket | §6.1 / §4.9.6 |
| 5.8 | 额度可用量计算服务：`included(按最早到期优先) → 欢迎额度 → 超额包` | §4.9.4 |
| 5.9 | `credit_reservation` 服务：batch 级预留 + `credit_reservation_line` 按 bucket 分拆 + consume / release | §4.9.5 |
| 5.10 | `POST /api/generation/preflight` API：预计消耗 / 各类余额 / 是否足够 | §6.1 |
| 5.11 | `GET /api/billing/summary` API：当前计划 + `includedRemaining` + `includedPeriodType` + 欢迎余额 + 超额包余额 + 最近购买记录 | §6.1 |
| 5.12 | Billing 前端页面（Polaris）：计划选择 + 月/年切换 + 超额包购买入口 + 余额展示 + 购买记录 | §4.9 |
| 5.13 | 降级回 Free 处理：关闭增量扫描 + 补发当月 Free bucket（若不存在）+ 历史欢迎额度/超额包保留 | §4.9.2 |
| 5.14 | 升级处理：新计划 included 发放 + 增量扫描启用 + 首次付费欢迎额度（仅一次） | §4.9.2 |
| 5.15 | `reservation_reaper` Job：过期预留释放 | §7 |
| 5.16 | Dashboard 首页配额摘要集成（替换 Phase 4 占位） | §4.4 |

**依赖关系**
- 前置：Phase 2（`credit_bucket` / `credit_reservation` / `billing_subscription` 等表结构）
- 前置：Phase 1（Shopify Session / Access Token，用于调用 Billing API）
- 外部：Shopify Billing API（`appSubscriptionCreate` / `appPurchaseOneTimeCreate`）

**技术方案**
- **Shopify 订阅**：使用 `appSubscriptionCreate` GraphQL mutation 创建月付/年付订阅；年付通过 `interval: ANNUAL` 参数实现；回调 URL 指向 `/api/billing/callback`
- **bucket 发放**：所有发放操作在事务内完成 bucket 创建 + `credit_ledger(GRANT)` 写入；周期性 bucket 使用 `unique(shop_id, bucket_type, cycle_key)` 防重
- **Free 月配额发放**：`quota_grant` BullMQ repeatable job 每日 00:05 UTC 执行一次；查询所有 `current_plan = FREE` 且当前自然月无 `FREE_MONTHLY_INCLUDED` bucket 的店铺，批量发放；`cycle_key = FREE:YYYY-MM`
- **额度消费顺序**：`credit_reservation` 创建时，按固定优先级遍历 active buckets（`included family(expires_at ASC)` → `WELCOME` → `OVERAGE_PACK`），分拆 `credit_reservation_line`
- **超额包**：`appPurchaseOneTimeCreate` 创建确认链接；用户确认后回调写入 `overage_pack_purchase` + `credit_bucket(OVERAGE_PACK)` + `billing_ledger`
- **降级/升级**：通过 Shopify 订阅状态 webhook 或定期同步（`billing_sync` job）检测计划变更

**验收标准**
1. ✅ 新安装店铺：`WELCOME(50)` + `FREE_MONTHLY_INCLUDED(25)` bucket 存在
2. ✅ Free 月切：下一自然月 bucket 自动发放，同月不重复发放
3. ✅ 升级到 Starter 月付：`MONTHLY_INCLUDED(150)` bucket 创建 + 首次付费欢迎额度 `WELCOME(200)` 创建
4. ✅ 升级到 Growth 年付：`ANNUAL_INCLUDED(4200)` bucket 一次性创建
5. ✅ 降级回 Free：当月 Free bucket 补发（若不存在）；增量扫描标记关闭
6. ✅ `preflight` 返回正确的可用额度与消费顺序
7. ✅ `reservation` 创建后，可用额度扣减正确；release 后恢复
8. ✅ Free 计划不可购买超额包
9. ✅ Starter 购买超额包成功后，`OVERAGE_PACK(100)` bucket 创建
10. ✅ `billing/summary` 正确展示计划信息与各类余额
11. ✅ `credit_bucket(shop_id, bucket_type, cycle_key)` 唯一约束阻止重复发放

**预估工时**：10 人天

---

### Phase 6：AI 生成管线

**阶段目标**
实现完整的 AI Alt Text 生成管线：用户选择缺失候选 → 额度预检与预留 → 逐条 Shopify 线上真值复核 → 上下文构建（含共享文件分级策略）→ AI 调用 → 草稿落库 → 额度正式消费。此阶段结束后，用户可为缺失图片批量生成 AI 草稿。

**功能范围**

| # | 功能项 | 对应架构 |
|---|--------|----------|
| 6.1 | `POST /api/generation/start` API：前置校验（锁 / scope / status / 非 decorative）→ preflight → reservation → 创建 `generation_batch` → 投递 `generate_alt` job | §4.6.1–4.6.3 |
| 6.2 | `generate_alt` BullMQ Job — Step A：Shopify 线上真值复核（按 `alt_plane` 分类读取当前 Alt） | §4.6.4 Step A |
| 6.3 | 真值复核已非空处理：`SKIPPED_ALREADY_FILLED` + 释放预留 + 不调 AI + 不扣费 | §4.6.4 Step A |
| 6.4 | `generate_alt` Job — Step B：上下文构建（`RESOURCE_SPECIFIC` / `FILE_NEUTRAL` / `SHARED_NEUTRAL`） | §4.3.10 / §4.6.4 Step B |
| 6.5 | `generate_alt` Job — Step C：AI Gateway 调用（主模型 + 降级模型） | §4.6.4 Step C |
| 6.6 | AI 输出清洗：英文、trim、≤125 字符、去除 "image of" / "photo of" 开头 | §4.6.4 Step C |
| 6.7 | `generate_alt` Job — Step D 成功：写 `alt_draft`（含 `model_used` / `context_mode` / `context_snapshot`）→ reservation 转 consume(1) → 写 `credit_ledger` → candidate → `GENERATED` | §4.6.4 Step D |
| 6.8 | `generate_alt` Job — Step D 失败：candidate → `GENERATION_FAILED_RETRYABLE` + 释放预留 + 记录 error | §4.6.4 Step D |
| 6.9 | batch 完成后释放未使用预留 | §4.9.5 |
| 6.10 | GENERATE 锁管理：batch 开始获取 → heartbeat → 完成释放 | §4.2.4 |
| 6.11 | SSE 进度推送：生成阶段进度（current / total / status） | §6.1 |
| 6.12 | 前端生成触发交互：选择候选 → 预检展示（预计消耗 / 各类余额）→ 确认 → 进度展示 → 完成/失败汇总 | §4.6 |
| 6.13 | 额度不足时前端阻断：展示余额不足提示 + 升级/购买超额包引导 | §4.6.3 |

**依赖关系**
- 前置：Phase 4（候选列表、`alt_candidate` 状态、`effective_read_scope_flags`）
- 前置：Phase 5（`credit_reservation` / `credit_bucket` / preflight / consume 服务）
- 外部：AI Provider API Key 与 Endpoint 配置

**技术方案**
- **AI Gateway**：封装统一 `AIGateway` 服务类，接受 `imageUrl + contextSnapshot + constraints` 参数，返回 `altText`；支持主模型调用 → 失败时自动切换降级模型；超时设为 30 秒
- **真值复核**：每条 job 在调用 AI 前，按 `alt_plane` 调用对应的 Shopify GraphQL 查询（单条读取，非 Bulk）；使用 Shopify API rate limiter 控制并发（建议 `generate_alt` queue concurrency = 5~10）
- **上下文构建**：从 `image_usage WHERE present_status = PRESENT` 统计 `usageCountPresent` 和 `usageTypesPresent`，按 §4.3.10 规则判定 `context_mode`；`SHARED_NEUTRAL` 时 Prompt 显式约束"不提及具体产品名"
- **Prompt 设计**：系统 Prompt 模板化，包含：角色设定、输出约束（英文 / ≤125字符 / 简洁具体 / 无 "image of" / 无关键词堆砌）、上下文注入、图片 URL；`context_mode` 不同时切换 Prompt 变体
- **幂等**：`credit_ledger` 使用 `idempotency_key = gen:{batch_id}:{candidate_id}:consume` 防止重复扣费

**验收标准**
1. ✅ 选择 5 张缺失图片 → preflight 展示正确 → 确认 → 生成完成 → `alt_draft` 写入 5 条 → `credit_ledger` 写入 5 条 CONSUME
2. ✅ 其中 1 张在生成前被商家手动补 Alt → 该条 `SKIPPED_ALREADY_FILLED` + 无 CONSUME 记录 + 最终实际扣费 4 张
3. ✅ `SHARED_NEUTRAL` 模式下生成的 Alt Text 不包含任何具体产品名称
4. ✅ AI 主模型超时 → 降级模型成功 → 草稿正常写入
5. ✅ AI 主模型 + 降级模型均失败 → candidate → `GENERATION_FAILED_RETRYABLE` + 预留释放
6. ✅ `GENERATION_FAILED_RETRYABLE` 的 candidate 可再次被选中进入生成
7. ✅ 额度不足 → `409 INSUFFICIENT_CREDIT` → 前端展示升级引导
8. ✅ GENERATE 锁期间，`POST /api/scan/start` 返回 409
9. ✅ 生成结果 Alt Text 均 ≤ 125 字符，无 "image of" / "photo of" 开头
10. ✅ SSE 实时推送生成进度

**预估工时**：10 人天

---

### Phase 7：审阅编辑与写回

**阶段目标**
实现审阅/编辑列表与批量写回闭环：用户可浏览 AI 草稿、手动编辑、按图片类型筛选、全选/单选后执行写回。写回按 `alt_plane` 路由到不同 Shopify mutation，并完成审计记录。此阶段结束后，MVP 核心闭环"扫描 → 生成 → 审阅 → 写回"完整跑通。

**功能范围**

| # | 功能项 | 对应架构 |
|---|--------|----------|
| 7.1 | 审阅列表前端页面（Polaris）：缩略图 + 图片类型标签 + `primaryUsage` 标题/位置 + AI 草稿 + 可编辑输入框 + 状态标签 + 装饰性标记开关 + 共享文件影响范围提示 | §4.7 |
| 7.2 | `POST /api/draft/update` API：保存用户编辑到 `alt_draft.edited_text` | §6.1 |
| 7.3 | 写回前二次确认弹窗：选中数量 + 图片类型分布 + `FILE_ALT usageCountPresent > 1` 共享文件全局影响提示 + `SHARED_NEUTRAL` 标识 | §4.7 |
| 7.4 | `POST /api/writeback/start` API：前置校验（锁 / scope / status / draft 存在 / 非 decorative）→ 获取 WRITEBACK 锁 → 创建 `writeback_batch` → 投递 `writeback` job | §4.8.1–4.8.2 |
| 7.5 | `writeback` BullMQ Job — Step A：二次读校验（线上当前 Alt 已非空则跳过） | §4.8.3 Step A |
| 7.6 | `writeback` Job — Step B：按 `alt_plane` 路由写回 mutation | §4.8.3 Step B |
| 7.7 | 写回 mutation 实现：`FILE_ALT → fileUpdate` / `COLLECTION_IMAGE_ALT → collectionUpdate` / `ARTICLE_IMAGE_ALT → articleUpdate` | §4.8.3 Step B |
| 7.8 | `writeback` Job — Step C 成功：`alt_draft.final_text` 固化 + 写 `audit_log` + candidate → `WRITTEN` + target `current_alt_text` 乐观更新 | §4.8.3 Step C |
| 7.9 | `writeback` Job — Step C 失败：candidate → `WRITEBACK_FAILED_RETRYABLE` + 记录 error | §4.8.3 Step C |
| 7.10 | WRITEBACK 锁管理 + SSE 写回进度 | §4.2.4 / §6.1 |
| 7.11 | 失败项前端展示 + 重试操作 | §4.8 |
| 7.12 | 写回完成汇总页面 | — |
| 7.13 | `GET /api/history` API：审计记录分页查询 | §6.1 |
| 7.14 | History 前端页面：审计记录列表（新旧 Alt / 图片类型 / 时间 / 模型） | §6.1 |

**依赖关系**
- 前置：Phase 6（`alt_draft` 数据、`GENERATED` / `WRITEBACK_FAILED_RETRYABLE` 状态的 candidate）
- 前置：Phase 4（候选列表 UI 框架）
- 外部：Shopify `fileUpdate` / `collectionUpdate` / `articleUpdate` mutation 可用

**技术方案**
- **写回路由**：`WritebackRouter` 服务类，接受 `alt_plane` 参数，返回对应的 Shopify mutation executor；每个 executor 封装 mutation 构造 + 错误解析 + `userErrors` 处理
- **`fileUpdate` 特殊处理**：使用 `MediaImage` GID（`gid://shopify/MediaImage/xxx`），不使用旧版 `ProductImage` GID（参见附录）；写回前检查文件 READY 状态
- **二次读校验**：写回 job 在执行 mutation 前，再次读取线上当前 Alt；已非空则标记 `RESOLVED`，不执行写回
- **`audit_log` 写入**：事务内完成 `alt_draft.final_text` 固化 + `audit_log` 插入 + `alt_candidate.status` 更新 + `alt_target` 乐观更新
- **并发控制**：`writeback` queue concurrency = 3~5，避免触发 Shopify API rate limit

**验收标准**
1. ✅ 选择 3 张 GENERATED 候选 → 编辑其中 1 张 → 全选写回 → 二次确认弹窗正确展示 → 写回成功 → candidate 全部 `WRITTEN`
2. ✅ `FILE_ALT` 写回使用 `fileUpdate`，Shopify 后台图片 Alt 已更新
3. ✅ `COLLECTION_IMAGE_ALT` 写回使用 `collectionUpdate`，集合封面 Alt 已更新
4. ✅ `ARTICLE_IMAGE_ALT` 写回使用 `articleUpdate`，文章封面 Alt 已更新
5. ✅ 写回前商家已手动补 Alt → 该条跳过，不执行 mutation
6. ✅ 写回失败 → candidate → `WRITEBACK_FAILED_RETRYABLE` → 前端展示失败项 → 可点击重试
7. ✅ `audit_log` 正确记录 `old_alt_text` / `new_alt_text` / `written_at` / `model_used`
8. ✅ History 页面可分页浏览审计记录（90 天内）
9. ✅ 共享文件（`usageCountPresent > 1`）的二次确认弹窗明确提示全局影响
10. ✅ WRITEBACK 锁期间，`POST /api/scan/start` 返回 409

**预估工时**：8 人天

---

### Phase 8：增量扫描与 Webhook 驱动

**阶段目标**
实现付费计划专属的 Webhook 驱动增量扫描：产品与集合资源变更时，通过 debounce/coalesce + 计划权限/scope/图片指纹四重 gate 过滤噪音，仅在图片实际变化时原子更新已发布结果并重建展示投影。

**功能范围**

| # | 功能项 | 对应架构 |
|---|--------|----------|
| 8.1 | `continuous_scan_debounce` BullMQ Job：按 `(shop_id, topic, resource_id)` 合并 30~90 秒窗口内事件 | §4.3.9 Debounce |
| 8.2 | Worker 消费四重 Gate 实现 | §4.3.9 Worker 消费 gate |
| 8.3 | Gate 1 — 互斥锁 gate：有 RUNNING 锁则 delayed 重试 | §4.3.9 gate 1 |
| 8.4 | Gate 2 — 计划权限 gate：Free → `SKIPPED_PLAN` | §4.3.9 gate 2 |
| 8.5 | Gate 3 — scope gate：资源类型不在 `scan_scope_flags` 内 → `SKIPPED_SCOPE` | §4.3.9 gate 3 |
| 8.6 | Gate 4 — 图片指纹 gate：计算指纹 → 与 `resource_image_fingerprint` 比对 → 相同则 `SKIPPED_NO_IMAGE_CHANGE` | §4.3.9 gate 4 |
| 8.7 | Product 图片指纹计算：按 `media_image_id + alt + url` 排序拼接后 `sha256` | §4.3.9 图片指纹规则 |
| 8.8 | Collection 图片指纹计算：`image.url + image.altText` 的 `sha256` | §4.3.9 图片指纹规则 |
| 8.9 | `continuous_scan_product` Job：读取单个 product 当前 media → 原子 upsert `FILE_ALT` targets + `image_usage(PRODUCT)` + `position_index` + 该 product usage sweep + 重算 target `present_status` + 重算 candidate + 重建 `candidate_group_projection` + 更新指纹 | §4.3.9 products |
| 8.10 | `continuous_scan_collection` Job：读取单个 collection 当前图片 → 原子 upsert/sweep `COLLECTION_IMAGE_ALT` + 重算 candidate + 重建投影 + 更新指纹 | §4.3.9 collections |
| 8.11 | 增量更新不触碰全量扫描的待发布结果 | §4.3.9 |
| 8.12 | `shops.incremental_scan_enabled` 随计划升降级联动更新 | §4.9.2 |
| 8.13 | `webhook_event` 状态更新（PROCESSED / COALESCED / SKIPPED_* / FAILED） | §5.17 |

**依赖关系**
- 前置：Phase 1（Webhook 接收与 `webhook_event` 落库）
- 前置：Phase 3（已发布结果数据结构、`image_usage` / `candidate_group_projection` 维护逻辑）
- 前置：Phase 5（计划权限判断 `current_plan`）
- 前置：Phase 2（`resource_image_fingerprint` 表、Scope 服务）

**技术方案**
- **Debounce**：使用 Redis Key `debounce:{shop_id}:{topic}:{resource_id}`，设置 TTL = 60 秒；首次事件立即创建 key 并投递 delayed job（delay = 60s）；窗口内后续事件更新 key 的 payload（取最新 webhook_id），标记旧事件 `COALESCED`；delayed job 到期后消费最终合并的任务
- **图片指纹**：product 指纹按 `[...mediaImages].sort(id).map(m => m.id + m.alt + m.imageUrl).join('|')` 后 `crypto.createHash('sha256')` 取 hex；collection 指纹按 `image.url + '|' + image.altText`
- **原子 Patch**：增量写入在单个 Prisma `$transaction` 中完成所有 upsert + sweep + candidate 重算 + projection 重建，保证前台读取一致性
- **锁延迟消费**：Gate 1 检测到锁存在时，使用 BullMQ `moveToDelayed` 将 job 延迟 30 秒重试，最多 20 次

**验收标准**
1. ✅ Free 店铺：`products/update` → `SKIPPED_PLAN` → 已发布结果不变
2. ✅ 付费店铺但 PRODUCT_MEDIA 不在 scope：`products/update` → `SKIPPED_SCOPE`
3. ✅ 付费店铺 30 秒内收到同一 product 3 个 update：仅执行 1 次增量，2 条标记 `COALESCED`
4. ✅ product 只改标题不改图片：`SKIPPED_NO_IMAGE_CHANGE`
5. ✅ product 新增 1 张媒体：指纹变化 → 增量刷新 → 新 `alt_target` + `image_usage` + `alt_candidate` 写入 → Dashboard 统计更新
6. ✅ product 删除 1 张媒体：对应 usage → `NOT_FOUND` → 若无其他 PRESENT usage 则 target → `NOT_FOUND`
7. ✅ collection 更换封面图：指纹变化 → `COLLECTION_IMAGE_ALT` target 更新 → candidate 状态重算
8. ✅ 有 SCAN 锁时 webhook 到达：job 延迟消费，锁释放后正常处理
9. ✅ 降级到 Free 后，`shops.incremental_scan_enabled = false`，后续 webhook 全部跳过

**预估工时**：7 人天

---

### Phase 9：设置、清理与运维收尾

**阶段目标**
完成 Settings 页面（含独立 scope 设置）、数据留存清理任务、GDPR 完整实现、锁超时回收、可观测性埋点。此阶段结束后，App 具备完整的运维自治能力。

**功能范围**

| # | 功能项 | 对应架构 |
|---|--------|----------|
| 9.1 | Settings 前端页面：scope 勾选（独立保存，不触发扫描）+ 当前计划展示 + 帮助链接 | §4.2.3 / §6.1 |
| 9.2 | `cleanup` BullMQ repeatable Job：草稿 30 天过期清理 / 审计 90 天过期清理 / staging & scan_result 7 天清理 / 失败 attempt 7 天清理 / webhook_event 已处理 7 天清理 | §4.10 |
| 9.3 | GDPR 完整实现：`CUSTOMERS_DATA_REQUEST`（无数据，返回空）/ `CUSTOMERS_REDACT`（无数据）/ `SHOP_REDACT`（删除 shop 全量数据） | §4.10 / §9 |
| 9.4 | `APP_UNINSTALLED` 完整实现：删除 shop 全量数据（所有关联表级联删除） | §4.10 |
| 9.5 | `gdpr_delete` BullMQ Job：异步执行大批量数据删除 | §7 |
| 9.6 | 锁超时回收 Job：检测 `heartbeat_at` 超过 30 分钟无更新的锁，标记 `EXPIRED` 并释放 | §4.2.5 |
| 9.7 | 结构化日志完善：补全 §10 建议的所有字段（`shop_domain` / `batch_id` / `alt_plane` / `model_used` / `context_mode` / `duration_ms` / `error_code`） | §10 |
| 9.8 | 关键指标埋点：扫描行数、attempt 次数、增量 skip 分布、生成 skip/success/fail 分布、写回成功率、reservation 统计 | §10 |
| 9.9 | 前端"帮助与反馈"链接入口（导航栏/页脚） | §11 产品意图书 |
| 9.10 | `alt_draft.expires_at` 自动设置（created_at + 30 天）+ 查询时过滤过期 draft | §4.10 |

**依赖关系**
- 前置：Phase 1~8 全部完成（本阶段为收尾打磨）
- 外部：无新外部依赖

**技术方案**
- **数据删除**：`APP_UNINSTALLED` 触发 `gdpr_delete` job，按表依赖顺序（先子表后主表）批量 DELETE，每批 1000 行，避免长事务锁表
- **`SHOP_REDACT`**：与 `APP_UNINSTALLED` 共用删除逻辑，增加幂等检查（已删除则跳过）
- **Cleanup Job**：BullMQ repeatable job 每日 02:00 UTC 执行一次；每类清理独立 SQL，WHERE `expires_at < NOW()` 或 `created_at < NOW() - interval`
- **日志**：使用 `pino` 结构化日志库；在所有 worker job 入口/出口统一注入上下文字段；Railway 自带日志聚合
- **指标**：MVP 阶段将关键指标写入结构化日志（JSON），后续可对接 Datadog / Grafana

**验收标准**
1. ✅ Settings 页面修改 scope 后，Dashboard 立即反映新的 `effective_read_scope_flags`
2. ✅ 创建超过 30 天的 draft 被 cleanup job 清除
3. ✅ 创建超过 90 天的 audit_log 被 cleanup job 清除
4. ✅ 卸载 App → `APP_UNINSTALLED` → 该 shop 全部数据删除
5. ✅ `SHOP_REDACT` Webhook → 该 shop 数据完整清除
6. ✅ 模拟锁超时（30 分钟无 heartbeat）→ 锁被自动回收 → 后续操作可正常获取锁
7. ✅ 结构化日志在 Railway 中可按 `shop_domain` / `batch_id` 检索

**预估工时**：6 人天

---

### Phase 10：集成测试与上线准备

**阶段目标**
执行全面的集成测试（覆盖架构文档 §11 所列全部测试场景）、修复发现的问题、准备 Shopify App Store 提审材料与最小 FAQ 文档。此阶段结束后，App 可提交 Shopify App Review。

**功能范围**

| # | 功能项 | 对应架构 |
|---|--------|----------|
| 10.1 | Bulk 解析专项测试（§11.1）：4 类样本、`__parentId`、去重、`position_index` | §11.1 |
| 10.2 | Bulk URL 过期与 Attempt 隔离测试（§11.2） | §11.2 |
| 10.3 | 已发布结果与原子发布测试（§11.3） | §11.3 |
| 10.4 | Partial Success 与 sweep 保护测试（§11.4） | §11.4 |
| 10.5 | 首次扫描说明确认测试（§11.5） | §11.5 |
| 10.6 | scope_flags 闭环测试（§11.6）：含重新勾回恢复显示 | §11.6 |
| 10.7 | 互斥锁测试（§11.7） | §11.7 |
| 10.8 | 装饰性图片测试（§11.8） | §11.8 |
| 10.9 | 增量扫描全场景测试（§11.9）：plan/scope/debounce/fingerprint | §11.9 |
| 10.10 | `image_usage` sweep 测试（§11.10） | §11.10 |
| 10.11 | 展示投影与图片位置测试（§11.11） | §11.11 |
| 10.12 | 共享文件上下文策略测试（§11.12） | §11.12 |
| 10.13 | Candidate 可重试状态测试（§11.13） | §11.13 |
| 10.14 | 生成前 Shopify 真值复核测试（§11.14） | §11.14 |
| 10.15 | 计费与额度全场景测试（§11.15）：含年付一次性发放、Free 月切、降级补发 | §11.15 |
| 10.16 | 图片类型覆盖测试（§11.16） | §11.16 |
| 10.17 | 端到端全链路 Smoke Test：安装 → 首次扫描 → 生成 → 审阅编辑 → 写回 → 验证 Shopify 后台 Alt 已更新 | — |
| 10.18 | App Store 提审材料：App listing 文案、截图、隐私政策、支持邮箱 | 产品意图书 §11 |
| 10.19 | 最小 FAQ 页面：安装说明、扫描流程、配额计费、增量扫描、装饰性标记、产品边界声明 | 产品意图书 §11 |
| 10.20 | 性能与稳定性验证：500~800 张图片店铺全量扫描 + 生成 + 写回端到端耗时 | 产品意图书 §2 |
| 10.21 | Bug 修复与 UI 打磨 | — |

**依赖关系**
- 前置：Phase 1~9 全部完成
- 外部：Shopify 开发店铺（至少 2 个，含不同图片数量级）

**技术方案**
- **测试策略**：
  - **单元测试**：核心服务（scope 计算、额度消费顺序、指纹计算、Prompt 构建、上下文模式判定）使用 Vitest
  - **集成测试**：Bulk 解析使用固化的脱敏 NDJSON 文件；计费逻辑使用 PostgreSQL test database
  - **端到端测试**：在 Shopify 开发店铺上手动执行完整流程；自动化测试使用 Playwright（可选）
- **样本准备**：创建至少 2 个开发店铺：
  - 店铺 A：50 张产品图片 + 20 张 Files + 5 个集合 + 10 篇文章（含共享文件场景）
  - 店铺 B：500+ 张图片（大批量性能验证）
- **提审材料**：按 Shopify App Review Checklist 逐项核对

**验收标准**
1. ✅ §11.1–§11.16 全部测试场景通过
2. ✅ 端到端 Smoke Test 通过：从安装到 Shopify 后台 Alt 更新全链路无阻断
3. ✅ 500 张图片店铺：全量扫描 < 5 分钟、50 张生成 < 3 分钟、50 张写回 < 2 分钟
4. ✅ 写回成功率 > 99%
5. ✅ 无静默失败：所有失败项在前端可见且可重试
6. ✅ App Store 提审材料完整：listing 文案 + 截图 + 隐私政策 + 支持邮箱 + FAQ
7. ✅ FAQ 覆盖产品意图书 §11 要求的全部主题
8. ✅ 0 个 P0 bug 遗留

**预估工时**：7 人天

---

## 4. 阶段依赖关系图

```
Phase 1 ─── Infrastructure & App Shell
   │
   ▼
Phase 2 ─── Data Model & Core Services
   │
   ├──────────────────────┐
   ▼                      ▼
Phase 3 ── Full Scan    Phase 5 ── Billing
   │                      │
   ▼                      │
Phase 4 ── Dashboard     │
   │                      │
   ├──────────────────────┘
   ▼
Phase 6 ─── AI Generation
   │
   ▼
Phase 7 ─── Review & Writeback
   │
   ▼
Phase 8 ─── Incremental Scan (depends on Phase 3 + 5)
   │
   ▼
Phase 9 ─── Settings, Cleanup & Observability
   │
   ▼
Phase 10 ── Integration Testing & Launch
```

> **可并行窗口**：Phase 3（全量扫描）与 Phase 5（计费）可由不同工程师并行开发，在 Phase 6 开始前合流。Phase 4（Dashboard）依赖 Phase 3 产出，但 UI 框架可提前搭建。

---

## 5. 里程碑检查点

| 里程碑 | 对应阶段 | 产出 | 预期时间点 |
|:---|:---|:---|:---|
| **M1：App 可安装** | Phase 1 完成 | 开发店铺可安装、Polaris 空壳加载 | 第 2 周末 |
| **M2：Schema 就绪** | Phase 2 完成 | 26 张表迁移完成、Bootstrap API 可用 | 第 3 周末 |
| **M3：扫描可用** | Phase 3 完成 | 全量扫描端到端跑通，已发布结果可查询 | 第 5 周末 |
| **M4：Dashboard 可用** | Phase 4 完成 | 仪表盘四组统计 + 候选列表 + 装饰性标记 | 第 7 周末 |
| **M5：计费上线** | Phase 5 完成 | 五档计划 + 额度系统 + 订阅流程 | 第 8 周末 |
| **M6：核心闭环跑通** | Phase 7 完成 | 扫描 → 生成 → 审阅 → 写回全链路 | 第 11 周末 |
| **M7：增量扫描可用** | Phase 8 完成 | Webhook 驱动增量，付费专属 | 第 12 周末 |
| **M8：可提交审核** | Phase 10 完成 | 全部测试通过 + 提审材料完整 | 第 14 周末 |

---

## 6. 技术风险与缓解措施速查

| 风险 | 影响阶段 | 缓解措施 |
|:---|:---|:---|
| Shopify Bulk 输出结构变化 | Phase 3 | 固化脱敏样本 + 回归测试；解析器对未知字段容错 |
| Bulk URL 过期导致解析失败 | Phase 3 | attempt 级重跑，不回滚整 job |
| Shopify API Rate Limit | Phase 6/7/8 | BullMQ concurrency 限制 + 内置 rate limiter + 指数退避 |
| AI 服务不可用 | Phase 6 | AI Gateway 主/降级双模型 + 超时 30s + 重试 2 次 |
| 共享文件误写产品专属描述 | Phase 6 | `context_mode` 强制分级 + `SHARED_NEUTRAL` Prompt 约束 |
| 年付额度口径不一致 | Phase 5 | 独立 `ANNUAL_INCLUDED` bucket + 账本交叉验证 |
| Free 月配额重复发放 | Phase 5 | `unique(shop_id, bucket_type, cycle_key)` 数据库约束 |
| 前台新旧数据混杂 | Phase 3 | 待发布结果 + 原子发布 + 前台始终读已发布数据 |
| 大店铺 publish 事务过大 | Phase 3 | 按资源类型切片事务；必要时引入批量操作 |
| scope 改动后展示不一致 | Phase 4/8 | `effective_read_scope_flags` 实时计算 + 三层 scope 闭环 |






# Phase 1 任务拆解

---

**1. 在 Shopify Partner Dashboard 创建 App**
- 类型：**人工任务**
- 目标：在 Shopify Partner Dashboard 中创建一个新的 App，获取 `SHOPIFY_API_KEY` 和 `SHOPIFY_API_SECRET`
- 产出物：App 的 API Key、API Secret；App URL 与重定向 URL 的初始配置
- 依赖关系：需要 Shopify Partner 账号（已注册并登录）
- 备注：涉及浏览器登录 Partner Dashboard、填写 App 信息、选择 App 类型（Custom/Public）。AI 无法操作真实浏览器 GUI。

---

**2. 创建 Shopify 开发店铺**
- 类型：**人工任务**
- 目标：创建一个用于开发与测试的 Shopify Development Store
- 产出物：可用的开发店铺域名（如 `alttext-dev.myshopify.com`）
- 依赖关系：Shopify Partner 账号
- 备注：需在 Partner Dashboard 手动创建，涉及账号操作与店铺配置选择。

---

**3. 注册 Railway 账号并创建 Project**
- 类型：**人工任务**
- 目标：在 Railway 平台创建项目，并 Provision PostgreSQL 和 Redis 两个内置插件
- 产出物：Railway Project 创建完成；获取 `DATABASE_URL`、`REDIS_URL`；确定 `web` 和 `worker` 两个 Service 的占位
- 依赖关系：Railway 账号（需注册/登录，可能涉及绑定 GitHub 或支付方式）
- 备注：涉及平台注册、付费计划选择、插件 Provision 等 GUI 操作。AI 无法完成。

---

**4. 生成环境变量清单与 `.env.example` 文件**
- 类型：**AI任务**
- 目标：根据 Phase 1 需要的所有配置项，生成完整的环境变量清单文档和 `.env.example` 模板文件
- 产出物：`.env.example` 文件，包含所有变量名、说明注释、示例值（如 `SHOPIFY_API_KEY=`、`SHOPIFY_API_SECRET=`、`DATABASE_URL=`、`REDIS_URL=`、`TOKEN_ENCRYPTION_KEY=`、`NODE_ENV=` 等）
- 依赖关系：无
- 备注：AI 生成模板，真实值由人工在后续任务中填入。

---

**5. 使用 Shopify CLI 初始化项目脚手架**
- 类型：**AI任务**
- 目标：基于 `@shopify/shopify-app-react-router` 模板生成完整的项目目录结构，包含 React Router + TypeScript 基础配置
- 产出物：完整的项目目录结构；`package.json`（含所有依赖：`@shopify/shopify-app-react-router`、`@shopify/polaris-types`、`@shopify/app-bridge-react`、`prisma`、`bullmq`、`ioredis`、`pino` 等）；`tsconfig.json`；`.gitignore`
- 依赖关系：任务 4 完成（`.env.example` 可用）
- 备注：AI 输出完整的初始化命令序列和/或直接生成全部文件内容。如使用 `shopify app init` 命令，AI 可给出精确的交互选项指引。

---

**6. 设计并编写 Prisma Schema（Phase 1 三表）**
- 类型：**AI任务**
- 目标：编写 `schema.prisma`，定义 `shops`、`sessions`、`webhook_event` 三张表的完整字段、索引与关系
- 产出物：`prisma/schema.prisma` 文件，包含：
  - `shops` 表：`id`, `shop_domain`(unique), `access_token_encrypted`, `installed_at`, `uninstalled_at`, `current_plan`(default FREE), `scan_scope_flags`, `created_at`, `updated_at`
  - `sessions` 表：兼容 `@shopify/shopify-app-session-storage-prisma` 所需字段
  - `webhook_event` 表：`id`, `shop_domain`, `topic`, `webhook_id`(unique, 幂等键), `payload`(JSON), `status`(enum: RECEIVED/PROCESSING/DONE/FAILED), `received_at`, `processed_at`
- 依赖关系：任务 5 完成（项目结构已存在）
- 备注：AI 需确保 `sessions` 表结构与 Shopify 官方 Prisma session storage adapter 兼容。

---

**7. 编写 Prisma 迁移脚本与数据库初始化配置**
- 类型：**AI任务**
- 目标：生成 Prisma 迁移命令和初始化脚本，确保在 Railway PostgreSQL 上可执行
- 产出物：迁移命令文档（`npx prisma migrate dev --name init`）；`prisma/seed.ts`（如需种子数据）；`package.json` 中增加 `prisma:migrate` 和 `prisma:generate` 脚本
- 依赖关系：任务 6 完成
- 备注：无

---

**8. 实现 Token 加密/解密工具模块**
- 类型：**AI任务**
- 目标：编写 AES-256-GCM 加密/解密工具函数，用于 Offline Access Token 的安全存储与读取
- 产出物：`app/utils/encryption.server.ts`，导出 `encrypt(plaintext: string): string` 和 `decrypt(ciphertext: string): string`；从 `TOKEN_ENCRYPTION_KEY` 环境变量读取密钥；包含单元测试文件 `app/utils/encryption.server.test.ts`
- 依赖关系：任务 5 完成（项目结构存在）
- 备注：AI 需确保 IV 随机生成、AuthTag 附带、输出格式为 `iv:authTag:ciphertext` 的 Base64 编码。

---

**9. 实现 Shopify OAuth 安装流程与 Session 持久化**
- 类型：**AI任务**
- 目标：配置 `@shopify/shopify-app-react-router` 的 OAuth 流程，完成安装回调中的 Offline Access Token 获取、加密存储与 Session 持久化
- 产出物：
  - `app/shopify.server.ts`：Shopify App 实例配置（apiKey、apiSecretKey、scopes、hostName、sessionStorage 指向 Prisma adapter）
  - 安装回调 hook（`afterAuth`）：将 `shop_domain`、`access_token_encrypted`、`installed_at` 写入 `shops` 表（upsert 逻辑，支持重装场景清除 `uninstalled_at`）
  - Session storage 配置使用 `@shopify/shopify-app-session-storage-prisma`
- 依赖关系：任务 6（Schema 完成）、任务 8（加密模块完成）
- 备注：AI 编写全部代码。OAuth 的实际触发验证在后续人工任务中进行。

---

**10. 实现 Webhook 注册配置**
- 类型：**AI任务**
- 目标：在 Shopify App 配置中声明需要注册的 Webhook topics，确保安装时自动注册
- 产出物：在 `app/shopify.server.ts` 或 `shopify.app.toml` 中配置 Webhook 订阅列表：`APP_UNINSTALLED`、`CUSTOMERS_DATA_REQUEST`、`CUSTOMERS_REDACT`、`SHOP_REDACT`、`BULK_OPERATIONS_FINISH`、`PRODUCTS_CREATE`、`PRODUCTS_UPDATE`、`PRODUCTS_DELETE`、`COLLECTIONS_CREATE`、`COLLECTIONS_UPDATE`、`COLLECTIONS_DELETE`
- 依赖关系：任务 9 完成
- 备注：使用 `shopify.app.toml` 声明式注册或 `shopifyApp()` 配置中的 `webhooks` 字段。

---

**11. 实现 Webhook Receiver 路由（HMAC 校验 + 幂等落库 + BullMQ 投递）**
- 类型：**AI任务**
- 目标：编写统一的 Webhook 接收端点，完成 HMAC 签名校验、`webhook_event` 幂等落库、快速返回 200、异步投递到 BullMQ
- 产出物：
  - `app/routes/webhooks.tsx`（或 `app/routes/api.webhooks.ts`）：React Router action handler
  - HMAC 校验逻辑（使用 `@shopify/shopify-api` 内置验证或手动实现）
  - 幂等检查：基于 `X-Shopify-Webhook-Id` 去重
  - `webhook_event` 表落库（status=RECEIVED）
  - BullMQ `webhookQueue.add()` 投递
  - 单元测试：模拟合法/非法签名、重复投递场景
- 依赖关系：任务 6（Schema）、任务 13（BullMQ 初始化）
- 备注：无

---

**12. 实现 `APP_UNINSTALLED` Handler**
- 类型：**AI任务**
- 目标：编写 `APP_UNINSTALLED` Webhook 的业务处理逻辑
- 产出物：
  - `app/jobs/handlers/appUninstalled.server.ts`：接收 `webhook_event` 记录，将对应 `shops` 表的 `uninstalled_at` 设为当前时间戳
  - 更新 `webhook_event.status` 为 DONE
  - 单元测试文件
- 依赖关系：任务 11 完成
- 备注：Phase 9 会补全完整的数据清理逻辑，此阶段仅标记 `uninstalled_at`。

---

**13. 实现 GDPR Webhook Handler（占位）**
- 类型：**AI任务**
- 目标：为三个 GDPR Webhook（`CUSTOMERS_DATA_REQUEST`、`CUSTOMERS_REDACT`、`SHOP_REDACT`）编写占位处理器
- 产出物：
  - `app/jobs/handlers/gdpr.server.ts`：三个函数，均落库 `webhook_event`（status=DONE）后返回
  - 路由级别确保返回 200
- 依赖关系：任务 11 完成
- 备注：占位实现，Phase 9 补全真实逻辑。

---

**14. 初始化 BullMQ 连接与 Queue/Worker 配置**
- 类型：**AI任务**
- 目标：建立 BullMQ 基础设施，创建队列实例与 Worker 空循环
- 产出物：
  - `app/queue/connection.server.ts`：IORedis 连接实例，从 `REDIS_URL` 环境变量读取
  - `app/queue/queues.server.ts`：定义 `webhookQueue` 队列
  - `worker/index.ts`：BullMQ Worker 入口，监听 `webhookQueue`，根据 topic 分发到对应 handler（`APP_UNINSTALLED` / GDPR），未识别 topic 日志警告并标记 DONE
  - `worker/tsconfig.json`（如 worker 独立编译）
- 依赖关系：任务 5（项目结构）
- 备注：此阶段 worker 仅处理 Webhook 相关 job，其余 queue 在后续阶段添加。

---

**15. 实现结构化日志模块**
- 类型：**AI任务**
- 目标：配置基于 `pino` 的结构化日志系统，统一 web 和 worker 的日志格式
- 产出物：
  - `app/utils/logger.server.ts`：pino 实例，支持 JSON 格式输出、log level 由 `LOG_LEVEL` 环境变量控制
  - 在 `shopify.server.ts`、Webhook receiver、Worker 入口等关键位置插入日志调用
- 依赖关系：任务 5 完成
- 备注：无

---

**16. 编写 App Bridge + Polaris 空壳页面与导航结构**
- 类型：**AI任务**
- 目标：创建 Embedded App 的前端页面骨架，包含 App Bridge 初始化和 Polaris 导航占位
- 产出物：
  - `app/root.tsx`：AppProvider（Polaris）+ App Bridge Provider 初始化
  - `app/routes/app.tsx`：嵌套布局，包含 `NavigationMenu`（Polaris `Frame` + `Navigation`），设置导航项：Dashboard / Review / History / Billing / Settings / Help
  - `app/routes/app._index.tsx`：Dashboard 占位页（显示 "Welcome to AltText Magic" 和空白 Polaris `Page`）
  - `app/routes/app.review.tsx`：Review 占位页
  - `app/routes/app.history.tsx`：History 占位页
  - `app/routes/app.billing.tsx`：Billing 占位页
  - `app/routes/app.settings.tsx`：Settings 占位页
  - `app/routes/app.help.tsx`：Help 占位页
- 依赖关系：任务 5（项目结构）、任务 9（shopify.server.ts 存在）
- 备注：所有页面此阶段为占位 UI，后续阶段逐步填充。

---

**17. 编写 Railway 部署配置文件**
- 类型：**AI任务**
- 目标：生成 Railway 部署所需的全部配置文件，使 `web` 和 `worker` 可分别部署
- 产出物：
  - `railway.toml` 或 `railway.json`：定义 `web` service（启动命令 `npm run start`）和 `worker` service（启动命令 `npm run worker`）
  - `Procfile`（如 Railway 需要）
  - `package.json` 中添加 `start`（React Router production server）、`worker`（运行 `worker/index.ts` 编译后入口）、`build`（含 Prisma generate + React Router build + Worker tsc build）、`postinstall`（Prisma generate）等脚本
  - `Dockerfile`（可选，若 Railway 使用 Nixpacks 则提供 `nixpacks.toml`）
  - 部署文档：说明如何在 Railway Dashboard 中创建两个 Service 并分别绑定到同一个 repo 的不同启动命令
- 依赖关系：任务 5、14 完成
- 备注：AI 生成配置文件和文档，实际在 Railway 平台操作由人工完成。

---

**18. 生成 AES-256-GCM 加密密钥**
- 类型：**人工任务**
- 目标：生成一个安全的 32 字节随机加密密钥，用于 Access Token 加密
- 产出物：一个 Base64 或 Hex 编码的 256-bit 随机密钥字符串
- 依赖关系：无
- 备注：AI 可提供生成命令（如 `openssl rand -hex 32`），但密钥的实际生成与安全保管必须由人工完成，不可在聊天记录或代码中暴露。

---

**19. 在 Railway 平台配置 Service 与环境变量**
- 类型：**人工任务**
- 目标：在 Railway Dashboard 中创建 `web` 和 `worker` 两个 Service，配置 GitHub 仓库关联、环境变量注入
- 产出物：Railway 上 `web` / `worker` / `postgres` / `redis` 四个 Service 配置完成；所有环境变量（`SHOPIFY_API_KEY`、`SHOPIFY_API_SECRET`、`DATABASE_URL`、`REDIS_URL`、`TOKEN_ENCRYPTION_KEY`、`SCOPES`、`HOST` 等）已注入
- 依赖关系：任务 1（API Key/Secret）、任务 3（Railway Project/插件已创建）、任务 17（配置文件就绪）、任务 18（加密密钥已生成）
- 备注：涉及平台 GUI 操作、凭证粘贴、域名绑定。AI 无法完成。

---

**20. 将 Shopify App URL 和重定向 URL 更新为 Railway 域名**
- 类型：**人工任务**
- 目标：在 Shopify Partner Dashboard 中将 App URL 和 OAuth Redirect URL 更新为 Railway 部署后的实际域名
- 产出物：Partner Dashboard 中 App 配置的 URL 已更新，与 Railway `web` service 域名一致
- 依赖关系：任务 19 完成（Railway Service 已部署，域名可用）
- 备注：涉及 Partner Dashboard GUI 操作。

---

**21. 首次部署到 Railway 并验证服务健康**
- 类型：**人工任务**
- 目标：触发首次部署，确认 `web` / `worker` / `postgres` / `redis` 四个 Service 全部 healthy
- 产出物：Railway Dashboard 显示所有 Service 状态为 Active/Healthy；数据库迁移已执行成功；`web` 服务可通过公网域名访问
- 依赖关系：任务 19、20 完成
- 备注：需人工在 Railway 触发部署、查看日志、排查首次部署问题。

---

**22. 在开发店铺安装 App 并验证 OAuth 流程**
- 类型：**人工任务**
- 目标：在 Shopify 开发店铺中安装 App，走完 OAuth 授权流程，验证 Embedded iframe 加载
- 产出物：
  - App 安装成功，Shopify Admin 内可看到 Embedded iframe 中的 Polaris 空壳页面
  - `shops` 表中写入正确记录：`shop_domain`、`access_token_encrypted`（可解密还原）、`installed_at`、`current_plan=FREE`
- 依赖关系：任务 2（开发店铺存在）、任务 21（服务已部署）
- 备注：涉及浏览器操作、OAuth 授权确认。

---

**23. 验证 Webhook 接收与处理**
- 类型：**人工任务**
- 目标：在开发店铺卸载 App，验证 `APP_UNINSTALLED` Webhook 被正确接收并处理；手动触发 GDPR Webhook 测试
- 产出物：
  - `webhook_event` 表中有 `APP_UNINSTALLED` 记录，status=DONE
  - `shops` 表中对应记录的 `uninstalled_at` 已被设置
  - GDPR Webhook 端点返回 200（可通过 Shopify Partner Dashboard 的 Webhook 测试功能或 curl 验证）
  - Railway 日志中可见结构化日志输出
- 依赖关系：任务 22 完成（App 已安装）
- 备注：需人工在 Shopify Admin 执行卸载操作，并在 Railway 日志和数据库中检查结果。

---

**24. 编写 Phase 1 验收检查清单文档**
- 类型：**AI任务**
- 目标：基于 Phase 1 的 6 条验收标准，生成可执行的验收检查清单和操作步骤文档
- 产出物：`docs/phase1-acceptance-checklist.md`，包含每条验收标准的具体操作步骤、预期结果、实际结果填写栏
- 依赖关系：无（可与其他任务并行）
- 备注：无

---

**25. 最终验收确认**
- 类型：**人工任务**
- 目标：按照验收检查清单逐项确认所有 6 条验收标准全部通过
- 产出物：Phase 1 验收报告，标注每条验收标准的通过状态
- 依赖关系：任务 22、23 完成
- 备注：最终验收需人工在真实环境中执行并确认。

---


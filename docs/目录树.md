alttext-magic/
│
│  ══════════════════════════════════════════════════════════
│  项目根配置
│  ══════════════════════════════════════════════════════════
│
├── package.json
├── shopify.app.toml                   # Shopify CLI 应用声明（client_id / scopes / webhooks）
├── vite.config.ts                     # remix.config.js → Vite + @react-router/dev + tsconfigPaths
├── react-router.config.ts             # React Router v7 路由配置（ssr: true）
├── tsconfig.json
├── env.d.ts                           # Vite 环境变量类型声明
├── .env.example                       # 环境变量模板
├── .eslintrc.cjs
├── .prettierrc
├── .gitignore
├── docker-compose.yml                 # 本地开发：postgres + redis
├── Dockerfile                         # 生产构建
├── Procfile                           # Railway 多进程：web + worker
├── railway.toml                       # Railway 部署配置
├── README.md
│
│  ══════════════════════════════════════════════════════════
│  数据库
│  ══════════════════════════════════════════════════════════
│
├── prisma/
│   ├── schema.prisma                     # 全部 26+ 张表定义
│   ├── migrations/                       # Prisma 自动迁移
│   │   └── ...
│   └── seed.ts                           # 开发/演示数据种子
│
│  ══════════════════════════════════════════════════════════
│  Shopify CLI 扩展（如暂无可留空目录）
│  ══════════════════════════════════════════════════════════
│
├── extensions/                        # App extensions 目录（Theme / UI / Function 等）
│   └── .gitkeep
│
│  ══════════════════════════════════════════════════════════
│  React Router 应用（Web / API / Webhook）
│  ══════════════════════════════════════════════════════════
│
├── app/
│   ├── entry.server.tsx
│   ├── root.tsx
│   ├── shopify.server.ts              # Shopify App 配置（OAuth / App Bridge / Session / Billing）
│   │
│   │  ┌─────────────────────────────────┐
│   │  │ 路由（页面 + API + Webhook）     │
│   │  └─────────────────────────────────┘
│   │
│   ├── routes/
│   │   │
│   │   │  ── 根索引（重定向到 /app）────
│   │   │
│   │   ├── _index.tsx                            # 根 / → redirect("/app")
│   │   │
│   │   │  ── App 布局路由 ────
│   │   │
│   │   ├── app.tsx                               # 布局壳（AppProvider + NavMenu + <Outlet/>）
│   │   │
│   │   │  ── 前端页面路由（嵌套在 app.tsx 下）────
│   │   │
│   │   ├── app._index.tsx                        # 仪表盘（Dashboard）
│   │   ├── app.onboarding.tsx                    # 首次扫描说明确认 & scope 选择
│   │   ├── app.review.tsx                        # 候选审阅 / 编辑 / 装饰性标记
│   │   ├── app.history.tsx                       # 写回审计历史
│   │   ├── app.billing.tsx                       # 计费管理（计划 / 超额包 / 额度详情）
│   │   ├── app.settings.tsx                      # 设置（scope / 通知版本）
│   │   │
│   │   │  ── REST API 路由（全部 .tsx）────
│   │   │
│   │   ├── api.bootstrap.tsx                     # GET 初始化引导数据
│   │   ├── api.dashboard.tsx                     # GET 分组统计
│   │   ├── api.settings.scope.tsx                # POST 独立 scope 设置
│   │   ├── api.scan.start.tsx                    # POST 启动全量扫描
│   │   ├── api.scan.status.tsx                   # GET  扫描进度
│   │   ├── api.candidates.tsx                    # GET  候选列表（按 group + status）
│   │   ├── api.candidates.\$id.usages.tsx         # GET  单条 candidate 的 usage 详情
│   │   ├── api.decorative.mark.tsx               # POST 标记装饰性
│   │   ├── api.decorative.unmark.tsx             # POST 取消装饰性
│   │   ├── api.generation.preflight.tsx          # POST 生成预检（额度足够？）
│   │   ├── api.generation.start.tsx              # POST 启动生成批次
│   │   ├── api.draft.update.tsx                  # POST 编辑草稿
│   │   ├── api.writeback.start.tsx               # POST 启动写回批次
│   │   ├── api.history.tsx                       # GET  审计记录
│   │   ├── api.billing.summary.tsx               # GET  计费摘要
│   │   ├── api.billing.change-plan.tsx           # POST 变更计划
│   │   ├── api.billing.purchase-pack.tsx         # POST 购买超额包
│   │   ├── api.sse.tsx                           # GET  SSE 实时进度推送
│   │   │
│   │   │  ── Webhook 路由（全部 .tsx）────
│   │   │
│   │   ├── webhooks.app.uninstalled.tsx          # 
│   │   ├── webhooks.app.gdpr.\$.tsx               # GDPR 三类 catch-all
│   │   ├── webhooks.bulk-operations-finish.tsx   # 
│   │   ├── webhooks.products.tsx                 # products/create + products/update
│   │   ├── webhooks.collections.tsx              # collections/create + collections/update
│   │   ├── webhooks.billing.tsx                  # 订阅 / 一次性购买回调
│   │   │
│   │   │  ── OAuth 路由 ────
│   │   │
│   │   ├── auth.login/                           # 登录页（目录路由）
│   │   │   ├── route.tsx                             #   路由处理（loader → 登录引导）
│   │   │   ├── login.tsx                             #   登录表单组件
│   │   │   └── error.server.tsx                      #   OAuth 错误处理
│   │   │
│   │   └── auth.\$.tsx                            # OAuth catch-all 回调
│   │
│   │  ┌─────────────────────────────────┐
│   │  │ React 组件（Polaris）            │
│   │  └─────────────────────────────────┘
│   │
│   ├── components/
│   │   │
│   │   ├── dashboard/
│   │   │   ├── GroupStatsCard.tsx                    # 单组统计卡片（total / hasAlt / missing / decorative）
│   │   │   ├── ScanStatusBanner.tsx                  # 扫描进行中横幅
│   │   │   ├── QuotaSummary.tsx                      # 额度摘要条
│   │   │   └── ScopeIndicator.tsx                    # 当前有效 scope 指示器
│   │   │
│   │   ├── review/
│   │   │   ├── CandidateList.tsx                    # 候选列表容器
│   │   │   ├── CandidateRow.tsx                     # 单行：缩略图 + 标题 + 位置 + 状态 + 操作
│   │   │   ├── DraftEditor.tsx                      # 可编辑输入框（草稿 / AI 生成文本）
│   │   │   ├── UsageDetailPopover.tsx               # 共享文件影响范围弹窗
│   │   │   ├── WritebackConfirmModal.tsx            # 写回前二次确认
│   │   │   ├── DecorativeToggle.tsx                 # 装饰性标记开关
│   │   │   ├── ContextModeBadge.tsx                 # RESOURCE_SPECIFIC / SHARED_NEUTRAL 标识
│   │   │   └── GroupFilter.tsx                      # 图片类型筛选
│   │   │
│   │   ├── onboarding/
│   │   │   ├── ScanNotice.tsx                       # 扫描说明（范围/用途/留存/AI 边界）
│   │   │   └── ScopeSelector.tsx                    # 四类资源勾选
│   │   │
│   │   ├── billing/
│   │   │   ├── PlanCard.tsx                         # 计划卡片（FREE→MAX）
│   │   │   ├── QuotaBreakdown.tsx                   # included / welcome / overage 明细
│   │   │   ├── OveragePackCard.tsx                   # 超额包购买入口
│   │   │   └── BillingIntervalToggle.tsx            # 月付/年付切换
│   │   │
│   │   ├── settings/
│   │   │   └── ScopeSettings.tsx                    # scope 独立设置面板
│   │   │
│   │   └── common/
│   │       ├── ProgressBar.tsx
│   │       ├── StatusBadge.tsx                      # candidate 状态标签
│   │       └── ImpactScopeSummary.tsx               # 影响范围摘要组件
│   │
│   │  ┌─────────────────────────────────┐
│   │  │ React Hooks                      │
│   │  └─────────────────────────────────┘
│   │
│   ├── hooks/
│   │   ├── useSSE.ts                                # SSE 连接与事件消费
│   │   ├── useScanStatus.ts                         # 扫描状态轮询
│   │   ├── useBatchProgress.ts                      # 生成/写回批次进度
│   │   └── useQuota.ts                              # 额度数据缓存
│   │
│   │  ┌─────────────────────────────────┐
│   │  │ 前端工具                         │
│   │  └─────────────────────────────────┘
│   │
│   └── lib/
│       ├── scope-utils.ts                           # effectiveReadScopeFlags 前端计算
│       └── format.ts                                # 数字/日期格式化
│
│  ══════════════════════════════════════════════════════════
│  服务端共享内核（Web 路由 + Worker 共用）
│  ══════════════════════════════════════════════════════════
│
├── server/
│   │
│   │  ┌─────────────────────────────────┐
│   │  │ 配置                             │
│   │  └─────────────────────────────────┘
│   │
│   ├── config/
│   │   ├── index.ts                                 # 环境变量读取与校验
│   │   ├── plans.ts                                 # 五档计划定义（价格 / 月配额 / 年配额 / 超额包 / 欢迎额度）
│   │   ├── queue-names.ts                           # 所有 BullMQ 队列名常量
│   │   └── constants.ts                             # 枚举（AltPlane / ResourceType / CandidateStatus / ContextMode …）
│   │
│   │  ┌─────────────────────────────────┐
│   │  │ 数据库                           │
│   │  └─────────────────────────────────┘
│   │
│   ├── db/
│   │   └── prisma.server.ts                         # Prisma Client 单例
│   │
│   │  ┌─────────────────────────────────┐
│   │  │ 业务模块                         │
│   │  └─────────────────────────────────┘
│   │
│   ├── modules/
│   │   │
│   │   ├── shop/
│   │   │   ├── shop.service.ts                      # 店铺 CRUD、安装初始化、卸载清理
│   │   │   ├── scope.service.ts                     # scan_scope_flags / last_published_scope_flags / effective 计算
│   │   │   └── shop.types.ts
│   │   │
│   │   ├── auth/
│   │   │   ├── session.service.ts                   # Offline token 加解密与持久化
│   │   │   └── webhook-hmac.ts                      # HMAC 校验工具
│   │   │
│   │   ├── notice/
│   │   │   ├── scan-notice-ack.service.ts           # notice_version 确认记录管理
│   │   │   └── notice.types.ts
│   │   │
│   │   ├── lock/
│   │   │   ├── operation-lock.service.ts            # shop_operation_lock 获取 / 释放 / 心跳 / 超时回收
│   │   │   └── lock.types.ts
│   │   │
│   │   ├── scan/
│   │   │   │
│   │   │   ├── catalog/                             # ── 全量扫描 ──
│   │   │   │   ├── scan-job.service.ts              # scan_job 生命周期管理
│   │   │   │   ├── scan-task.service.ts             # scan_task 创建与状态流转
│   │   │   │   ├── scan-task-attempt.service.ts     # attempt 创建 / 重试 / 上限判定
│   │   │   │   ├── bulk-query-builder.ts            # 4 类 GraphQL Bulk 查询模板
│   │   │   │   ├── bulk-result-downloader.ts        # 流式 HTTP 下载（处理 URL 过期 / 403 / 410）
│   │   │   │   ├── parsers/
│   │   │   │   │   ├── ndjson-stream-parser.ts      # 通用 NDJSON 流式解析器
│   │   │   │   │   ├── product-media.parser.ts      # __parentId 关联 + position_index 推导
│   │   │   │   │   ├── files.parser.ts
│   │   │   │   │   ├── collection.parser.ts
│   │   │   │   │   └── article.parser.ts
│   │   │   │   ├── staging.service.ts               # stg_* 表批量写入
│   │   │   │   ├── derive.service.ts                # staging → scan_result_target / scan_result_usage
│   │   │   │   ├── publish.service.ts               # scan_result_* → alt_target / image_usage / alt_candidate / projection
│   │   │   │   └── sweep.service.ts                 # 按资源类型切片做缺席清理
│   │   │   │
│   │   │   ├── continuous/                          # ── 增量扫描 ──
│   │   │   │   ├── webhook-event.service.ts         # webhook_event 落库与幂等
│   │   │   │   ├── debounce.service.ts              # Redis debounce / coalesce（30~90s 窗口）
│   │   │   │   ├── gate-checker.ts                  # 四重 gate：锁 / 计划 / scope / 指纹
│   │   │   │   ├── fingerprint.service.ts           # 图片指纹计算（product / collection）
│   │   │   │   ├── product-incremental.service.ts   # 产品增量：读取 → 指纹 → upsert → sweep → 重建投影
│   │   │   │   └── collection-incremental.service.ts
│   │   │   │
│   │   │   ├── scan.types.ts
│   │   │   └── scan.constants.ts
│   │   │
│   │   ├── target/
│   │   │   ├── alt-target.service.ts                # alt_target CRUD、present_status 重算
│   │   │   ├── image-usage.service.ts               # image_usage upsert / sweep / present 聚合
│   │   │   └── target.types.ts
│   │   │
│   │   ├── candidate/
│   │   │   ├── alt-candidate.service.ts             # alt_candidate CRUD
│   │   │   ├── candidate-convergence.ts             # 状态收敛逻辑（decorative / draft / resolved / retryable）
│   │   │   ├── group-projection.service.ts          # candidate_group_projection 重建规则
│   │   │   └── candidate.types.ts
│   │   │
│   │   ├── decorative/
│   │   │   ├── decorative-mark.service.ts           # 标记 / 取消 / 与 candidate 联动
│   │   │   └── decorative.types.ts
│   │   │
│   │   ├── generation/
│   │   │   ├── generation.service.ts                # 生成批次编排（预检 → 预留 → 真值复核 → AI → 收敛）
│   │   │   ├── truth-verifier.ts                    # Shopify 线上 Alt 实时复核
│   │   │   ├── context-builder.ts                   # context_mode 判定（RESOURCE_SPECIFIC / FILE_NEUTRAL / SHARED_NEUTRAL）
│   │   │   ├── prompt-builder.ts                    # AI Prompt 构建（含 SHARED_NEUTRAL 约束）
│   │   │   ├── alt-text-cleaner.ts                  # 生成结果清洗（trim / 英文 / ≤125 字符）
│   │   │   ├── draft.service.ts                     # alt_draft CRUD
│   │   │   └── generation.types.ts
│   │   │
│   │   ├── writeback/
│   │   │   ├── writeback.service.ts                 # 写回批次编排（二次读校验 → 路由 → mutation → 审计）
│   │   │   ├── writeback-router.ts                  # 按 alt_plane 分发到对应 mutation
│   │   │   ├── mutations/
│   │   │   │   ├── file-update.mutation.ts          # fileUpdate
│   │   │   │   ├── collection-update.mutation.ts    # collectionUpdate
│   │   │   │   └── article-update.mutation.ts       # articleUpdate
│   │   │   ├── audit.service.ts                     # audit_log 写入
│   │   │   └── writeback.types.ts
│   │   │
│   │   ├── billing/
│   │   │   ├── subscription.service.ts              # billing_subscription 管理
│   │   │   ├── overage-pack.service.ts              # 超额包购买 / 确认
│   │   │   ├── plan-change.service.ts               # 升级 / 降级 / 回归 Free 全流程
│   │   │   ├── credit/
│   │   │   │   ├── credit-bucket.service.ts         # bucket CRUD / 状态流转
│   │   │   │   ├── credit-reservation.service.ts    # 预留创建 / 逐条消费 / 释放
│   │   │   │   ├── credit-ledger.service.ts         # 流水记录（幂等键）
│   │   │   │   ├── quota-calculator.ts              # 可用额度实时计算
│   │   │   │   ├── consumption-order.ts             # included → welcome → overage 优先级
│   │   │   │   └── free-monthly-grant.service.ts    # Free 月配额发放（cycle_key 防重）
│   │   │   ├── billing-ledger.service.ts
│   │   │   ├── plan-config.ts                       # 计划→配额/价格映射表
│   │   │   └── billing.types.ts
│   │   │
│   │   ├── batch/
│   │   │   ├── job-batch.service.ts                 # job_batch / job_item 管理（进度统计）
│   │   │   └── batch.types.ts
│   │   │
│   │   └── gdpr/
│   │       ├── gdpr.service.ts                      # GDPR 响应 / 店铺数据清理
│   │       └── gdpr.types.ts
│   │
│   │  ┌─────────────────────────────────┐
│   │  │ Shopify API 客户端               │
│   │  └─────────────────────────────────┘
│   │
│   ├── shopify/
│   │   ├── client.ts                                # GraphQL 客户端工厂（带限流 / 重试）
│   │   ├── bulk-operations.ts                       # bulkOperationRunQuery 提交 + 状态查询 + 降级轮询
│   │   ├── queries/
│   │   │   ├── product-media.query.ts               # 产品媒体 Bulk 查询
│   │   │   ├── files.query.ts                       # Files 图片 Bulk 查询
│   │   │   ├── collection.query.ts                  # 集合 Bulk 查询
│   │   │   ├── article.query.ts                     # 文章 Bulk 查询
│   │   │   ├── product-single.query.ts              # 单产品读取（增量 / 真值复核）
│   │   │   ├── collection-single.query.ts
│   │   │   ├── media-image-alt.query.ts             # 单 MediaImage alt 读取
│   │   │   ├── collection-image-alt.query.ts
│   │   │   └── article-image-alt.query.ts
│   │   ├── mutations/
│   │   │   ├── file-update.ts
│   │   │   ├── collection-update.ts
│   │   │   ├── article-update.ts
│   │   │   ├── webhook-subscription-create.ts
│   │   │   ├── app-subscription-create.ts
│   │   │   └── app-purchase-one-time-create.ts
│   │   └── shopify.types.ts
│   │
│   │  ┌─────────────────────────────────┐
│   │  │ AI 网关                          │
│   │  └─────────────────────────────────┘
│   │
│   ├── ai/
│   │   ├── ai-gateway.ts                           # 主模型调用 + 降级策略
│   │   ├── providers/
│   │   │   ├── openai.provider.ts
│   │   │   └── fallback.provider.ts
│   │   └── ai.types.ts
│   │
│   │  ┌─────────────────────────────────┐
│   │  │ BullMQ 队列定义                  │
│   │  └─────────────────────────────────┘
│   │
│   ├── queues/
│   │   ├── connection.ts                            # Redis 连接 + BullMQ 连接选项
│   │   ├── registry.ts                              # 所有队列统一注册
│   │   │
│   │   │  ── 扫描相关 ────
│   │   ├── scan-start.queue.ts
│   │   ├── parse-bulk.queue.ts
│   │   ├── derive-scan.queue.ts
│   │   ├── publish-scan.queue.ts
│   │   ├── continuous-scan-debounce.queue.ts
│   │   ├── continuous-scan-product.queue.ts
│   │   ├── continuous-scan-collection.queue.ts
│   │   │
│   │   │  ── 生成 / 写回 ────
│   │   ├── generate-alt.queue.ts
│   │   ├── writeback.queue.ts
│   │   │
│   │   │  ── 计费 ────
│   │   ├── billing-sync.queue.ts
│   │   ├── quota-grant.queue.ts
│   │   │
│   │   │  ── 运维 ────
│   │   ├── reservation-reaper.queue.ts
│   │   ├── cleanup.queue.ts
│   │   └── gdpr-delete.queue.ts
│   │
│   │  ┌─────────────────────────────────┐
│   │  │ SSE 推送                         │
│   │  └─────────────────────────────────┘
│   │
│   ├── sse/
│   │   ├── progress-publisher.ts                    # Redis PubSub 发布进度事件
│   │   └── sse.service.ts                           # SSE 连接管理
│   │
│   │  ┌─────────────────────────────────┐
│   │  │ 通用工具                         │
│   │  └─────────────────────────────────┘
│   │
│   ├── crypto/
│   │   └── token-encryption.ts                      # access_token AES 加解密
│   │
│   └── utils/
│       ├── logger.ts                                # 结构化日志（shop_domain / batch_id / duration_ms …）
│       ├── errors.ts                                # 自定义错误类（InsufficientCredit / LockConflict …）
│       └── hash.ts                                  # SHA-256 指纹工具
│
│  ══════════════════════════════════════════════════════════
│  Worker 进程
│  ══════════════════════════════════════════════════════════
│
├── worker/
│   ├── index.ts                                     # Worker 启动入口：注册所有 processor + scheduler
│   │
│   ├── processors/                                  # BullMQ Job Processors（编排层，调用 server/modules）
│   │   │
│   │   │  ── 扫描 ────
│   │   ├── scan-start.processor.ts                  # 创建 scan_task → 提交 bulk
│   │   ├── parse-bulk.processor.ts                  # 流式下载 → 流式解析 → staging 落库
│   │   ├── derive-scan.processor.ts                 # staging → scan_result_*
│   │   ├── publish-scan.processor.ts                # scan_result_* → 已发布表（原子事务）
│   │   │
│   │   │  ── 增量扫描 ────
│   │   ├── continuous-scan-debounce.processor.ts    # debounce 窗口合并
│   │   ├── continuous-scan-product.processor.ts     # 产品增量刷新
│   │   ├── continuous-scan-collection.processor.ts
│   │   │
│   │   │  ── 生成 / 写回 ────
│   │   ├── generate-alt.processor.ts                # 真值复核 → 上下文构建 → AI → 草稿 → 扣额度
│   │   ├── writeback.processor.ts                   # 二次读校验 → mutation → 审计
│   │   │
│   │   │  ── 计费 ────
│   │   ├── billing-sync.processor.ts                # 订阅状态同步
│   │   ├── quota-grant.processor.ts                 # included / welcome / free-monthly 发放
│   │   │
│   │   │  ── 运维 ────
│   │   ├── reservation-reaper.processor.ts          # 过期 reservation 释放
│   │   ├── cleanup.processor.ts                     # 草稿 30d / 审计 90d / staging 7d / webhook_event 7d
│   │   └── gdpr-delete.processor.ts                 # GDPR 店铺数据删除
│   │
│   └── schedulers/                                  # 定时调度（分布式锁实现 cron）
│       ├── free-monthly-grant.scheduler.ts          # 每日检查 Free 月配额是否需发放
│       ├── cleanup.scheduler.ts                     # 定期清理过期数据
│       ├── reservation-reaper.scheduler.ts          # 定期释放过期预留
│       └── lock-timeout.scheduler.ts                # 超时锁回收（30min 无心跳）
│
│  ══════════════════════════════════════════════════════════
│  测试
│  ══════════════════════════════════════════════════════════
│
├── tests/
│   └── ...
│
│  ══════════════════════════════════════════════════════════
│  运维脚本
│  ══════════════════════════════════════════════════════════
│
└── scripts/
    ├── dev.sh                                       # 本地开发启动（docker-compose up + dev + worker dev）
    ├── migrate.ts                                   # 数据库迁移脚本
    ├── seed.ts                                      # 种子数据
    └── verify-bulk-samples.ts                       # 验证固化 NDJSON 样本可解析

- `id`
- `shop_id`
- `credit_bucket_id`
- `reservation_id`
- `alt_candidate_id`
- `event_type`（GRANT / RESERVE / CONSUME / RELEASE / EXPIRE / ADJUST）
- `amount`
- `idempotency_key`
- `created_at`

约束：

- `unique(idempotency_key)`

---


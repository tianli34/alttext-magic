
- `id`
- `shop_id`
- `bucket_type`（FREE_MONTHLY_INCLUDED / MONTHLY_INCLUDED / ANNUAL_INCLUDED / WELCOME / OVERAGE_PACK）
- `source_type`（FREE_MONTHLY_GRANT / MONTHLY_GRANT / ANNUAL_GRANT / INSTALL_BONUS / FIRST_PAID_BONUS / PACK_PURCHASE）
- `plan_code`
- `granted_amount`
- `consumed_amount`
- `reserved_amount`
- `remaining_amount`
- `effective_at`
- `expires_at`
- `cycle_key`
- `purchase_id`
- `status`（ACTIVE / EXHAUSTED / EXPIRED）
- `created_at`
- `updated_at`

约束：

- 周期性额度桶增加防重约束：  
  `unique(shop_id, bucket_type, cycle_key)`  
  适用于 `FREE_MONTHLY_INCLUDED / MONTHLY_INCLUDED / ANNUAL_INCLUDED` 且 `cycle_key is not null`

---


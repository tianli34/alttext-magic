## 验证要求
每项任务完成后（若适用），执行以下验证操作：
- npm run lint（代码格式校验）
- npm run typecheck（类型校验）
- npm run test（单元测试）
- npx prisma validate（Prisma 模型校验）

## 输出格式
针对每项任务，需包含以下内容：
1. 变更内容总结
2. 变更的文件列表
3. 执行的命令
4. 验证结果
5. 剩余风险或前置假设

## 验证要求
每项任务完成后（若适用），执行以下验证操作：
- pnpm run lint（代码格式校验）
- pnpm run typecheck（类型校验）
- pnpm run test（单元测试）
- pnpm prisma validate（Prisma 模型校验）

## 输出格式
针对每项任务，需包含以下内容：
1. 变更内容总结
2. 变更的文件列表
3. 执行的命令
4. 验证结果
5. 剩余风险或前置假设



pnpm run typecheck
tsc --noEmit

pnpm prisma validate




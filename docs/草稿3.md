alttext-magic/
│
├── .env.example                                 # 环境变量模板
├── .eslintrc.cjs
├── .gitignore
├── .prettierrc
├── Dockerfile                                   # 生产构建
├── Procfile                                     # Railway 多进程：web + worker
├── README.md
├── docker-compose.yml                           # 本地开发：postgres + redis
├── env.d.ts                                     # Vite / React Router 类型声明
├── package.json
├── railway.toml                                 # Railway 部署配置
├── react-router.config.ts                       # 官方 React Router framework mode 配置（替代 remix.config.js）
├── shopify.app.toml                             # Shopify CLI 应用配置
├── shopify.web.toml                             # Shopify CLI Web 进程配置
├── tsconfig.json
├── vite.config.ts                               # 官方模板 Vite 配置
│
├── prisma/
│   ├── migrations/                              # Prisma 自动迁移目录
│   ├── schema.prisma                            # 全部 26+ 张表定义
│   └── seed.ts                                  # 开发/演示数据种子
│
├── app/
│   ├── db.server.ts                             # 官方模板数据库入口（可转发到 server/db/prisma.server.ts）
│   ├── entry.client.tsx
│   ├── entry.server.tsx
│   ├── root.tsx
│   ├── routes.ts                                # 官方 React Router 路由清单
│   ├── shopify.server.ts                        # Shopify App 配置（OAuth / App Bridge / Session）
│   │
│   ├── routes/
│   │   ├── _index/
│   │   │   ├── route.tsx                        # 官方模板根入口 / 安装页
│   │   │   └── styles.module.css                # 根入口样式
│   │   │
│   │   ├── app.tsx                              # 官方嵌入式应用父布局（认证 / NavMenu / Outlet）
│   │   ├── app._index.tsx                       # 仪表盘（Dashboard）
│   │   ├── app.onboarding.tsx                   # 首次扫描说明确认 & scope 选择
│   │   ├── app.review.tsx                       # 候选审阅 / 编辑 / 装饰性标记
│   │   ├── app.history.tsx                      # 写回审计历史
│   │   ├── app.billing.tsx                      # 计费管理（计划 / 超额包 / 额度详情）
│   │   ├── app.settings.tsx                     # 设置（scope / 通知版本）
│   │   │
│   │   ├── api.bootstrap.ts                     # GET  初始化引导数据
│   │   ├── api.dashboard.ts                     # GET  分组统计
│   │   ├── api.settings.scope.ts                # POST 独立 scope 设置
│   │   ├── api.scan.start.ts                    # POST 启动全量扫描
│   │   ├── api.scan.status.ts                   # GET  扫描进度
│   │   ├── api.candidates.ts                    # GET  候选列表（按 group + status）
│   │   ├── api.candidates.$id.usages.ts         # GET  单条 candidate 的 usage 详情
│   │   ├── api.decorative.mark.ts               # POST 标记装饰性
│   │   ├── api.decorative.unmark.ts             # POST 取消装饰性
│   │   ├── api.generation.preflight.ts          # POST 生成预检（额度足够？）
│   │   ├── api.generation.start.ts              # POST 启动生成批次
│   │   ├── api.draft.update.ts                  # POST 编辑草稿
│   │   ├── api.writeback.start.ts               # POST 启动写回批次
│   │   ├── api.history.ts                       # GET  审计记录
│   │   ├── api.billing.summary.ts               # GET  计费摘要
│   │   ├── api.billing.change-plan.ts           # POST 变更计划
│   │   ├── api.billing.purchase-pack.ts         # POST 购买超额包
│   │   ├── api.billing.callback.ts              # GET/POST 订阅 / 一次性购买确认回调
│   │   ├── api.sse.ts                           # GET  SSE 实时进度推送
│   │   │
│   │   ├── auth.$.tsx                           # 官方 OAuth 路由
│   │   ├── auth.login/
│   │   │   ├── error.server.tsx                 # 官方登录错误边界
│   │   │   └── route.tsx                        # 官方登录入口
│   │   │
│   │   ├── webhooks.app.uninstalled.tsx
│   │   ├── webhooks.customers.data_request.tsx
│   │   ├── webhooks.customers.redact.tsx
│   │   ├── webhooks.shop.redact.tsx
│   │   ├── webhooks.bulk_operations.finish.tsx
│   │   ├── webhooks.products.create.tsx
│   │   ├── webhooks.products.update.tsx
│   │   ├── webhooks.collections.create.tsx
│   │   └── webhooks.collections.update.tsx
│   │
│   ├── components/
│   │   ├── billing/
│   │   │   ├── BillingIntervalToggle.tsx        # 月付/年付切换
│   │   │   ├── OveragePackCard.tsx              # 超额包购买入口
│   │   │   ├── PlanCard.tsx                     # 计划卡片（FREE→MAX）
│   │   │   └── QuotaBreakdown.tsx               # included / welcome / overage 明细
│   │   │
│   │   ├── common/
│   │   │   ├── ImpactScopeSummary.tsx           # 影响范围摘要组件
│   │   │   ├── ProgressBar.tsx
│   │   │   └── StatusBadge.tsx                  # candidate 状态标签
│   │   │
│   │   ├── dashboard/
│   │   │   ├── GroupStatsCard.tsx               # 单组统计卡片（total / hasAlt / missing / decorative）
│   │   │   ├── QuotaSummary.tsx                 # 额度摘要条
│   │   │   ├── ScanStatusBanner.tsx             # 扫描进行中横幅
│   │   │   └── ScopeIndicator.tsx               # 当前有效 scope 指示器
│   │   │
│   │   ├── onboarding/
│   │   │   ├── ScanNotice.tsx                   # 扫描说明（范围/用途/留存/AI 边界）
│   │   │   └── ScopeSelector.tsx                # 四类资源勾选
│   │   │
│   │   ├── review/
│   │   │   ├── CandidateList.tsx                # 候选列表容器
│   │   │   ├── CandidateRow.tsx                 # 单行：缩略图 + 标题 + 位置 + 状态 + 操作
│   │   │   ├── ContextModeBadge.tsx             # RESOURCE_SPECIFIC / SHARED_NEUTRAL 标识
│   │   │   ├── DecorativeToggle.tsx             # 装饰性标记开关
│   │   │   ├── DraftEditor.tsx                  # 可编辑输入框（草稿 / AI 生成文本）
│   │   │   ├── GroupFilter.tsx                  # 图片类型筛选
│   │   │   ├── UsageDetailPopover.tsx           # 共享文件影响范围弹窗
│   │   │   └── WritebackConfirmModal.tsx        # 写回前二次确认
│   │   │
│   │   └── settings/
│   │       └── ScopeSettings.tsx                # scope 独立设置面板
│   │
│   ├── hooks/
│   │   ├── useBatchProgress.ts                  # 生成/写回批次进度
│   │   ├── useQuota.ts                          # 额度数据缓存
│   │   ├── useSSE.ts                            # SSE 连接与事件消费
│   │   └── useScanStatus.ts                     # 扫描状态轮询
│   │
│   └── lib/
│       ├── format.ts                            # 数字/日期格式化
│       └── scope-utils.ts                       # effectiveReadScopeFlags 前端计算
│
├── server/
│   ├── ai/
│   │   ├── ai-gateway.server.ts                 # 主模型调用 + 降级策略
│   │   ├── ai.types.ts
│   │   └── providers/
│   │       ├── fallback.provider.server.ts
│   │       └── openai.provider.server.ts
│   │
│   ├── config/
│   │   ├── constants.ts                         # 枚举（AltPlane / ResourceType / CandidateStatus / ContextMode ...）
│   │   ├── index.server.ts                      # 环境变量读取与校验
│   │   ├── plans.ts                             # 五档计划定义（价格 / 月配额 / 年配额 / 超额包 / 欢迎额度）
│   │   └── queue-names.ts                       # 所有 BullMQ 队列名常量
│   │
│   ├── crypto/
│   │   └── token-encryption.server.ts           # access_token AES 加解密
│   │
│   ├── db/
│   │   └── prisma.server.ts                     # Prisma Client 单例（共享内核）
│   │
│   ├── modules/
│   │   ├── auth/
│   │   │   ├── session.service.server.ts        # Offline token 加解密与持久化
│   │   │   └── webhook-hmac.server.ts           # HMAC 校验工具
│   │   │
│   │   ├── batch/
│   │   │   ├── batch.types.ts
│   │   │   └── job-batch.service.server.ts      # job_batch / job_item 管理（进度统计）
│   │   │
│   │   ├── billing/
│   │   │   ├── billing-ledger.service.server.ts
│   │   │   ├── billing.types.ts
│   │   │   ├── overage-pack.service.server.ts   # 超额包购买 / 确认
│   │   │   ├── plan-change.service.server.ts    # 升级 / 降级 / 回归 Free 全流程
│   │   │   ├── plan-config.ts                   # 计划→配额/价格映射表
│   │   │   ├── subscription.service.server.ts   # billing_subscription 管理
│   │   │   └── credit/
│   │   │       ├── consumption-order.ts         # included → welcome → overage 优先级
│   │   │       ├── credit-bucket.service.server.ts
│   │   │       ├── credit-ledger.service.server.ts
│   │   │       ├── credit-reservation.service.server.ts
│   │   │       ├── free-monthly-grant.service.server.ts
│   │   │       └── quota-calculator.ts
│   │   │
│   │   ├── candidate/
│   │   │   ├── alt-candidate.service.server.ts  # alt_candidate CRUD
│   │   │   ├── candidate-convergence.ts         # 状态收敛逻辑（decorative / draft / resolved / retryable）
│   │   │   ├── candidate.types.ts
│   │   │   └── group-projection.service.server.ts
│   │   │
│   │   ├── decorative/
│   │   │   ├── decorative-mark.service.server.ts
│   │   │   └── decorative.types.ts
│   │   │
│   │   ├── gdpr/
│   │   │   ├── gdpr.service.server.ts           # GDPR 响应 / 店铺数据清理
│   │   │   └── gdpr.types.ts
│   │   │
│   │   ├── generation/
│   │   │   ├── alt-text-cleaner.ts              # 生成结果清洗（trim / 英文 / ≤125 字符）
│   │   │   ├── context-builder.ts               # context_mode 判定（RESOURCE_SPECIFIC / FILE_NEUTRAL / SHARED_NEUTRAL）
│   │   │   ├── draft.service.server.ts          # alt_draft CRUD
│   │   │   ├── generation.service.server.ts     # 生成批次编排（预检 → 预留 → 真值复核 → AI → 收敛）
│   │   │   ├── generation.types.ts
│   │   │   ├── prompt-builder.ts                # AI Prompt 构建（含 SHARED_NEUTRAL 约束）
│   │   │   └── truth-verifier.server.ts         # Shopify 线上 Alt 实时复核
│   │   │
│   │   ├── lock/
│   │   │   ├── lock.types.ts
│   │   │   └── operation-lock.service.server.ts # shop_operation_lock 获取 / 释放 / 心跳 / 超时回收
│   │   │
│   │   ├── notice/
│   │   │   ├── notice.types.ts
│   │   │   └── scan-notice-ack.service.server.ts
│   │   │
│   │   ├── scan/
│   │   │   ├── scan.constants.ts
│   │   │   ├── scan.types.ts
│   │   │   ├── catalog/
│   │   │   │   ├── bulk-query-builder.ts                # 4 类 GraphQL Bulk 查询模板
│   │   │   │   ├── bulk-result-downloader.server.ts     # 流式 HTTP 下载（处理 URL 过期 / 403 / 410）
│   │   │   │   ├── derive.service.server.ts             # staging → scan_result_target / scan_result_usage
│   │   │   │   ├── publish.service.server.ts            # scan_result_* → alt_target / image_usage / alt_candidate / projection
│   │   │   │   ├── scan-job.service.server.ts           # scan_job 生命周期管理
│   │   │   │   ├── scan-task-attempt.service.server.ts  # attempt 创建 / 重试 / 上限判定
│   │   │   │   ├── scan-task.service.server.ts          # scan_task 创建与状态流转
│   │   │   │   ├── staging.service.server.ts            # stg_* 表批量写入
│   │   │   │   ├── sweep.service.server.ts              # 按资源类型切片做缺席清理
│   │   │   │   └── parsers/
│   │   │   │       ├── article.parser.ts
│   │   │   │       ├── collection.parser.ts
│   │   │   │       ├── files.parser.ts
│   │   │   │       ├── ndjson-stream-parser.ts
│   │   │   │       └── product-media.parser.ts
│   │   │   │
│   │   │   └── continuous/
│   │   │       ├── collection-incremental.service.server.ts
│   │   │       ├── debounce.service.server.ts
│   │   │       ├── fingerprint.service.server.ts
│   │   │       ├── gate-checker.server.ts
│   │   │       ├── product-incremental.service.server.ts
│   │   │       └── webhook-event.service.server.ts
│   │   │
│   │   ├── shop/
│   │   │   ├── scope.service.server.ts          # scan_scope_flags / last_published_scope_flags / effective 计算
│   │   │   ├── shop.service.server.ts           # 店铺 CRUD、安装初始化、卸载清理
│   │   │   └── shop.types.ts
│   │   │
│   │   ├── target/
│   │   │   ├── alt-target.service.server.ts     # alt_target CRUD、present_status 重算
│   │   │   ├── image-usage.service.server.ts    # image_usage upsert / sweep / present 聚合
│   │   │   └── target.types.ts
│   │   │
│   │   └── writeback/
│   │       ├── audit.service.server.ts          # audit_log 写入
│   │       ├── writeback-router.server.ts       # 按 alt_plane 分发到对应 mutation
│   │       ├── writeback.service.server.ts      # 写回批次编排（二次读校验 → 路由 → mutation → 审计）
│   │       ├── writeback.types.ts
│   │       └── mutations/
│   │           ├── article-update.mutation.server.ts
│   │           ├── collection-update.mutation.server.ts
│   │           └── file-update.mutation.server.ts
│   │
│   ├── queues/
│   │   ├── cleanup.queue.server.ts
│   │   ├── connection.server.ts                 # Redis 连接 + BullMQ 连接选项
│   │   ├── continuous-scan-collection.queue.server.ts
│   │   ├── continuous-scan-debounce.queue.server.ts
│   │   ├── continuous-scan-product.queue.server.ts
│   │   ├── derive-scan.queue.server.ts
│   │   ├── gdpr-delete.queue.server.ts
│   │   ├── generate-alt.queue.server.ts
│   │   ├── parse-bulk.queue.server.ts
│   │   ├── publish-scan.queue.server.ts
│   │   ├── quota-grant.queue.server.ts
│   │   ├── registry.server.ts                   # 所有队列统一注册
│   │   ├── reservation-reaper.queue.server.ts
│   │   ├── scan-start.queue.server.ts
│   │   ├── billing-sync.queue.server.ts
│   │   └── writeback.queue.server.ts
│   │
│   ├── shopify/
│   │   ├── bulk-operations.server.ts            # bulkOperationRunQuery 提交 + 状态查询 + 降级轮询
│   │   ├── client.server.ts                     # GraphQL 客户端工厂（带限流 / 重试）
│   │   ├── shopify.types.ts
│   │   ├── mutations/
│   │   │   ├── app-purchase-one-time-create.server.ts
│   │   │   ├── app-subscription-create.server.ts
│   │   │   ├── article-update.server.ts
│   │   │   ├── collection-update.server.ts
│   │   │   ├── file-update.server.ts
│   │   │   └── webhook-subscription-create.server.ts
│   │   └── queries/
│   │       ├── article-image-alt.query.ts
│   │       ├── article.query.ts
│   │       ├── collection-image-alt.query.ts
│   │       ├── collection-single.query.ts
│   │       ├── collection.query.ts
│   │       ├── files.query.ts
│   │       ├── media-image-alt.query.ts
│   │       ├── product-media.query.ts
│   │       └── product-single.query.ts
│   │
│   ├── sse/
│   │   ├── progress-publisher.server.ts         # Redis PubSub 发布进度事件
│   │   └── sse.service.server.ts                # SSE 连接管理
│   │
│   └── utils/
│       ├── errors.ts                            # 自定义错误类（InsufficientCredit / LockConflict …）
│       ├── hash.ts                              # SHA-256 指纹工具
│       └── logger.server.ts                     # 结构化日志（shop_domain / batch_id / duration_ms …）
│
├── worker/
│   ├── index.ts                                 # Worker 启动入口：注册所有 processor + scheduler
│   │
│   ├── processors/
│   │   ├── billing-sync.processor.ts            # 订阅状态同步
│   │   ├── cleanup.processor.ts                 # 草稿 30d / 审计 90d / staging 7d / webhook_event 7d
│   │   ├── continuous-scan-collection.processor.ts
│   │   ├── continuous-scan-debounce.processor.ts # debounce 窗口合并
│   │   ├── continuous-scan-product.processor.ts  # 产品增量刷新
│   │   ├── derive-scan.processor.ts             # staging → scan_result_*
│   │   ├── gdpr-delete.processor.ts             # GDPR 店铺数据删除
│   │   ├── generate-alt.processor.ts            # 真值复核 → 上下文构建 → AI → 草稿 → 扣额度
│   │   ├── parse-bulk.processor.ts              # 流式下载 → 流式解析 → staging 落库
│   │   ├── publish-scan.processor.ts            # scan_result_* → 已发布表（原子事务）
│   │   ├── quota-grant.processor.ts             # included / welcome / free-monthly 发放
│   │   ├── reservation-reaper.processor.ts      # 过期 reservation 释放
│   │   ├── scan-start.processor.ts              # 创建 scan_task → 提交 bulk
│   │   └── writeback.processor.ts               # 二次读校验 → mutation → 审计
│   │
│   └── schedulers/
│       ├── cleanup.scheduler.ts                 # 定期清理过期数据
│       ├── free-monthly-grant.scheduler.ts      # 每日检查 Free 月配额是否需发放
│       ├── lock-timeout.scheduler.ts            # 超时锁回收（30min 无心跳）
│       └── reservation-reaper.scheduler.ts      # 定期释放过期预留
│
├── tests/
│
└── scripts/
    ├── dev.sh                                   # 本地开发启动（docker-compose up + shopify app dev + worker dev）
    ├── migrate.ts                               # 数据库迁移脚本
    ├── seed.ts                                  # 种子数据
    └── verify-bulk-samples.ts                   # 验证固化 NDJSON 样本可解析
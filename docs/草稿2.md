



















解释以下验收任务

### 验收) 重复 webhook 幂等
- 同一个 `webhook_id` 重放
- 不重复插入 / 不重复 enqueue
- HTTP 仍返回 200

















对于以下验收任务，具体怎么操作？

### 5) GDPR
- 三类 GDPR webhook 均返回 200
- `webhook_event` 有记录
- worker 标记 processed



shopify app webhook trigger --topic customers/data_request --address https://nonidolatrous-nayeli-gushily.ngrok-free.dev/webhooks


docker exec alttext-magic-redis-1 redis-cli LLEN shopify-webhooks:completed
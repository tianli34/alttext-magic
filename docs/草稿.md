

<问题>
`.tmp-lock-cjs/` 和 `.tmp-lock-test/`是应该删除的吗？


<content>
已实现 shop 级互斥锁服务，核心在 [operation-lock.service.ts](/E:/alttext-magic/server/modules/lock/operation-lock.service.ts:60) 和 [lock.types.ts](/E:/alttext-magic/server/modules/lock/lock.types.ts:13)。`acquireLock`、`releaseLock`、`heartbeatLock`、`cleanupExpiredLocks` 都封在 service 层，按要求使用了 Prisma 事务和 `SELECT ... FOR UPDATE`；owner 目前以 `batchId` 判定，因为现有 schema 只有 `batch_id` 可持久化。另补了清理入口 [lock-timeout.scheduler.ts](/E:/alttext-magic/worker/schedulers/lock-timeout.scheduler.ts:16)，避免 SQL 外泄到 route/scheduler。

验证方面新增了集成测试 [operation-lock.service.e2e.ts](/E:/alttext-magic/tests/operation-lock.service.e2e.ts:60)，覆盖 acquire 冲突、release 后重新 acquire、heartbeat 续租、超时 cleanup 回收。我在跑测试时顺手修掉了一个真实时区 bug：`timestamp without time zone` 和 SQL `CURRENT_TIMESTAMP` 混用会误回收刚 heartbeat 的锁，现已统一用应用层 `Date` 比较。`npx tsc --noEmit` 已通过，编译后执行 `node .tmp-lock-cjs/tests/operation-lock.service.e2e.js` 也已通过。状态文档已极简更新 [docs/STATUS.md](/E:/alttext-magic/docs/STATUS.md:14)。

验证时生成了未跟踪的临时目录 `.tmp-lock-cjs/` 和 `.tmp-lock-test/`；当前环境策略阻止我递归删除它们。
</content>



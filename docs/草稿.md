







【指令】
以下是两篇对某软件扫描设计的分析，请将它们整合成一篇，如果存在互相矛盾或冲突，请指出。

---

已把 migration 真正落成，并补了数据库结构校验脚本。

变更在 [migration.sql](/E:/alttext-magic/prisma/migrations/20260408193000_phase2_core_schema_finalize/migration.sql)、[validate-database-schema.ts](/E:/alttext-magic/scripts/validate-database-schema.ts)、[package.json](/E:/alttext-magic/package.json)、[STATUS.md](/E:/alttext-magic/docs/STATUS.md)。这份 migration 会把当前 Prisma schema 的 Phase 2 表、关系、索引全部落库，并包含你要求的关键唯一约束：
- `alt_target(shop_id, alt_plane, write_target_id, locale)`
- `alt_candidate(alt_target_id)`
- `candidate_group_projection(shop_id, group_type, alt_candidate_id)`
- `credit_bucket(shop_id, bucket_type, cycle_key)`
- `shop_operation_lock(shop_id)`，在实现上由主键保证唯一

我额外修了 `webhook_events` 的兼容迁移，避免旧数据在新增 `idempotency_key` 和把 `status` 从 `TEXT` 转成 enum 时卡死。新增的 `pnpm run prisma:schema:check` 会校验核心表和上述关键索引是否存在。

验证结果：
- `npx prisma migrate dev` 已在空库 `alttext_magic_migrate_dev_test` 跑通
- `npx prisma migrate deploy` 已在空库 `alttext_magic_migrate_deploy_test` 可重放
- 默认 `.env` 指向的 `mydatabase` 也已恢复到与 migration 一致，`npx prisma migrate dev` 现在返回 `Already in sync`
- `pnpm run prisma:schema:check` 已在空库和默认开发库通过

额外说明：仓库级 `pnpm exec tsc --noEmit` 仍有既有的 `@prisma/client` 类型错误，集中在现有若干文件，不是这次 migration/校验脚本引入的。



【问题】
以下是任务和AI跑完任务后的输出，请解释【AI输出】是什么意思？


---

<content>

## Task P3-12：发布后收敛逻辑

### 目标
补完 publish 后的业务收敛：FILE_ALT 存在性、candidate 状态、projection、店铺元数据。

### 主要工作
- 基于 `image_usage` 的 PRESENT 集合重算 `FILE_ALT` target 存在性
- candidate 发布收敛：
  - 根据 target 状态
  - decorative_mark
  - draft 状态
  - 派生 `alt_candidate.status`
- 重建 `candidate_group_projection`
  - 覆盖 `FILE_ALT / COLLECTION / ARTICLE`
- 更新店铺字段：
  - `shops.last_published_scope_flags`
  - `shops.last_published_at`
  - `shops.last_published_scan_job_id`
- 最终更新 `scan_job` 为：
  - `SUCCESS / PARTIAL_SUCCESS / FAILED`
- 释放 SCAN 锁

### 交付物
- 发布后收敛逻辑
- projection rebuild 逻辑
- 店铺 publish 元数据更新

### 验收标准
- `FILE_ALT` 是否存在与 usage PRESENT 集合一致
- projection 数据正确生成
- publish 元数据正确更新
- 锁正常释放

---

【AI输出】
这次没有重写 `publish` 主事务，因为 [publish.service.ts]里已经覆盖了你列出的大部分收敛项：`FILE_ALT` 按 `image_usage PRESENT` 重算、candidate 状态派生、`candidate_group_projection` 重建、`shops.last_published_*` 更新、`scan_job.publish_status` 更新。实际缺口是“终态收尾”这一步。

我补了两条收尾链路：在 [operation-lock.service.ts]和 [lock.types.ts]新增按 `operationType` 释放锁的能力，解决了文档要求“发布后释放 SCAN 锁”但 worker 拿不到原始 `batchId` 的代码冲突；然后把它接到 [derive-scan.processor.ts]和 [publish-scan.processor.ts]，保证 `scan_job` 收敛为 `FAILED` 时立即释放 `SCAN` 锁，`publish_scan_result` 成功或抛错时也会释放锁，避免店铺长期卡死。对应测试已补到 [tests/derive-scan.test.ts]和 [tests/publish-scan.test.ts]，并最小更新了 [docs/STATUS.md]。

验证已通过：`pnpm typecheck`、`node --import tsx tests\publish-scan.test.ts`、`node --import tsx tests\derive-scan.test.ts`。当前 worktree 里还有不少与本任务无关的既有改动，我没有回退它们。

</content>

---



这次把 parse 阶段的“Bulk URL 过期恢复”接进了真实状态机里：`BULK_OPERATIONS_FINISH(COMPLETED)` 现在只把 attempt 置为 `READY_TO_PARSE`，task 继续保持 `RUNNING`；真正 parse 成功后，才在 [parse-bulk.processor.ts] 把 attempt/task 标成成功。针对 `403/404/expired/timeout/fetch failed/body null` 这类下载失败，我在同文件里补了错误分类、`max_parse_attempts` 上限判断、attempt 失败落库、task 回到 `PENDING` 并自动重新提交 bulk；超限后会把 task 置 `FAILED`，并触发 `scan_job` 汇总收敛。相关 task 状态辅助函数加在了 [scan-task.service.ts]，webhook 完成态修正在 [scan-task-attempt.service.ts]。

P3-13 SSE 进度推送 + 扫描进度页前端 已完成。修复了 2 个 TypeScript 编译错误后，`tsc --noEmit` 和 `tsc --noEmit -p tsconfig.worker.json` 均通过。

## 修复的 TS 编译错误

1. **[`StatusBadge.tsx`]** — 移除了 `<s-box>` 的 `display="inline"` 属性（s-box 不支持 "inline" 值）
2. **[`api.sse.tsx`]** — 移除了无效的 `controller as WritableStreamDefaultWriter` 类型转换
3. **[`sse.service.ts`]** — 将 writer 参数类型从 `WritableStreamDefaultWriter<Uint8Array>` 简化为 `{ write, close }` 结构类型，消除适配层类型不匹配

## 已完成清单

| 模块 | 文件 | 说明 |
|------|------|------|
| 常量 | [`scan.constants.ts`] | `SCAN_PHASE` 枚举 |
| Redis 进度 | [`progress-publisher.ts`]| `initScanProgress`/`updateScanProgressPhase`/`incrementScanProgress`/`setScanProgressStatus`/`getScanProgress` |
| Worker 集成 | [`scan-start.service.ts`]、[`parse-bulk.processor.ts`]、[`derive-scan.processor.ts`]、[`publish-scan.processor.ts`] | 6 个关键阶段写 Redis 进度 |
| SSE 服务 | [`sse.service.ts`]| 2s 轮询 Redis → SSE 事件流 |
| SSE 端点 | [`api.sse.tsx`]| `GET /api/sse?scanJobId=` 鉴权 + 流式响应 |
| Hooks | [`useSSE.ts`]、[`useScanStatus.ts`]、[`useBatchProgress.ts`]| SSE 连接/状态查询/聚合逻辑 |
| 组件 | [`ProgressBar.tsx`]、[`StatusBadge.tsx`]、[`ScanStatusBanner.tsx`]| 进度条/状态徽章/进度页 |
| Dashboard | [`app._index.tsx`]| 扫描进行中展示进度页 |
| 状态文档 | [`STATUS.md`]| 新增 P3-13 完成记录 |
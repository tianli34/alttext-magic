已完成。

核心改动在 [publish.service.ts]和新的 publish 队列链路上：新增了 [publish-scan.queue.ts]、[publish-scan.processor.ts]，并在 [worker/index.ts]注册了 `publish-scan` worker。`derive` / `parse` 终态收敛后现在会在 job 首次进入 `SUCCESS` / `PARTIAL_SUCCESS` 时投递 publish，`FAILED` 则收敛为 `NOT_PUBLISHED`。

发布事务现在做到这些事：
- 成功切片原子发布到 `alt_target` / `image_usage`
- 失败切片保留旧已发布数据
- 成功 `usage_type` 缺席项 sweep 为 `NOT_FOUND`
- `FILE_ALT` 按 `PRESENT image_usage` 重算 target 存在性，避免单侧成功误删另一侧旧数据
- 同事务收敛 `alt_candidate` 和 `candidate_group_projection`
- 发布完成后更新 `shops.last_published_*` 和 `scan_job.publish_status`

状态已按要求最小更新到 [docs/STATUS.md]。

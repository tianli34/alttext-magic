已完成 `BULK_OPERATIONS_FINISH` 这条链路：在 [scan-start.service.ts](/E:/alttext-magic/server/modules/scan/catalog/scan-start.service.ts) 里补了 webhook 终态收敛、`COMPLETED` 时的 `parse_bulk_to_staging` 入队，以及 `trySubmitNextBatch(scanJobId)` 的 Shop 级 Redis 分布式锁。锁实现放在 [bulk-slot-lock.server.ts](/E:/alttext-magic/server/modules/scan/catalog/bulk-slot-lock.server.ts)，parse 队列生产者放在 [parse-bulk.queue.ts](/E:/alttext-magic/server/queues/parse-bulk.queue.ts)，队列名也补到了 [queue-names.ts](/E:/alttext-magic/server/config/queue-names.ts)。

另外把 [scan-task-attempt.service.ts](/E:/alttext-magic/server/modules/scan/catalog/scan-task-attempt.service.ts) 做成了幂等终态更新：重复 webhook 会直接短路，不会重复 parse、不重复补位；并发 `trySubmitNextBatch` 只会有一个持锁成功，避免同一 task 被重复提交。测试加在 [bulk-operations-finish.webhook.test.ts](/E:/alttext-magic/tests/bulk-operations-finish.webhook.test.ts)，`docs/STATUS.md` 也做了极简更新。

已验证：
- `npm run typecheck`
- `npx tsx tests/bulk-operations-finish.webhook.test.ts`

一个需要说明的文档/代码冲突是：当前 schema 没有独立的 `error_code` 字段，也没有可持久化的 `CANCELED` attempt 枚举，所以我把 `error_code` 合并进错误消息持久化，并将 `CANCELED` 按失败终态收敛处理。

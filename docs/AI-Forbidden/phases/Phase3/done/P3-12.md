这次没有重写 `publish` 主事务，因为 [publish.service.ts]里已经覆盖了你列出的大部分收敛项：`FILE_ALT` 按 `image_usage PRESENT` 重算、candidate 状态派生、`candidate_group_projection` 重建、`shops.last_published_*` 更新、`scan_job.publish_status` 更新。实际缺口是“终态收尾”这一步。

我补了两条收尾链路：在 [operation-lock.service.ts]和 [lock.types.ts]新增按 `operationType` 释放锁的能力，解决了文档要求“发布后释放 SCAN 锁”但 worker 拿不到原始 `batchId` 的代码冲突；然后把它接到 [derive-scan.processor.ts]和 [publish-scan.processor.ts]，保证 `scan_job` 收敛为 `FAILED` 时立即释放 `SCAN` 锁，`publish_scan_result` 成功或抛错时也会释放锁，避免店铺长期卡死。对应测试已补到 [tests/derive-scan.test.ts]和 [tests/publish-scan.test.ts]，并最小更新了 [docs/STATUS.md]。

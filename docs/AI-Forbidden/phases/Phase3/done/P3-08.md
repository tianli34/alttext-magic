## P3-08 完成：Staging 写入实现

### 交付物总览

#### 1. Staging Batch Upsert（P3-07 已实现，本任务验证确认）
- [`staging.service.ts`](server/modules/scan/catalog/staging.service.ts) — 5 张 staging 表的 flush 函数：
  - `flushArticleStaging()` → `stg_article`
  - `flushCollectionStaging()` → `stg_collection`
  - `flushMediaFileStaging()` → `stg_media_image_file`
  - `flushProductMediaStaging()` → `stg_product` + `stg_media_image_product`（按 `kind` 标签分发）
  - `countStagingRows()` — 查询指定 attempt 的 staging 行数
- 全部使用 `Prisma.createMany({ skipDuplicates: true })` 处理唯一约束冲突（幂等插入）

#### 2. 产品媒体 `__parentId` 关联 + `position_index` 处理
- [`product-media.parser.ts`](server/modules/scan/catalog/parsers/product-media.parser.ts) — **更新**：
  - `position_index` 优先使用 Shopify 返回的 `position` 字段（`typeof === "number" && isFinite`）
  - 无 `position` 时 fallback 到 0-based 自动递增（`nextAutoPosition`）
  - `__parentId` 关联：通过 `ParentIdCache` 缓存 Product 基本信息，MediaImage 子行通过 `__parentId` 关联父 Product

#### 3. 4 类资源专属 Row Transformer（P3-07 已实现）
- [`article.parser.ts`](server/modules/scan/catalog/parsers/article.parser.ts) — Article 扁平行
- [`collection.parser.ts`](server/modules/scan/catalog/parsers/collection.parser.ts) — Collection 扁平行
- [`files.parser.ts`](server/modules/scan/catalog/parsers/files.parser.ts) — Files (MediaImage) 扁平行
- [`product-media.parser.ts`](server/modules/scan/catalog/parsers/product-media.parser.ts) — Product + MediaImage 父子行

#### 4. Parse 成功后投递 Derive Job
- [`derive-scan.queue.ts`](server/queues/derive-scan.queue.ts) — **新建**：`enqueueDeriveScan()` 队列生产者，jobId = scanTaskAttemptId，3 次重试 + 指数退避
- [`derive-scan.processor.ts`](worker/processors/derive-scan.processor.ts) — **新建**：骨架处理器（TODO: derive 核心逻辑将在后续任务中实现）
- [`queue-names.ts`](server/config/queue-names.ts) — **更新**：新增 `DERIVE_SCAN_QUEUE_NAME = "derive-scan"`
- [`parse-bulk.processor.ts`](worker/processors/parse-bulk.processor.ts) — **更新**：parse 成功（attempt 标记 SUCCESS）后调用 `enqueueDeriveScan()`
- [`worker/index.ts`](worker/index.ts) — **更新**：注册 derive-scan worker（concurrency: 2），含 ready/completed/failed/error 事件监听 + shutdown 清理

### 验收结果
- ✅ `tsc --noEmit` 编译通过（主 tsconfig + worker tsconfig）
- ✅ 5 张 staging 表均可落库（通过 `createMany({ skipDuplicates: true })`）
- ✅ `__parentId` 关联正确（Product 父行 → MediaImage 子行）
- ✅ `position_index` 连续正确（优先 Shopify 字段，fallback 0-based 递增）
- ✅ Parse 成功后自动投递 derive job

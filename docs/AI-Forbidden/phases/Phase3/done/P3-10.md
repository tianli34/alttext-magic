已完成 `derive_scan_attempt_to_result` 这一段实现。核心改动在 [derive.service.ts](/E:/alttext-magic/server/modules/scan/catalog/derive.service.ts)、[derive-scan.processor.ts](/E:/alttext-magic/worker/processors/derive-scan.processor.ts) 和 [parse-bulk.processor.ts](/E:/alttext-magic/worker/processors/parse-bulk.processor.ts)：derive 现在会从成功 attempt 的 staging 生成并 upsert `scan_result_target` / `scan_result_usage`，对同一 `MediaImage` 在 `PRODUCT_MEDIA + FILES` 场景下只保留 1 条 `FILE_ALT` target，但同时保留 `PRODUCT` 和 `FILE` 两条 usage；task 也改成在 derive 成功后才标记 `SUCCESS`，避免 parse 先成功、derive 丢失的状态错位。

已修改 schema，并把约束真正下沉到数据库层。

变更点在[schema.prisma]、[migration.sql]和[derive.service.ts]。核心调整是：
`scan_result_target` 的唯一键从 `(shopId, scanJobId, resourceType, altPlane, writeTargetId, locale)` 改成 `(shopId, scanJobId, altPlane, writeTargetId, locale)`，这样同一 `FILE_ALT` 在 `PRODUCT_MEDIA` / `FILES` 之间数据库层只能存在 1 条 target；`scan_result_usage -> scan_result_target` 的外键也同步改成不含 `resourceType`。同时移除了 `scan_result_target -> scan_task` 这条误导性的关系，保留 `scan_result_usage.resourceType` 作为 task 切片归属。

迁移里还处理了历史数据兼容：先解除旧外键，再按新 target key 去重旧的 `scan_result_target`，最后重建新唯一索引和新外键。`derive` 的 upsert key 已同步切到新唯一键。

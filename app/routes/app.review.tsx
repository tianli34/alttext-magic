import { AppPlaceholderPage } from "../components/common/AppPlaceholderPage";

export default function AppReviewPage() {
  return (
    <AppPlaceholderPage
      title="Review"
      description="审核页面占位中，后续将在这里展示候选 Alt Text 列表、编辑区和批量确认操作。"
    />
  );
}

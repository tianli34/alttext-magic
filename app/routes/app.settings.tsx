import { AppPlaceholderPage } from "../components/common/AppPlaceholderPage";

export default function AppSettingsPage() {
  return (
    <AppPlaceholderPage
      title="Settings"
      description="设置页面占位中，后续将在这里维护扫描范围、生成策略和应用偏好配置。"
    />
  );
}
